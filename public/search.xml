<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Humans and Agents in Software Engineering Loops</title>
    <url>/posts/1409738761.html</url>
    <content><![CDATA[<p>人类应该远离软件开发过程让 AI 自由发挥（vibe code），还是需要开发者在循环中检查每一行代码？我相信答案应该聚焦于将想法转化为成果这一目标。我们人类的正确位置是构建和管理这个工作循环，而不是要么完全交给 Agent，要么微观管理它们的产出。让我们称这种方式为”在循环之上”（on the loop）。</p>
<p>作为软件创造者，我们通过将想法转化为可运行的软件并在学习和演进想法的过程中不断迭代来构建成果。这是”为什么循环”（why loop）。在 AI 崛起之前，人类将运行这个循环，因为我们才是想要其产出的人。</p>
<p>构建软件的过程是”如何循环”（how loop）。如何循环涉及创建、选择和使用中间产物，如代码、测试、工具和基础设施。它还可能涉及技术设计和架构决策记录（ADR）等文档。我们习惯于将这些视为交付物，但中间产物实际上只是达到目的的手段。</p>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/chenzhenyang.github.io/master/images/humans-agents-image1.png" alt="The software delivery feedback loops: An upper &quot;why&quot; loop connected to a lower &quot;how&quot; loop. The why loop iterates over an idea and working software. The how loop iterates over interim artefacts like specs, code, and tests."></p>
<p><em>图 1：为什么循环在想法和软件之间迭代，如何循环在构建软件的过程中迭代</em></p>
<p>实际上，如何循环包含多个循环。最外层的如何循环为为什么循环指定并交付可运行的软件。最内层的循环生成并测试代码。中间的循环将更高层次的工作分解为更小的任务供下层循环实现，然后验证结果。</p>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/chenzhenyang.github.io/master/images/humans-agents-image2.png" alt="Multiple levels of &quot;how&quot; loops supporting the &quot;why&quot; loop. An outer loop iterates on a feature. A middle loop iterates on stories. An inner loop iterates on code."></p>
<p><em>图 2：如何循环有多个层次的内层循环，它们工作在完整实现的更小增量上</em></p>
<p>这些循环可能遵循设计评审和测试阶段等实践。它们可能通过应用微服务或 CUPID 等架构方法和设计模式来构建系统。就像从这些实践和模式中产生的中间产物一样，它们都是实现我们真正关心的成果的手段。</p>
<p>但也许我们并不关心用于实现目标的手段？也许我们可以让大语言模型（LLM）随意运行如何循环？</p>
<h2><span id="人类在循环之外">人类在循环之外</span></h2><p>许多人已经发现了让人类只专注于为什么循环而将如何循环交给 Agent 处理的乐趣。这是”氛围编程”（vibe coding）的常见定义。规范驱动开发（Spec Driven Development, SDD）的一些解释也大致相同，人类投入精力编写我们想要的成果，但不规定 LLM 应该如何实现它。</p>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/chenzhenyang.github.io/master/images/humans-agents-image3.png" alt="Humans Outside the loop: An upper &quot;why&quot; loop with a human on top. The loop iterates over an idea and working software. This is connected to a lower &quot;how&quot; loop by a robot, which iterates over interim artefacts like code."></p>
<p><em>图 3：人类运行为什么循环，Agent 运行如何循环</em></p>
<p>人类远离如何循环的吸引力在于，为什么循环才是我们真正关心的。软件开发是一个混乱的领域，不可避免地会陷入过度工程化的过程和应对技术债务的泥潭。而且迄今为止，每个新的 LLM 模型都更擅长接受用户提示并输出可运行的软件。如果你对其输出不满意，告诉 LLM，它会给你另一个迭代版本。</p>
<p>如果 LLM 可以在没有我们的情况下编写和修改代码，我们还在乎代码是否”干净”吗？只要 LLM 能理解，变量名是否清晰表达其目的并不重要。也许我们甚至不需要关心软件是用什么语言编写的？</p>
<p>我们关心的是外部质量，而不是为了内部质量本身。外部质量是我们作为软件用户或其他利益相关者所体验到的。功能质量是必须的，系统需要正确工作。对于生产软件，我们还关心非功能性的运营质量。我们的系统不应该崩溃，应该快速运行，我们也不希望它将机密数据发布到社交媒体网站。我们不想产生巨额的云托管费用，而且在许多领域我们需要通过合规性审计。</p>
<p>当内部质量影响外部成果时，我们才关心它。当人类编码者在代码库中爬取、添加功能和修复 bug 时，他们可以在干净的代码库中更快、更可靠地完成工作。但 LLM 并不关心开发者体验，不是吗？</p>
<p>理论上，我们的 LLM Agent 可以生成一个极其复杂的意大利面条式代码库，通过运行临时 shell 命令来测试和修复它，最终产生一个正确、合规、高性能的系统。我们只需让我们的 swarm（集群）像 Ralph Wiggum 一样在它上面工作，运行在从它们漂浮的沸腾海洋中获取能源的数据中心里，最终我们会达到目标。</p>
<p>实际上，设计良好、结构清晰的代码库相比混乱的代码库具有对外部重要的优势。当 LLM 能更快地理解和修改它们工作的代码时，它们会工作得更快，更少陷入螺旋。我们确实在乎构建我们所需系统的时间和成本。</p>
<h2><span id="人类在循环之中">人类在循环之中</span></h2><p>一些开发者认为，保持内部质量的唯一方法是紧密参与如何循环的最低层次。通常，当 Agent 在某些破损的代码上陷入螺旋时，人类开发者可以在几秒钟内理解并修复它。在许多情况下，人类的经验和判断力仍然超过 LLM。</p>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/chenzhenyang.github.io/master/images/humans-agents-image4.png" alt="Humans in the loop: A single &quot;why+how&quot; loop with a human at the top and a robot at the bottom. The loop iterates over idea, interim artefacts like code and tests, and the working software."></p>
<p><em>图 4：人类运行为什么循环和如何循环</em></p>
<p>当人们谈论”人类在循环中”时，他们通常指的是人类作为最内层循环（代码生成发生的地方）的守门人，例如手动检查 LLM 创建的每一行代码。</p>
<p>当我们坚持过于紧密地参与过程时，挑战在于我们会成为瓶颈。Agent 生成代码的速度比人类手动检查的速度快。关于使用 AI 的开发者生产力的报告显示出混合的结果，这可能至少部分是因为人类花费更多时间指定和审查代码，而不是通过使用 LLM 生成代码所节省的时间。</p>
<p>我们需要采用经典的”左移”（shift left）思维。曾经，我们编写所有代码，传递给 QA 团队测试，然后尝试修复足够的 bug 以发布版本。然后我们发现，当开发者在工作的同时编写和运行测试时，我们可以立即发现和修复问题，这使得整个过程更快、更可靠。</p>
<p>对人类有效的方法对 Agent 也有效。当 Agent 能够自己评估所生成代码的质量，而不是依赖我们为其检查时，它们会产生更好的代码。我们需要指导它们我们在寻找什么，并为它们提供实现这一目标的最佳方法的指导。</p>
<h2><span id="人类在循环之上">人类在循环之上</span></h2><p>我们可以让 Agent 更擅长生成产出，而不是亲自检查它们的产出。控制如何循环内不同层次循环的规范、质量检查和工作流指导的集合是 Agent 的 harness（操控框架）。构建和维护这些 harness 的新兴实践，<a href="https://martinfowler.com/articles/exploring-gen-ai/harness-engineering.html">Harness Engineering（操控框架工程）</a>，就是人类在循环之上工作的方式。</p>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/chenzhenyang.github.io/master/images/humans-agents-image5.png" alt="Humans on the loop: An upper &quot;why&quot; loop connected to a lower &quot;how&quot; loop by a human. The why loop iterates over an idea and working software.A robot sits at the bottom of the lower &quot;how&quot; loop, which iterates over interim artefacts like specs and code."></p>
<p><em>图 5：人类定义如何循环，Agent 运行它</em></p>
<p>类似于在循环之上的概念也被描述为”中间循环”，包括 <a href="https://martinfowler.com/bliki/FutureOfSoftwareDevelopment.html">软件开发未来研讨会</a> 的参与者。中间循环指的是将人类的注意力转移到比编码循环更高层次的循环上。</p>
<p>在循环之中和在循环之上的区别最明显地体现在当我们对 Agent 的产出（包括中间产物）不满意时所做的事情。”在循环之中”的方式是修复产物，无论是直接编辑它，还是告诉 Agent 进行我们想要的修正。”在循环之上”的方式是改变产生该产物的 harness，以便它产生我们想要的结果。</p>
<p>我们通过持续改进 harness 来持续改进我们获得的成果质量。然后我们可以将其提升到另一个层次。</p>
<h2><span id="agent-飞轮">Agent 飞轮</span></h2><p>下一个层次是人类指导 Agent 来管理和改进 harness，而不是手动完成。</p>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/chenzhenyang.github.io/master/images/humans-agents-image6.png" alt="Flywheel: An upper &quot;why&quot; loop connected to a lower &quot;how&quot; loop by a human and a robot. The why loop iterates over an idea and working software. The how loop iterates over interim artefacts like specs."></p>
<p><em>图 6：人类指导 Agent 构建和改进如何循环</em></p>
<p>我们通过为 Agent 提供评估循环性能所需的信息来构建飞轮。一个好的起点是 harness 中已经包含的测试和评估。随着我们为其提供更丰富的信号，飞轮变得更强大。添加测量性能和验证失败场景的流水线阶段。引入来自生产环境的运营数据、用户旅程日志和商业结果，以扩大 Agent 可以分析的范围和深度。</p>
<p>对于工作流的每个步骤，我们让 Agent 审查结果并推荐对 harness 的改进。范围包括可能改进这些结果的工作流任何上游部分的改进。我们现在拥有的是一个能够生成自我改进建议的 Agent harness。</p>
<p>我们首先以交互方式考虑这些建议，提示 Agent 实施具体的更改。我们也可以让 Agent 将它们的建议添加到产品待办事项列表（backlog）中，这样我们就可以确定优先级并安排它们，让 Agent 在自动化流程中接收、应用和测试这些建议。</p>
<p>随着我们获得信心，Agent 可以为它们的建议打分，包括风险、成本和收益。然后我们可能会决定具有某些分数的建议应该自动批准和应用。</p>
<p>在某个时刻，这看起来可能很像人类远离循环的老派氛围编程。我怀疑对于经常完成的标准类型工作，当改进循环达到收益递减时，情况将是这样。但通过工程化 harness，我们不仅会获得一次性的”足够好”的解决方案，还会获得稳健的、甚至可能是反脆弱的系统，它们能够持续自我改进。</p>
<hr>
<hr>
<p><strong>原文链接</strong>: <a href="https://martinfowler.com/articles/exploring-gen-ai/humans-and-agents.html">https://martinfowler.com/articles/exploring-gen-ai/humans-and-agents.html</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenSpec README</title>
    <url>/posts/658987613.html</url>
    <content><![CDATA[<summary><strong>最受喜爱的规范 (spec) 框架。</strong></summary>

<p><a href="https://github.com/Fission-AI/OpenSpec/stargazers"><img src="/images/Stars" alt="https:&#x2F;&#x2F;img.shields.io&#x2F;github&#x2F;stars&#x2F;Fission-AI&#x2F;OpenSpec?style&#x3D;flat-square&amp;label&#x3D;Stars"></a><br><a href="https://www.npmjs.com/package/@fission-ai/openspec"><img src="/images/Downloads" alt="https:&#x2F;&#x2F;img.shields.io&#x2F;npm&#x2F;dm&#x2F;@fission-ai&#x2F;openspec?style&#x3D;flat-square&amp;label&#x3D;Downloads&#x2F;mo"></a><br><a href="https://github.com/Fission-AI/OpenSpec/graphs/contributors"><img src="/images/Contributors" alt="https:&#x2F;&#x2F;img.shields.io&#x2F;github&#x2F;contributors&#x2F;Fission-AI&#x2F;OpenSpec?style&#x3D;flat-square&amp;label&#x3D;Contributors"></a></p>
<p>我们的理念：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">→ 灵活而非僵化</span><br><span class="line">→ 迭代而非瀑布</span><br><span class="line">→ 简单而非复杂</span><br><span class="line">→ 为现有项目 (brownfield) 而建，不仅限于新项目 (greenfield)</span><br><span class="line">→ 可从个人项目扩展至企业级</span><br></pre></td></tr></table></figure>

<blockquote>
<p>[!TIP]<br>**全新工作流 (workflow) 现已可用！**我们已用全新的 artifact 引导工作流重构了 OpenSpec。</p>
<p>运行 <code>/opsx:propose &quot;你的想法&quot;</code> 开始使用。→ <a href="docs/opsx.md">了解更多</a></p>
</blockquote>
<h3><span id="团队">团队</span></h3><p>在团队中使用 OpenSpec？<a href="mailto:&#116;&#101;&#x61;&#x6d;&#115;&#64;&#x6f;&#x70;&#x65;&#x6e;&#115;&#112;&#101;&#x63;&#46;&#x64;&#101;&#118;">发送邮件至这里</a> 以访问我们的 Slack 频道。</p>
<!-- TODO: Add GIF demo of /opsx:propose → /opsx:archive workflow -->

<h2><span id="实战演示">实战演示</span></h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">你：/opsx:propose add-dark-mode</span><br><span class="line">AI:  创建了 openspec/changes/add-dark-mode/</span><br><span class="line">     ✓ proposal.md — 为什么要做这个，哪些会改变</span><br><span class="line">     ✓ specs/       — 需求和场景</span><br><span class="line">     ✓ design.md    — 技术方案</span><br><span class="line">     ✓ tasks.md     — 实现清单</span><br><span class="line">     已准备好实现！</span><br><span class="line"></span><br><span class="line">你：/opsx:apply</span><br><span class="line">AI:  正在实现任务...</span><br><span class="line">     ✓ 1.1 添加主题上下文提供者</span><br><span class="line">     ✓ 1.2 创建切换组件</span><br><span class="line">     ✓ 2.1 添加 CSS 变量</span><br><span class="line">     ✓ 2.2 连接 localStorage</span><br><span class="line">     所有任务完成！</span><br><span class="line"></span><br><span class="line">你：/opsx:archive</span><br><span class="line">AI:  已归档至 openspec/changes/archive/2025-01-23-add-dark-mode/</span><br><span class="line">     规范已更新。准备好下一个功能。</span><br></pre></td></tr></table></figure>
<summary><strong>OpenSpec Dashboard</strong></summary>

<h2><span id="快速开始">快速开始</span></h2><p><strong>需要 Node.js 20.19.0 或更高版本。</strong></p>
<p>全局安装 OpenSpec：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g @fission-ai/openspec@latest</span><br></pre></td></tr></table></figure>

<p>然后导航到你的项目目录并初始化：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> your-project</span><br><span class="line">openspec init</span><br></pre></td></tr></table></figure>

<p>现在告诉你的 AI：<code>/opsx:propose &lt;你想要构建的东西&gt;</code></p>
<p>如果你想要扩展工作流 (<code>/opsx:new</code>、<code>/opsx:continue</code>、<code>/opsx:ff</code>、<code>/opsx:verify</code>、<code>/opsx:sync</code>、<code>/opsx:bulk-archive</code>、<code>/opsx:onboard</code>)，使用 <code>openspec config profile</code> 选择并用 <code>openspec update</code> 应用。</p>
<blockquote>
<p>[!NOTE]<br>不确定你的工具是否受支持？<a href="docs/supported-tools.md">查看完整列表</a> – 我们支持 20+ 工具并持续增长。</p>
<p>也适用于 pnpm、yarn、bun 和 nix。<a href="docs/installation.md">查看安装选项</a>。</p>
</blockquote>
<h2><span id="文档">文档</span></h2><p>→ <strong><a href="docs/getting-started.md">入门指南</a></strong>：第一步<br><br>→ <strong><a href="docs/workflows.md">工作流</a></strong>：组合和模式<br><br>→ <strong><a href="docs/commands.md">命令</a></strong>：斜杠命令和技能<br><br>→ <strong><a href="docs/cli.md">CLI</a></strong>：终端参考<br><br>→ <strong><a href="docs/supported-tools.md">支持的工具</a></strong>：工具集成和安装路径<br><br>→ <strong><a href="docs/concepts.md">概念</a></strong>：整体架构<br><br>→ <strong><a href="docs/multi-language.md">多语言</a></strong>：多语言支持<br><br>→ <strong><a href="docs/customization.md">自定义</a></strong>：打造属于你的 OpenSpec</p>
<h2><span id="为什么选择-openspec">为什么选择 OpenSpec？</span></h2><p>AI 编程助手功能强大，但当需求仅存在于聊天记录中时往往不可预测。OpenSpec 添加了轻量级的规范层，让你在编写任何代码之前就构建内容达成一致。</p>
<ul>
<li><strong>先达成一致再构建</strong> — 人类和 AI 在编写代码前就规范达成一致</li>
<li><strong>保持有序</strong> — 每个变更都有自己的文件夹，包含提案、规范、设计和任务</li>
<li><strong>灵活工作</strong> — 随时更新任何 artifact，没有僵化的阶段限制</li>
<li><strong>使用你的工具</strong> — 通过斜杠命令与 20+ AI 助手协作</li>
</ul>
<h3><span id="我们与其他方案的对比">我们与其他方案的对比</span></h3><p><strong>对比 <a href="https://github.com/github/spec-kit">Spec Kit</a></strong> (GitHub) — 详尽但笨重。僵化的阶段限制、大量 Markdown、Python 设置。OpenSpec 更轻量且允许自由迭代。</p>
<p><strong>对比 <a href="https://kiro.dev/">Kiro</a></strong> (AWS) — 功能强大但你被锁定在他们的 IDE 中且仅限于 Claude 模型。OpenSpec 与你已经使用的工具协作。</p>
<p><strong>对比无规范</strong> — 没有规范的 AI 编程意味着模糊的提示和不可预测的结果。OpenSpec 带来可预测性而无需繁文缛节。</p>
<h2><span id="更新-openspec">更新 OpenSpec</span></h2><p><strong>升级包</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g @fission-ai/openspec@latest</span><br></pre></td></tr></table></figure>

<p><strong>刷新 Agent 指令</strong></p>
<p>在每个项目内运行此命令以重新生成 AI 指导并确保最新的斜杠命令处于活动状态：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openspec update</span><br></pre></td></tr></table></figure>

<h2><span id="使用说明">使用说明</span></h2><p><strong>模型选择</strong>：OpenSpec 最适合高推理能力的模型。我们推荐 Opus 4.5 和 GPT 5.2 用于规划和实现。</p>
<p><strong>上下文卫生</strong>：OpenSpec 受益于干净的上下文窗口。在开始实现前清理你的上下文，并在整个会话中保持良好的上下文卫生。</p>
<h2><span id="贡献">贡献</span></h2><p><strong>小修复</strong> — Bug 修复、拼写错误纠正和小型改进可以直接作为 PR 提交。</p>
<p><strong>大型变更</strong> — 对于新功能、重大重构或架构变更，请先提交 OpenSpec 变更提案，这样我们可以在实现开始前就意图和目标达成一致。</p>
<p>编写提案时，请牢记 OpenSpec 理念：我们服务于广泛的用户群体，涵盖不同的编程 Agent、模型和使用场景。变更应该对所有人都有用。</p>
<p><strong>欢迎 AI 生成的代码</strong> — 只要经过测试和验证。包含 AI 生成代码的 PR 应提及所使用的编程 Agent 和模型（例如：”使用 claude-opus-4-5-20251101 通过 Claude Code 生成”）。</p>
<h3><span id="开发">开发</span></h3><ul>
<li>安装依赖：<code>pnpm install</code></li>
<li>构建：<code>pnpm run build</code></li>
<li>测试：<code>pnpm test</code></li>
<li>本地开发 CLI：<code>pnpm run dev</code> 或 <code>pnpm run dev:cli</code></li>
<li>约定式提交 (单行)：<code>type(scope): subject</code></li>
</ul>
<h2><span id="其他">其他</span></h2><summary><strong>遥测</strong></summary>

<p>OpenSpec 收集匿名使用统计。</p>
<p>我们仅收集命令名称和版本以了解使用模式。不收集参数、路径、内容或个人身份信息 (PII)。在 CI 中自动禁用。</p>
<p><strong>退出：</strong> <code>export OPENSPEC_TELEMETRY=0</code> 或 <code>export DO_NOT_TRACK=1</code></p>
<summary><strong>维护者与顾问</strong></summary>
查看 [MAINTAINERS.md](MAINTAINERS.md) 了解核心维护者和顾问名单，他们帮助指导项目发展。

<h2><span id="许可证">许可证</span></h2><p>MIT</p>
<summary><strong>最受喜爱的规范 (spec) 框架。</strong></summary>

<p><a href="https://github.com/Fission-AI/OpenSpec/stargazers"><img src="/images/Stars" alt="https:&#x2F;&#x2F;img.shields.io&#x2F;github&#x2F;stars&#x2F;Fission-AI&#x2F;OpenSpec?style&#x3D;flat-square&amp;label&#x3D;Stars"></a><br><a href="https://www.npmjs.com/package/@fission-ai/openspec"><img src="/images/Downloads" alt="https:&#x2F;&#x2F;img.shields.io&#x2F;npm&#x2F;dm&#x2F;@fission-ai&#x2F;openspec?style&#x3D;flat-square&amp;label&#x3D;Downloads&#x2F;mo"></a><br><a href="https://github.com/Fission-AI/OpenSpec/graphs/contributors"><img src="/images/Contributors" alt="https:&#x2F;&#x2F;img.shields.io&#x2F;github&#x2F;contributors&#x2F;Fission-AI&#x2F;OpenSpec?style&#x3D;flat-square&amp;label&#x3D;Contributors"></a></p>
<p>我们的理念：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">→ 灵活而非僵化</span><br><span class="line">→ 迭代而非瀑布</span><br><span class="line">→ 简单而非复杂</span><br><span class="line">→ 为现有项目 (brownfield) 而建，不仅限于新项目 (greenfield)</span><br><span class="line">→ 可从个人项目扩展至企业级</span><br></pre></td></tr></table></figure>

<blockquote>
<p>[!TIP]<br>**全新工作流 (workflow) 现已可用！**我们已用全新的 artifact 引导工作流重构了 OpenSpec。</p>
<p>运行 <code>/opsx:propose &quot;你的想法&quot;</code> 开始使用。→ <a href="docs/opsx.md">了解更多</a></p>
</blockquote>
<p align="center">
  关注 <a href="https://x.com/0xTab">@0xTab on X</a> 获取更新 · 加入 <a href="https://discord.gg/YctCnvvshC">OpenSpec Discord</a> 获取帮助和提问。
</p>

<h3><span id="团队">团队</span></h3><p>在团队中使用 OpenSpec？<a href="mailto:&#x74;&#101;&#97;&#x6d;&#x73;&#x40;&#111;&#112;&#x65;&#110;&#115;&#x70;&#x65;&#x63;&#x2e;&#100;&#101;&#118;">发送邮件至这里</a> 以访问我们的 Slack 频道。</p>
<!-- TODO: Add GIF demo of /opsx:propose → /opsx:archive workflow -->

<h2><span id="实战演示">实战演示</span></h2><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">你：/opsx:propose add-dark-mode</span><br><span class="line">AI:  创建了 openspec/changes/add-dark-mode/</span><br><span class="line">     ✓ proposal.md — 为什么要做这个，哪些会改变</span><br><span class="line">     ✓ specs/       — 需求和场景</span><br><span class="line">     ✓ design.md    — 技术方案</span><br><span class="line">     ✓ tasks.md     — 实现清单</span><br><span class="line">     已准备好实现！</span><br><span class="line"></span><br><span class="line">你：/opsx:apply</span><br><span class="line">AI:  正在实现任务...</span><br><span class="line">     ✓ 1.1 添加主题上下文提供者</span><br><span class="line">     ✓ 1.2 创建切换组件</span><br><span class="line">     ✓ 2.1 添加 CSS 变量</span><br><span class="line">     ✓ 2.2 连接 localStorage</span><br><span class="line">     所有任务完成！</span><br><span class="line"></span><br><span class="line">你：/opsx:archive</span><br><span class="line">AI:  已归档至 openspec/changes/archive/2025-01-23-add-dark-mode/</span><br><span class="line">     规范已更新。准备好下一个功能。</span><br></pre></td></tr></table></figure>

<details>
<summary><strong>OpenSpec Dashboard</strong></summary>
</details>

<h2><span id="快速开始">快速开始</span></h2><p><strong>需要 Node.js 20.19.0 或更高版本。</strong></p>
<p>全局安装 OpenSpec：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g @fission-ai/openspec@latest</span><br></pre></td></tr></table></figure>

<p>然后导航到你的项目目录并初始化：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> your-project</span><br><span class="line">openspec init</span><br></pre></td></tr></table></figure>

<p>现在告诉你的 AI：<code>/opsx:propose &lt;你想要构建的东西&gt;</code></p>
<p>如果你想要扩展工作流 (<code>/opsx:new</code>、<code>/opsx:continue</code>、<code>/opsx:ff</code>、<code>/opsx:verify</code>、<code>/opsx:sync</code>、<code>/opsx:bulk-archive</code>、<code>/opsx:onboard</code>)，使用 <code>openspec config profile</code> 选择并用 <code>openspec update</code> 应用。</p>
<blockquote>
<p>[!NOTE]<br>不确定你的工具是否受支持？<a href="docs/supported-tools.md">查看完整列表</a> – 我们支持 20+ 工具并持续增长。</p>
<p>也适用于 pnpm、yarn、bun 和 nix。<a href="docs/installation.md">查看安装选项</a>。</p>
</blockquote>
<h2><span id="文档">文档</span></h2><p>→ <strong><a href="docs/getting-started.md">入门指南</a></strong>：第一步<br><br>→ <strong><a href="docs/workflows.md">工作流</a></strong>：组合和模式<br><br>→ <strong><a href="docs/commands.md">命令</a></strong>：斜杠命令和技能<br><br>→ <strong><a href="docs/cli.md">CLI</a></strong>：终端参考<br><br>→ <strong><a href="docs/supported-tools.md">支持的工具</a></strong>：工具集成和安装路径<br><br>→ <strong><a href="docs/concepts.md">概念</a></strong>：整体架构<br><br>→ <strong><a href="docs/multi-language.md">多语言</a></strong>：多语言支持<br><br>→ <strong><a href="docs/customization.md">自定义</a></strong>：打造属于你的 OpenSpec</p>
<h2><span id="为什么选择-openspec">为什么选择 OpenSpec？</span></h2><p>AI 编程助手功能强大，但当需求仅存在于聊天记录中时往往不可预测。OpenSpec 添加了轻量级的规范层，让你在编写任何代码之前就构建内容达成一致。</p>
<ul>
<li><strong>先达成一致再构建</strong> — 人类和 AI 在编写代码前就规范达成一致</li>
<li><strong>保持有序</strong> — 每个变更都有自己的文件夹，包含提案、规范、设计和任务</li>
<li><strong>灵活工作</strong> — 随时更新任何 artifact，没有僵化的阶段限制</li>
<li><strong>使用你的工具</strong> — 通过斜杠命令与 20+ AI 助手协作</li>
</ul>
<h3><span id="我们与其他方案的对比">我们与其他方案的对比</span></h3><p><strong>对比 <a href="https://github.com/github/spec-kit">Spec Kit</a></strong> (GitHub) — 详尽但笨重。僵化的阶段限制、大量 Markdown、Python 设置。OpenSpec 更轻量且允许自由迭代。</p>
<p><strong>对比 <a href="https://kiro.dev/">Kiro</a></strong> (AWS) — 功能强大但你被锁定在他们的 IDE 中且仅限于 Claude 模型。OpenSpec 与你已经使用的工具协作。</p>
<p><strong>对比无规范</strong> — 没有规范的 AI 编程意味着模糊的提示和不可预测的结果。OpenSpec 带来可预测性而无需繁文缛节。</p>
<h2><span id="更新-openspec">更新 OpenSpec</span></h2><p><strong>升级包</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g @fission-ai/openspec@latest</span><br></pre></td></tr></table></figure>

<p><strong>刷新 Agent 指令</strong></p>
<p>在每个项目内运行此命令以重新生成 AI 指导并确保最新的斜杠命令处于活动状态：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openspec update</span><br></pre></td></tr></table></figure>

<h2><span id="使用说明">使用说明</span></h2><p><strong>模型选择</strong>：OpenSpec 最适合高推理能力的模型。我们推荐 Opus 4.5 和 GPT 5.2 用于规划和实现。</p>
<p><strong>上下文卫生</strong>：OpenSpec 受益于干净的上下文窗口。在开始实现前清理你的上下文，并在整个会话中保持良好的上下文卫生。</p>
<h2><span id="贡献">贡献</span></h2><p><strong>小修复</strong> — Bug 修复、拼写错误纠正和小型改进可以直接作为 PR 提交。</p>
<p><strong>大型变更</strong> — 对于新功能、重大重构或架构变更，请先提交 OpenSpec 变更提案，这样我们可以在实现开始前就意图和目标达成一致。</p>
<p>编写提案时，请牢记 OpenSpec 理念：我们服务于广泛的用户群体，涵盖不同的编程 Agent、模型和使用场景。变更应该对所有人都有用。</p>
<p><strong>欢迎 AI 生成的代码</strong> — 只要经过测试和验证。包含 AI 生成代码的 PR 应提及所使用的编程 Agent 和模型（例如：”使用 claude-opus-4-5-20251101 通过 Claude Code 生成”）。</p>
<h3><span id="开发">开发</span></h3><ul>
<li>安装依赖：<code>pnpm install</code></li>
<li>构建：<code>pnpm run build</code></li>
<li>测试：<code>pnpm test</code></li>
<li>本地开发 CLI：<code>pnpm run dev</code> 或 <code>pnpm run dev:cli</code></li>
<li>约定式提交 (单行)：<code>type(scope): subject</code></li>
</ul>
<h2><span id="其他">其他</span></h2><summary><strong>遥测</strong></summary>

<p>OpenSpec 收集匿名使用统计。</p>
<p>我们仅收集命令名称和版本以了解使用模式。不收集参数、路径、内容或个人身份信息 (PII)。在 CI 中自动禁用。</p>
<p><strong>退出：</strong> <code>export OPENSPEC_TELEMETRY=0</code> 或 <code>export DO_NOT_TRACK=1</code></p>
<summary><strong>维护者与顾问</strong></summary>

<p>查看 <a href="MAINTAINERS.md">MAINTAINERS.md</a> 了解核心维护者和顾问名单，他们帮助指导项目发展。</p>
<h2><span id="许可证">许可证</span></h2><p>MIT</p>
]]></content>
      <tags>
        <tag>OpenSpec</tag>
        <tag>规范框架</tag>
        <tag>AI 编程</tag>
        <tag>工作流</tag>
      </tags>
  </entry>
  <entry>
    <title>Centos上ShadowSocks搭建Socket5服务器</title>
    <url>/posts/3396596425.html</url>
    <content><![CDATA[<p>买的VPN最近好像跑路了，看了下阿里云的国外ECS，超便宜，美国西部做活动一年才300多RMB；</p>
<p>Shadowsock是一个安全的socks5代理软件，提供了服务端和客户端；可以很方便的搭建一个VPN；</p>
<h1><span id="服务器端搭建">服务器端搭建</span></h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ wget –no-check-certificate https://raw.githubusercontent.com/teddysun/shadowsocks_install/master/shadowsocks.sh</span><br><span class="line"><span class="built_in">chmod</span> +x shadowsocks.sh</span><br><span class="line">$ ./shadowsocks.sh 2&gt;&amp;1 | <span class="built_in">tee</span> shadowsocks.log</span><br></pre></td></tr></table></figure>
<p>安装成功会显示如下的信息，将下面的信息填写到客户端里面就可以了；</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Starting Shadowsocks success</span><br><span class="line"></span><br><span class="line">Congratulations, Shadowsocks-python server install completed!</span><br><span class="line">Your Server IP        :  47.88.56.208 </span><br><span class="line">Your Server Port      :  8989</span><br><span class="line">Your Password         :  xxxx </span><br><span class="line">Your Encryption Method:  rc4-md5</span><br><span class="line"></span><br><span class="line">Welcome to visit:https://teddysun.com/342.html</span><br><span class="line">Enjoy it!</span><br></pre></td></tr></table></figure>

<h1><span id="客户端配置">客户端配置</span></h1><p><img src="/!%5B%5D" alt="image" title="https:&#x2F;&#x2F;54architect.oss-cn-beijing.aliyuncs.com&#x2F;blog.54architect.com&#x2F;2019-03-21 15-06-37 的屏幕截图.png"></p>
<p>这里需要特别注意的是，有些socks5客户端提供了将socks5转为http(s)的功能，这里要选则socks5。</p>
<h1><span id="privoxy配置">Privoxy配置</span></h1><p>Privoxy可以将socks5转为http(s)协议；</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">forward-socks5   /               127.0.0.1:1080 .    </span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt-get install privoxy </span><br><span class="line">//开启privoxy 服务就行</span><br><span class="line"><span class="built_in">sudo</span>  service  privoxy start </span><br><span class="line">// 设置http 和 https 全局代理</span><br><span class="line"><span class="built_in">export</span> http_proxy=<span class="string">&#x27;http://localhost:8118&#x27;</span></span><br><span class="line"><span class="built_in">export</span> https_proxy=<span class="string">&#x27;https://localhost:8118&#x27;</span></span><br><span class="line">// 测试一下</span><br><span class="line">wget https://www.google.com  </span><br></pre></td></tr></table></figure>

<h1><span id="参考">参考</span></h1><p><a href="https://blog.csdn.net/li740207611/article/details/52045471">使用Privoxy将socks5代理转为http代理</a><br><a href="https://blog.csdn.net/yanzi1225627/article/details/51064306">Mac上Privoxy将shadowsocks的socks5代理转为http代理(解决SublimeText无法安装插件的问题)</a></p>
]]></content>
      <categories>
        <category>Nuclear</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Centos</tag>
        <tag>ShadowSocks</tag>
        <tag>Socks5</tag>
        <tag>Privoxy</tag>
      </tags>
  </entry>
  <entry>
    <title>Best Efforts 1PC Pattern Implements Distributed Transactions in Spring, without XA</title>
    <url>/posts/2540280255.html</url>
    <content><![CDATA[<h1><span id="简述">简述</span></h1><p>Distributed transactions in Spring, with and without XA一文中描述了在Spring中实现分布式事务的7种处理模式；3种基于XA协议，4种特定场景的非XA协议的模式；</p>
<h1><span id="最大努力一阶段提交模式">最大努力一阶段提交模式</span></h1><p>4种非XA协议模式的第一种是共享事务资源模式（Shared Transaction Resource pattern），其提供了特定场景的多资源事务同步模式；<br>第二种便是本文要说的最大努力一阶段提交提交模式，在共享事务资源模式中，将两个系统的事务使用同一个事务资源的一个事务来管理，从而达到一起成功一起失败的目的；</p>
<span id="more"></span>
<p>而在最大努力一阶段提交模式中，不特意控制两个资源在同一个事务中，而是寄希望于其中一个资源的事务管理相当简单，简单到唯一肯能发生错误的就是基础组建发生错误，而非业务处理错误。<br>比如说关系型数据库就不是一个这样的资源，他除了基础组建会发生错误以外，业务上还会发生各种各样的错误情况，比如说主键重复，查不到数据时抛出业务异常等等；<br>从消息系统里接收消息，就是一个符合这种情况的资源，从其接收到消息之后，业务处理成功之后，提交消息事务，在这里，接收消息和提交消息事务这两个操作没有业务逻辑，只有像消息服务器，网络这种基础组件出问题时，才有可能出问题。</p>
<h1><span id="举个例子">举个例子</span></h1><p>在这种情况下，如果我们就直接不管消息系统的提交结果，会怎么样？还是消息驱动的单个数据库更新场景，如果不控制消息系统接收消息的事务，只控制复杂的数据库事务，那么处理过程如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">开启消息事务</span><br><span class="line">接受消息</span><br><span class="line">开启数据库事务</span><br><span class="line">更新数据库</span><br><span class="line">提交数据库事务</span><br><span class="line">提交消息事务</span><br></pre></td></tr></table></figure>

<p>如果是开启消息事务或者接收消息，这两个步骤出问题，只可能是基础组件的问题，基础组件恢复之后，重新获取即可，对整个系统没有任何影响<br>如果开启数据库事务、更新数据库或者提交数据库事务出问题，事务回滚，没有提交消息事务，超时之后，消息会被重新接收<br>如果是提交消息事务出问题，这时数据库事务已经提交，但是消息事务提交失败，事务超时时候，消息服务器会将消息重新发送过来；这时，唯一的一个问题来了，同一个消息会被重新处理一遍。解决这个问题的杀手锏就是保证业务服务的幂等，这个做起来非常容易。</p>
<h1><span id="跟kafka有什么关系">跟Kafka有什么关系</span></h1><p>在Kafka Consumer中，接收消息的语义有三种模式，最多一次，最少一次，正好一次，没错，这就是最少一次的情况。</p>
<h1><span id="参考">参考</span></h1><p><a href="https://www.javaworld.com/article/2077963/distributed-transactions-in-spring--with-and-without-xa.html">Distributed transactions in Spring, with and without XA</a><br><a href="http://www.importnew.com/15812.html">如何实现XA式、非XA式Spring分布式事务</a></p>
]]></content>
      <categories>
        <category>Spring</category>
        <category>Spring Transaction</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>Transaction</tag>
        <tag>Distributed Transaction</tag>
      </tags>
  </entry>
  <entry>
    <title>Confluent Platform Quick Start (Docker)</title>
    <url>/posts/1992991654.html</url>
    <content><![CDATA[<h1><span id="问题">问题</span></h1><p>Confluent提供了Kafka的整套生态的docker镜像：<a href="https://github.com/confluentinc/cp-docker-images">confluentinc&#x2F;cp-docker-images</a>，并在<a href="https://docs.confluent.io/current/quickstart/ce-docker-quickstart.html">官方文档 Confluent Platform Quick Start (Docker) </a>中详细的描述了这些镜像的使用方法。</p>
<span id="more"></span>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">clone</span> https://github.com/confluentinc/cp-docker-images</span><br><span class="line">$ <span class="built_in">cd</span> cp-docker-images</span><br><span class="line">$ git checkout 5.1.2-post</span><br><span class="line">$ <span class="built_in">cd</span> examples/cp-all-in-one/</span><br><span class="line">$ docker-compose up -d --build</span><br></pre></td></tr></table></figure>

<p>如果按照上面的步骤，会发现在执行docker-compose up -d –build命令时会出现访问d1i4a15mxbxib1.cloudfront.net，访问不到的情况，是构建confluentinc&#x2F;ksql-examples:5.1.2镜像时发生的，注意是在构建镜像的阶段发生的，不是在启动镜像的阶段发生的。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Building connect</span><br><span class="line">Step 1/3 : FROM confluentinc/cp-kafka-connect:5.1.2</span><br><span class="line">---&gt; a556d728ef1e</span><br><span class="line">Step 2/3 : ENV CONNECT_PLUGIN_PATH=<span class="string">&quot;/usr/share/java,/usr/share/confluent-hub-components&quot;</span></span><br><span class="line">---&gt; Using cache</span><br><span class="line">---&gt; 3fc1228c32fb</span><br><span class="line">Step 3/3 : RUN confluent-hub install --no-prompt confluentinc/kafka-connect-datagen:latest</span><br><span class="line">---&gt; Running <span class="keyword">in</span> 305285df291e</span><br><span class="line">Running <span class="keyword">in</span> a <span class="string">&quot;--no-prompt&quot;</span> mode </span><br><span class="line">Implicit acceptance of the license below:  </span><br><span class="line">Apache License 2.0 </span><br><span class="line">https://www.apache.org/licenses/LICENSE-2.0 </span><br><span class="line">Downloading component Kafka Connect Datagen 0.1.1, provided by Confluent, Inc. from Confluent Hub and installing into /usr/share/confluent-hub-components </span><br><span class="line">java.net.UnknownHostException: d1i4a15mxbxib1.cloudfront.net </span><br></pre></td></tr></table></figure>

<p>通过查看github仓库的issue，<a href="https://github.com/confluentinc/cp-docker-images/issues/654">Unable to install kafka-connect-datagen:0.1.0 #654</a>，发现这个域名是<a href="https://api.hub.confluent.io/api/plugins">https://api.hub.confluent.io/api/plugins</a>接口返回的配置信息包含的。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ curl https://api.hub.confluent.io/api/plugins</span><br></pre></td></tr></table></figure>

<p>上面接口返回的信息其中一个如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;logo&quot;</span>:<span class="string">&quot;https://d1i4a15mxbxib1.cloudfront.net/api/plugins/debezium/debezium-connector-mysql/versions/0.9.2/assets/color_debezium_256px.png&quot;</span></span><br></pre></td></tr></table></figure>

<h1><span id="处理过程">处理过程</span></h1><p>第一步通过站长工具，找到d1i4a15mxbxib1.cloudfront.net的IP地址</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">d1i4a15mxbxib1.cloudfront.net 52.84.225.219 877978075 新加坡 Amazon数据中心</span><br><span class="line">d1i4a15mxbxib1.cloudfront.net 52.84.225.183 877978039 新加坡 Amazon数据中心</span><br><span class="line">d1i4a15mxbxib1.cloudfront.net 52.84.225.134 877977990 新加坡 Amazon数据中心</span><br><span class="line">d1i4a15mxbxib1.cloudfront.net 52.84.225.114 877977970 新加坡 Amazon数据中心 </span><br></pre></td></tr></table></figure>

<p>第二步在cp-docker-images&#x2F;examples&#x2F;cp-all-in-one&#x2F;Dockerfile，找到confluentinc&#x2F;ksql-examples镜像的Dockerfile文件</p>
<figure class="highlight bash"><figcaption><span>Dockerfile</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Copyright 2018 Confluent Inc.</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Licensed under the Apache License, Version 2.0 (the &quot;License&quot;);</span></span><br><span class="line"><span class="comment"># you may not use this file except in compliance with the License.</span></span><br><span class="line"><span class="comment"># You may obtain a copy of the License at</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># http://www.apache.org/licenses/LICENSE-2.0</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Unless required by applicable law or agreed to in writing, software</span></span><br><span class="line"><span class="comment"># distributed under the License is distributed on an &quot;AS IS&quot; BASIS,</span></span><br><span class="line"><span class="comment"># WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.</span></span><br><span class="line"><span class="comment"># See the License for the specific language governing permissions and</span></span><br><span class="line"><span class="comment"># limitations under the License.</span></span><br><span class="line"></span><br><span class="line">FROM confluentinc/cp-kafka-connect:5.1.2</span><br><span class="line"></span><br><span class="line">ENV CONNECT_PLUGIN_PATH=<span class="string">&quot;/usr/share/java,/usr/share/confluent-hub-components&quot;</span></span><br><span class="line"></span><br><span class="line">RUN confluent-hub install --no-prompt confluentinc/kafka-connect-datagen:latest</span><br></pre></td></tr></table></figure>
<p>第三步，单独构建镜像，指定host配置，使用其中一个即可</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">sudo</span> docker build -t confluentinc/ksql-examples:5.1.2 \</span><br><span class="line">&gt;--add-host d1i4a15mxbxib1.cloudfront.net:52.84.225.183 \</span><br><span class="line">&gt;-f Dockerfile .</span><br></pre></td></tr></table></figure>

<p>第四步，docker-compose启动</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ docker-compose up -d --build</span><br></pre></td></tr></table></figure>

<h1><span id="参考">参考</span></h1><p><a href="https://blog.csdn.net/zcw1994/article/details/84568426">docker容器添加自定义hosts</a><br><a href="https://docs.docker.com/network/proxy/#configure-the-docker-client">Configure Docker to use a proxy server</a><br><a href="https://github.com/confluentinc/cp-docker-images/issues/654">Unable to install kafka-connect-datagen:0.1.0 #654</a><br><a href="https://docs.confluent.io/current/quickstart/ce-docker-quickstart.html">Confluent Platform Quick Start (Docker)</a></p>
]]></content>
      <categories>
        <category>Apache Kafka</category>
      </categories>
      <tags>
        <tag>Apache Kafka</tag>
      </tags>
  </entry>
  <entry>
    <title>JAAS Authentication Example</title>
    <url>/posts/1376089128.html</url>
    <content><![CDATA[<p>例子程序来源于Java文档。</p>
<ol>
<li>配置Kerberos的Server端，配置KDC（kdc.conf）和Server（krb5.conf）。</li>
<li>增加一个Principal，一个用于程序测试。</li>
<li>将JassAcn.java和Jaas.conf文件拷贝到一个文件夹。</li>
<li>Javac编译JassAcn.java文件</li>
<li>用下面的命令执行class文件，替换成自己的配置</li>
</ol>
<span id="more"></span>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">java -Djava.security.krb5.realm=HIGHGO.COM-Djava.security.krb5.kdc=hadooph.highgo.com-Djava.security.auth.login.config=jaas.conf JaasAcn</span><br></pre></td></tr></table></figure>
<ol start="6">
<li>运行此class文件，提示输入Kerberosusername和Kerberos password，验证成功，打印Authentication succeeded!，验证失败，打印Authenticationfailed和失败原因。</li>
<li>也可将程序导入到Eclipse里运行，只要在运行时，在虚拟机参数里加入</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">-Djava.security.krb5.realm=HIGHGO.COM</span><br><span class="line">-Djava.security.krb5.kdc=hadooph.highgo.com</span><br><span class="line">-Djava.security.auth.login.config=jaas.conf</span><br></pre></td></tr></table></figure>
<p>即可。调试什么的更方便。</p>
<p>Appendix</p>
<figure class="highlight java"><figcaption><span>JaasAcn.java</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> javax.security.auth.login.LoginContext;</span><br><span class="line"><span class="keyword">import</span> javax.security.auth.login.LoginException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.sun.security.auth.callback.TextCallbackHandler;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * This JaasAcn application attempts to authenticate a user and reports whether</span></span><br><span class="line"><span class="comment"> * ornot the authentication was successful.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">JaasAcn</span> &#123;</span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">		<span class="comment">// Obtain a LoginContext,needed for authentication. Tell it</span></span><br><span class="line">		<span class="comment">// to use the LoginModuleimplementation specified by the</span></span><br><span class="line">		<span class="comment">// entry named&quot;JaasSample&quot; in the JAAS login configuration</span></span><br><span class="line">		<span class="comment">// file and to also use thespecified CallbackHandler.</span></span><br><span class="line">		<span class="type">LoginContext</span> <span class="variable">lc</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			lc = newLoginContext(<span class="string">&quot;JaasSample&quot;</span>, <span class="keyword">new</span> <span class="title class_">TextCallbackHandler</span>());</span><br><span class="line">		&#125; <span class="keyword">catch</span> (LoginException le) &#123;</span><br><span class="line">			System.err.println(<span class="string">&quot;Cannot create LoginContext. &quot;</span> + le.getMessage());</span><br><span class="line">			System.exit(-<span class="number">1</span>);</span><br><span class="line">		&#125; <span class="keyword">catch</span> (SecurityException se) &#123;</span><br><span class="line">			System.err.println(<span class="string">&quot;Cannot create LoginContext. &quot;</span> + se.getMessage());</span><br><span class="line">			System.exit(-<span class="number">1</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			<span class="comment">// attemptauthentication</span></span><br><span class="line">			lc.login();</span><br><span class="line">		&#125; <span class="keyword">catch</span> (LoginException le) &#123;</span><br><span class="line">			System.err.println(<span class="string">&quot;Authentication failed:&quot;</span>);</span><br><span class="line">			System.err.println(<span class="string">&quot;  &quot;</span> + le.getMessage());</span><br><span class="line">			System.exit(-<span class="number">1</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		System.out.println(<span class="string">&quot;Authentication succeeded!&quot;</span>);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><figcaption><span>Jaas.conf</span></figcaption><table><tr><td class="code"><pre><span class="line">JaasSample &#123;</span><br><span class="line"> com.sun.security.auth.<span class="keyword">module</span>.Krb5LoginModule required;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java ABC</tag>
      </tags>
  </entry>
  <entry>
    <title>Jaas Guide</title>
    <url>/posts/706353777.html</url>
    <content><![CDATA[<p>本指南扩展了在JAAS Authentication文档中的程序和policy文件，展示了JAAS Authorization组件，这个组件确保被认证的调用者对后续的安全敏感的操作有访问控制的权利（权限）。因为授权组件首先需要用户认证操作完成，请先阅读JAAS Authentication的doc。</p>
<span id="more"></span>
<p>本教程剩下的部分包含目录列出的内容：</p>
<p>如果你想先看下教程代码的运行，你可以直接跳到运行代码的部分，然后再回来看其他的部分了解更多。</p>
<h1><span id="什么是jaas-authorization">什么是JAAS Authorization？</span></h1><p>JAAS授权扩展了已经存在的JAVA安全机构，已存在的安全架构用一个策略文件来指定执行的代码被授予什么访问权限。在Java 2平台中介绍的这个架构是以代码为中心的。也就是说，权限基于代码的特性被授予：代码从哪里来，是否有数字签名，如果有，是谁签的。我们在JAAS Authentication教程的jassacn.policy文件中看到的例子。这个文件包含下面的内容：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">grant codebase<span class="string">&quot;file:./JaasAcn.jar&quot;</span> &#123;</span><br><span class="line">  permission javax.security.auth.AuthPermission</span><br><span class="line">                   <span class="string">&quot;createLoginContext.JaasSample&quot;</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>授权JaasAcn.jar中的代码指定的权限（没有指定签名者，所有代码签不签名不重要）。</p>
<p>JAAS授权增加新的用户中心的访问权限到已存在的代码中心的访问权限。权限的授予标准不仅基于什么代码在运行而且考虑到谁在运行它。</p>
<p>当一个应用用JAAS认证来认证一个用户（或者其他的实体，比如一个服务），一个Subject被创建作为结果。Subject的作用是代替被认证的用户。一个Subject由很多Principal组成，每一个Principal代表那个用户的一个身份。例如。一个Subject可以有一个名字身份（“Susan Smith“）和一个社会安全号码（”987-65-4321“），因此，可以区分于其他的Subject。</p>
<p>权限可以被策略文件来授予给特定的Principal。在用户被认证后，应用可以将一个Subject和当前的访问控制环境联系。对于后续的安全检查操作，（例如，一个本地的文件访问），java运行时环境将自动判定策略是否授权给一个特定的Principal需要的权限，如果授权了，操作将被允许，只有在与Subject连接的访问控制上下文中包含指定的Principal。</p>
<h1><span id="jaas授权如何工作">JAAS授权如何工作？</span></h1><p>一个授权操作，需要下面的步骤：</p>
<ol>
<li>用户必须被认证，就像在JAAS Authentication tutorial里描述的</li>
<li>安全策略必须配置基于Principal的条目</li>
<li>认证的结果，Subject必须与当前的访问控制环境连接。</li>
</ol>
<h1><span id="基于principal的策略文件怎么写">基于Principal的策略文件怎么写？</span></h1><p>策略文件的grant语句可以选择性的包含一个或者多个Principal字段。Principal字段声明用户或者被特定Principal代表的其他实体，执行特定的代码，有指定的权限。</p>
<p>因此，grant语句基本的格式是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">grant &lt;signer(s) field&gt;, &lt;codeBase URL&gt; &lt;Principal <span class="title function_">field</span><span class="params">(s)</span>&gt; &#123; </span><br><span class="line">  permission perm_class_name <span class="string">&quot;target_name&quot;</span>, <span class="string">&quot;action&quot;</span>; </span><br><span class="line">  .... </span><br><span class="line">  permission perm_class_name <span class="string">&quot;target_name&quot;</span>, <span class="string">&quot;action&quot;</span>; </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>每一个签名。CodeBase和Principal字段所在的位置是可选的，字段间的顺序也不重要：<br>一个Principal字段可以像下面这样：<br>也就是说，单词Principal（位置不重要），紧跟着一个Principal类和一个Principal的名字。<br>一个Principal类是一个实现了java.security.Principal接口的类。所有的Principal对象有一个相关联的名字，这个名字可以调用它的getName方法得到。名字所用的格式依赖于每一个Principal的实现。</p>
<p>本教程中使用的Kerberos的认证机制创建的Subject中的Principal的类型是javax.security.auth.kerberos.KerberosPrincipal，也就是应该被用作grant语句的Principal_class的部分。KerberosPrincipals 的用户名是” name@realm “格式。所以，如果用户名是mjones，Realm是KRBNT-OPS.ABC.COM，那么grant语句中使用的principal_name就是<a href="mailto:&#x6d;&#106;&#x6f;&#x6e;&#101;&#115;&#x40;&#x4b;&#x52;&#66;&#78;&#84;&#x2d;&#x4f;&#x50;&#x53;&#46;&#x41;&#66;&#67;&#x2e;&#x43;&#79;&#x4d;">mjones@KRBNT-OPS.ABC.COM</a>。</p>
<p>在一个grant语句中可能包含多个Principal字段。如果多个Principal字段被指定，grant语句中的权限仅在Subject包含所有这些Principal时被授予。<br>为了给不同的Principal授予同样的权限，创建多个只包含一个Principal字段的grant语句。</p>
<p>本教程的策略文件包含一个只有一个Principal字段的grant语句：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">grant codebase <span class="string">&quot;file:./SampleAction.jar&quot;</span>,</span><br><span class="line">    Principal javax.security.auth.kerberos.KerberosPrincipal </span><br><span class="line">        <span class="string">&quot;your_user_name@your_realm&quot;</span>  &#123;</span><br><span class="line"> </span><br><span class="line">   permission java.util.PropertyPermission <span class="string">&quot;java.home&quot;</span>, <span class="string">&quot;read&quot;</span>;</span><br><span class="line">   permission java.util.PropertyPermission <span class="string">&quot;user.home&quot;</span>, <span class="string">&quot;read&quot;</span>;</span><br><span class="line">   permission java.io.FilePermission <span class="string">&quot;foo.txt&quot;</span>, <span class="string">&quot;read&quot;</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>将Kerberos的用户名和Realm替换成你自己的。<br>上边的配置指出，显式授予指定的Principal执行SampleAction.jar中的代码的权限。</p>
<h1><span id="如何将一个subject和访问控制上下文关联">如何将一个Subject和访问控制上下文关联?</span></h1><p>为了创建和用访问控制上下文关联一个Subject，你需要下面的过程：</p>
<ol>
<li>用户必须先被认证，就是JAAS Authentication joc中描述的。</li>
<li>Subject类的静态的doAs方法必须被调用，传给其一个认证了的Subject和一个java.security.PrivilegedAction或者java.security.PrivilegedExceptionAction。（(See API for PrivilegedBlocks fora comparison of PrivilegedAction and PrivilegedExceptionAction.)）。doAs方法用当前的访问控制上下文关联给定的Subject，然后调用Action的run方法。Run方法的实现包含作为指定的Subject，所有被执行的代码。这样Action以特定的Subject执行。</li>
</ol>
<p>Subject类的静态的doAsPrivileged 方法可以代替doAs方法被调用。除了传给doAs方法的参数外，doAsPrivileged 需要第三个参数：一个AccessControlContext对象。不想doAs方法，用当前的访问控制上下文联系Subject，方法doAsPrivileged 用给定的访问控制上下文联系Subject。See doAs vs. doAsPrivileged in theJAAS Reference Guide for a comparison of those methods。</p>
<h1><span id="授权教程代码">授权教程代码</span></h1><p>本教程的代码包含两个文件：</p>
<ol>
<li>JaasAzn.java除了Subject.doAsPrivileged调用中额外的代码，与JAASAuthentication教程中的JaasAcn.java文件一样。</li>
<li>SampleAction.java包含SampleAction类。这个类实现了PrivilegedAction，有一个run方法，这个方法包含了所有我们想基于Principal认证检查执行的代码。</li>
</ol>
<h2><span id="jaasaznjava">JaasAzn.java</span></h2><p>JaasAzn.java与之前的教程中用的到JaasAcn.java完全一样，除了在mian方法的在认证完成后语句之后增加了3条语句。这些语句使一个带表一个通过认证的用户的Subject与当前访问控制上下文关联，然后执行SampleAction的run方法中的一些代码。用访问控制表关联Subject使SampleAction的run方法中安全敏感的操作（任何run方法中直接或间接调用的代码）在代表一个认证的用户的Principal被在当前的策略中被授予必要的权限时被执行。</p>
<p>跟JaasAcn.java一样，JaasAzn.java初始化一个LoginContext lc和调用它的login方法来执行认证。如果成功，被认证的Subject（包含一个代表这个用户的Principal）通过LoginContext的getSubject方法获得。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">Subject</span> <span class="variable">mySubject</span> <span class="operator">=</span> lc.getSubject();</span><br></pre></td></tr></table></figure>

<p>然后，Main方法调用Subject.doAsPrivileged，将通过认证的Subjectmysubject传给它，一个PrivilegedAction(SampleAction)和一个null AccessControlContext，就像下边的描述：<br>SampleAction 通过下面的方式被初始化：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">PrivilegedAction</span> <span class="variable">action</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SampleAction</span>();</span><br></pre></td></tr></table></figure>

<p>Subject.doAsPrivileged 通过下面的方式被执行：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Subject.doAsPrivileged(mySubject, action, <span class="literal">null</span>);</span><br></pre></td></tr></table></figure>

<p>doAsPrivileged 方法调用PrivilegedAction action (SampleAction)  的run方法开始执行剩下的代码，这些代码被认为是代表mySubject执行。</p>
<p>传给doAsPrivileged 方法一个null作为第三个参数AccessControlContext 表明mySubject应用与一个新的空的AccessControlContext关联。这个结果是以mySubject运行时，发生在SampleAction执行期间的安全检查将只需要SampleAction自己的代码（或者其他它调用的代码）权限。注意，doAsPrivileged 的调用者在执行期间不需要任何的权限。</p>
<h2><span id="sampleactionjava">SampleAction.java</span></h2><p>SampleAction.java包含SampleAction类。这个类实现了java.security.PrivilegedAction，有一个包含所有mySubject想执行的所有代码。对于本教程，我们将做3个步骤，每一个步骤都只有在代码被授予必要的权限的情况下才能做。我们将：</p>
<ol>
<li>读和打印java.home系统属性值</li>
<li>读和打印user.name的系统属性值</li>
<li>判断foo.txt文件在当前目录是否存在</li>
</ol>
<p>这是代码：</p>
<figure class="highlight java"><figcaption><span>SampleAction</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.File;</span><br><span class="line"><span class="keyword">import</span> java.security.PrivilegedAction;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SampleAction</span> <span class="keyword">implements</span> <span class="title class_">PrivilegedAction</span> &#123;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">public</span> Object <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line"> </span><br><span class="line">    System.out.println(<span class="string">&quot;\nYour java.home property value is: &quot;</span></span><br><span class="line">                + System.getProperty(<span class="string">&quot;java.home&quot;</span>));</span><br><span class="line"> </span><br><span class="line">    System.out.println(<span class="string">&quot;\nYour user.home property value is: &quot;</span></span><br><span class="line">                + System.getProperty(<span class="string">&quot;user.home&quot;</span>));</span><br><span class="line"> </span><br><span class="line">    <span class="type">File</span> <span class="variable">f</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">File</span>(<span class="string">&quot;foo.txt&quot;</span>);</span><br><span class="line">    System.out.print(<span class="string">&quot;\nfoo.txt does &quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (!f.exists())</span><br><span class="line">        System.out.print(<span class="string">&quot;not &quot;</span>);</span><br><span class="line">    System.out.println(<span class="string">&quot;exist in the current working directory.&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1><span id="登录配置文件">登录配置文件</span></h1><p>本教程使用的登录配置文件与在JAAS Authentication教程中使用的完全一样。因此，我们可以用jaas.conf文件，文件仅包含一个条目：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">JaasSample &#123;</span><br><span class="line">  com.sun.security.auth.<span class="keyword">module</span>.Krb5LoginModule required;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这个条目被命名为“JaasSample”，也就是我们的教程的应用程序JaasAcn和JaasAzn应用的名字。这个条目指定了用来做认证的LoginModule 是com.sun.security.auth.module包中的Krb5LoginModule ，为了使认证被认为是成功的，Krb5LoginModule需 要“succeed”。 Krb5LoginModule只有在用户提供的用户名和密码成功的登录到Kerberos KDC时才成功。</p>
<h1><span id="策略文件">策略文件</span></h1><p>授权教程保罗两个类，JaasAzn和SampleAction。每个类中的代码包含一些安全敏感的操作，因此，相关的权限需要在一个策略文件中被执行，以使操作被允许执行。</p>
<p>JaasAzn需要的权限<br>JaasAzn类的main方法做了两个操作需要权限的操作：</p>
<ol>
<li>创建了一个LoginContext</li>
<li>调用Subject类的doAsPrivileged 静态方法</li>
</ol>
<p>LoginContext的创建与认证教程中的方式一样，因此，它需要与createLoginContext.JaasSample相同的权限javax.security.auth.AuthPermission。</p>
<p>为了调用Subject类的doAsPrivileged 方法，你需要对doAsPrivileged有一个javax.security.auth.AuthPermission对象。</p>
<p>假设JaasAzn类被放到jaasAzn.jar文件中，这些权限需要通过策略文件中配置grant语句来授权：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">grant codebase <span class="string">&quot;file:./JaasAzn.jar&quot;</span> &#123;</span><br><span class="line">   permission javax.security.auth.AuthPermission </span><br><span class="line">                    <span class="string">&quot;createLoginContext.JaasSample&quot;</span>;</span><br><span class="line">   permission javax.security.auth.AuthPermission <span class="string">&quot;doAsPrivileged&quot;</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>SampleAction需要的权限<br>SampleAction代码做了3个需要权限的操作：</p>
<ol>
<li>读取java.home系统属性</li>
<li>读取user.home系统属性</li>
<li>检查当前文件中是否有foo.txt的文件</li>
</ol>
<p>这些操作需要的权限如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">permission java.util.PropertyPermission <span class="string">&quot;java.home&quot;</span>, <span class="string">&quot;read&quot;</span>;</span><br><span class="line">permission java.util.PropertyPermission <span class="string">&quot;user.home&quot;</span>, <span class="string">&quot;read&quot;</span>;</span><br><span class="line">permission java.io.FilePermission <span class="string">&quot;foo.txt&quot;</span>, <span class="string">&quot;read&quot;</span>;</span><br></pre></td></tr></table></figure>

<p>我们需要给中的SampleAction.class代码授予权限，我们将吧SampleAction.class放到SampleAction.jar文件中。然而，对于这个特定的grant语句，我们希望不仅授权给代码，而且授权给执行代码的特定的用户，来指定如何限制一个特定用户的访问权限。</p>
<p>因此，就像在中 How Do You Make Principal-BasedPolicy File Statements?,描述的，我们的grant语句看起来像下面这样：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">grant codebase <span class="string">&quot;file:./SampleAction.jar&quot;</span>,</span><br><span class="line">    Principal javax.security.auth.kerberos.KerberosPrincipal </span><br><span class="line">        <span class="string">&quot;your_user_name@your_realm&quot;</span>  &#123;</span><br><span class="line"> </span><br><span class="line">   permission java.util.PropertyPermission <span class="string">&quot;java.home&quot;</span>, <span class="string">&quot;read&quot;</span>;</span><br><span class="line">   permission java.util.PropertyPermission <span class="string">&quot;user.home&quot;</span>, <span class="string">&quot;read&quot;</span>;</span><br><span class="line">   permission java.io.FilePermission <span class="string">&quot;foo.txt&quot;</span>, <span class="string">&quot;read&quot;</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>用你的Kerberos用户名和Realm代替。例如，如果你的用户名是“mjones”，Realm是“KRBNT-OPERATIONS.ABC.COM”。你将用<a href="mailto:&#109;&#x6a;&#x6f;&#x6e;&#101;&#x73;&#64;&#75;&#x52;&#x42;&#78;&#84;&#45;&#79;&#x50;&#x45;&#82;&#65;&#x54;&#x49;&#x4f;&#78;&#x53;&#x2e;&#65;&#66;&#67;&#x2e;&#67;&#79;&#77;">mjones@KRBNT-OPERATIONS.ABC.COM</a>。</p>
<p>策略文件的全部内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** Java 2 Access Control Policy for the JaasAzn Application **/</span></span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="comment">/** Code-Based Access Control Policy for JaasAzn **/</span></span><br><span class="line"> </span><br><span class="line">grant codebase <span class="string">&quot;file:./JaasAzn.jar&quot;</span> &#123;</span><br><span class="line"> </span><br><span class="line">   permission javax.security.auth.AuthPermission </span><br><span class="line">                    <span class="string">&quot;createLoginContext.JaasSample&quot;</span>;</span><br><span class="line">   permission javax.security.auth.AuthPermission <span class="string">&quot;doAsPrivileged&quot;</span>;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="comment">/** User-Based Access Control Policy for the SampleAction class</span></span><br><span class="line"><span class="comment"> ** instantiated by JaasAzn </span></span><br><span class="line"><span class="comment"> **/</span></span><br><span class="line"> </span><br><span class="line">grant    codebase <span class="string">&quot;file:./SampleAction.jar&quot;</span>,</span><br><span class="line">    Principal javax.security.auth.kerberos.KerberosPrincipal </span><br><span class="line">        <span class="string">&quot;your_user_name@your_realm&quot;</span>  &#123;</span><br><span class="line"> </span><br><span class="line">   permission java.util.PropertyPermission <span class="string">&quot;java.home&quot;</span>, <span class="string">&quot;read&quot;</span>;</span><br><span class="line">   permission java.util.PropertyPermission <span class="string">&quot;user.home&quot;</span>, <span class="string">&quot;read&quot;</span>;</span><br><span class="line">   permission java.io.FilePermission <span class="string">&quot;foo.txt&quot;</span>, <span class="string">&quot;read&quot;</span>;</span><br><span class="line">&#125;;</span><br><span class="line">``` java</span><br><span class="line"># 运行授权教程的代码</span><br><span class="line">为了执行我们的JAAS授权教程代码，所有你需要做的是：</span><br><span class="line"></span><br><span class="line">## 将下面的文件到一个文件夹</span><br><span class="line"></span><br><span class="line">The JaasAzn.java sourcefile.</span><br><span class="line">The SampleAction.java sourcefile.</span><br><span class="line">The jaas.conf loginconfiguration file.</span><br><span class="line">The jaasazn.policy policyfile.</span><br><span class="line"></span><br><span class="line"># Replace <span class="string">&quot;your_user_name@your_realm&quot;</span>in jaasazn.policy with youruser name and realm.</span><br><span class="line"># Compile SampleAction.java and JaasAzn.java:</span><br><span class="line"></span><br><span class="line">``` java</span><br><span class="line">javac SampleAction.java JaasAzn.java</span><br></pre></td></tr></table></figure>

<h1><span id="create-a-jar-file-named-jaasaznjar-containing-jaasaznclass">Create a JAR file named JaasAzn.jar containing JaasAzn.class:</span></h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">jar -cvf JaasAzn.jar JaasAzn.class</span><br></pre></td></tr></table></figure>

<h1><span id="create-a-jar-file-named-sampleactionjar-containing-sampleactionclass">Create a JAR file named SampleAction.jar containing SampleAction.class:</span></h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">jar -cvf SampleAction.jar SampleAction.class</span><br></pre></td></tr></table></figure>

<h1><span id="execute-the-jaasazn-applicationspecifying">Execute the JaasAzn application,specifying</span></h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">1.</span> by anappropriate -classpath clause that classes should be searched <span class="keyword">for</span> intheJaasAzn.jar and SampleAction.jar JAR files,</span><br><span class="line"><span class="number">2.</span> by -Djava.security.manager thata security manager should be installed,</span><br><span class="line"><span class="number">3.</span> by -Djava.security.krb5.realm=&lt;your_realm&gt; thatyour Kerberos realm is the one specified.</span><br><span class="line"><span class="number">4.</span> by -Djava.security.krb5.kdc=&lt;your_kdc&gt; thatyour Kerberos KDC is the one specified.</span><br><span class="line"><span class="number">5.</span> by -Djava.security.policy=jaasazn.policy thatthe policy file to be used isjaasazn.policy, and</span><br><span class="line"><span class="number">6.</span> by -Djava.security.auth.login.config=jaas.conf thatthe login configuration file to be used is jaas.conf.</span><br></pre></td></tr></table></figure>
<p>下面是Windows下全部的命令：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">java -classpath JaasAzn.jar;SampleAction.jar </span><br><span class="line"> -Djava.security.manager </span><br><span class="line"> -Djava.security.krb5.realm=&lt;your_realm&gt; </span><br><span class="line"> -Djava.security.krb5.kdc=&lt;your_kdc&gt; </span><br><span class="line"> -Djava.security.policy=jaasazn.policy </span><br><span class="line"> -Djava.security.auth.login.config=jaas.conf JaasAzn</span><br></pre></td></tr></table></figure>

<p>这是UNIX下全部的命令</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">java -classpath JaasAzn.jar:SampleAction.jar </span><br><span class="line"> -Djava.security.manager </span><br><span class="line"> -Djava.security.krb5.realm=&lt;your_realm&gt; </span><br><span class="line"> -Djava.security.krb5.kdc=&lt;your_kdc&gt; </span><br><span class="line"> -Djava.security.policy=jaasazn.policy </span><br><span class="line"> -Djava.security.auth.login.config=jaas.conf JaasAzn</span><br></pre></td></tr></table></figure>

<p>将这些命令放到一行里。<br>你将被提示输入Kerberos的用户名和密码，指定在登录配置文件中的底层的Kerberos认证机制将登录到Kerberos。如果你的登录成功，你讲看到Authenticationsucceeded!信息，如果不是，你将看到AuthenticationFailed。<br>一旦认证成功，程序剩下的部分（SampleAction）将代表你这个用户执行，需要你已经被授权合适的权限。Jassazn.policy策略文件将授权给你必要的权限，所以你将看到java.home and user.home和你的当前文件夹是否有一个名叫foo.txt的文件的值被显示。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java ABC</tag>
        <tag>JAAS</tag>
      </tags>
  </entry>
  <entry>
    <title>Java Instument Package</title>
    <url>/posts/1167341847.html</url>
    <content><![CDATA[<h1><span id="javalanginstrument简介">java.lang.instrument简介</span></h1><p>Java5之后，增加了一个包java.lang.instrument，这个包的东西很少，两个接口，ClassFileTransformer和Instrumentation，一个类ClassDefinition，还有两个Exception：IllegalClassFormatException和UnmodifiableClassException；<br>先剧透一下整个包最重要的接口Instrumentation提供的两个功能，总体的功能就是Instrument了，具体来讲两个，从字节码级别为类增加Instrument所需的代码，第二个是获取对象的大小。<br>先看一下这个包的描述吧，下面是JavaDoc的翻译：</p>
<span id="more"></span>

<h2><span id="package-javalanginstrument">package java.lang.instrument</span></h2><p>这个包提供允许一个Java程序语言的代理测量运行在JVM中的程序的多个指标的服务。</p>
<h2><span id="package-javalanginstrumentdescription">Package Java.lang.instrumentDescription</span></h2><p>这个包提供允许一个Java程序语言的代理测量运行在JVM中的程序的多个指标的服务。监测的机制是修改类的方法的字节码。</p>
<h2><span id="package-specification">Package Specification</span></h2><p>一个代理就是部署一个Jar文件。Jar文件的manifest文件（每个jar包根目录下有个META-INF文件夹，此文件夹下的MANIFEST.MF文件就是这里的manifest文件）需要指定这个代理的类，指定之后，这个类将被加载以启动代理。因为支持命令行接口，所以一个代理可以通过在命令行中指定一个选项来移动。具体的实现也可能支持在虚拟机启动之后的某个时间开启代理。例如，一个具体的实现可能提供一个机制，使一个工具附加到一个运行的应用中，然后，初始化这个工具的代理，加载到正在运行的应用中。关于类的加载如何被初始化的细节，依赖于具体的实现。</p>
<h2><span id="command-line-interface">Command-Line Interface</span></h2><p>在用命令行接口的实现中，一个代理可以通过增加下面的Option到命令行中启动：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">-javaagent:jarpath[=options]</span><br></pre></td></tr></table></figure>

<p>Jarpath是代理jar文件的路径。Option是代理的选项。上边的命令可以在同一个命令行中使用多次，因此，可以创建多个代理。多个代理可以使用同一jarpath。一个代理Jar文件必须符合Jar文件规范。</p>
<p>一个代理jar文件的manifest必须包含Premain-Class属性。这个属性的值是代理类的名字。代理类必须实现一个public static的premain 方法，与应用程序中的main方法作为程序入口的原理类似。JVM初始化之后，每一个premain方法将被以代理被指定时的顺序调用，然后应用程序真正的main方法将被调用。每一个premain方法必须以启动的顺序返回结果。（premain&#x3D;pre-main）</p>
<p>Premain方法有两个重载的方法。JVM首先尝试调用代理类中下面的方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="title function_">voidpremain</span><span class="params">(String agentArgs, Instrumentation inst)</span>;</span><br></pre></td></tr></table></figure>

<p>如果代理类没有实现这个方法，jvm将尝试调用</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="title function_">voidpremain</span><span class="params">(String agentArgs)</span>;</span><br></pre></td></tr></table></figure>

<p>代理类可能还有一个agentmain方法，当代理类在JVM启动之后被启动时使用。当代理类使用命令行选项被启动时，agentmain方法不被调用。</p>
<p>Agent类通过系统的类加载器加载（ClassLoader.getSystemClassLoader）。这个类加载器通常跟用来加载包含应用程序的main方法的类加载器是一个。Premain方法将运行在跟应用程序main方法同样的安全机制和类加载下。代理的premain方法做什么是没有限制的。任何应用程序main方法可以做的，包括创建线程，在premain中都是合法的。</p>
<p>每一个代理经由agentArgs参数传给它的代理选项。代理选项以一个字符串被被传递，任何附加的解析操作应该是代理自己执行。</p>
<p>如果代理不能被解析（例如，因为代理类不能加载，或者因为代理类没有一个合适的premain方法），JVM将会abort。如果一个premain方法跑出一个未捕获的错误，JVM将会abort。</p>
<h2><span id="starting-agents-after-vm-startup">Starting Agents After VM Startup</span></h2><p>一个代理具体的实现可能提供一个在JVM启动之后启动的机制。关于代理被初始化的细节是具体实现特定的，但是通常应用程序已经启动，应用程序的main方法已经被调用。在代理的实现支持在JVM启动之后启动的情况下，需要遵循下面的规则：</p>
<ol>
<li>代理的Jar文件中的manifest文件必须包含Agent-Class属性。这个属性的值是代理类的名字。</li>
<li>代理类必须实现一个public static agentmain方法</li>
<li>系统类加载器（ ClassLoader.getSystemClassLoader）必须支持增加一个代理Jar文件到系统类路径。</li>
</ol>
<p>代理的Jar文件被追加到系统类路径中。这个类加载器是通常用来加载包含应用程序main方法的类的类加载器。代理类被加载之后，JVM尝试调用agentmain方法。JVM首先尝试调用代理类中的下面这个方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">agentmain</span><span class="params">(String agentArgs, Instrumentation inst)</span>;</span><br></pre></td></tr></table></figure>

<p>如果代理类中没有实现这个方法，然后JVM尝试调用：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="title function_">voidagentmain</span><span class="params">(String agentArgs)</span>;</span><br></pre></td></tr></table></figure>

<p>这个代理类可能也有一个premain方法，当代理在命令行选项中启动时使用；当这个代理类在JVM启动之后启动时，premain方法不会被调用。</p>
<p>代理通过agentArgs参数传给代理选项。代理选项以字符串的形式传给代理内部的方法，任何附加的解析操作应该代理自己操作。</p>
<p>Agentmain方法应该做一些启动代理之前必要的初始化操作。当启动完成时，这个方法应该返回。如果代理不能被启动（例如，因为代理类不能被加载，或者因为代理类不包含一致的agentmain方法），JVM将不会被abort。如果agentmain方法抛出一个未捕获的错误，这个错误将被忽略。</p>
<p>注：著名的Java运行时分析工具Profiler有在监测Java程序时有两种模式，一种是用Profiler启动应用，一种是应用启动，然后附加到Profiler中，就是分别用的上边的两种方式。</p>
<h2><span id="manifest-attributes">Manifest Attributes</span></h2><p>下面的manifest属性在一个代理的jar文件中被定义：</p>
<p>Premain-Class</p>
<p>当一个代理在JVM启动时被指定时，这个属性指定代理使用的类。也就是，这个类包含premain方法。当一个代理在JVM启动时被指定时，这个属性是必须的。如果没有配置这个属性，JVM将会abort。注意：这是一个雷的名字，不是一个文件名活路径。</p>
<p><strong>Agent-Class</strong><br>如果一个代理的实现支持在JVM启动之后的某个时间启动，这个属性指定一个代理类。也就是说，这个类要包含agentmain方法。这个属性是必须的，如果不配置的话，代理将不会被启动。注意：这是一个类的名字，不是一个文件名字，也不是文件路径。</p>
<p><strong>Boot-Class-Path</strong><br>这个代理的实现中用到的第三方jar包都放到这里边。</p>
<p><strong>Can-Redefine-Classes</strong><br>Boolean（true或false）。Agent是否可以重定义类。可选的，默认是false。</p>
<p><strong>Can-Retransform-Classes</strong><br>Boolean（true或false）。Agent是否可以将类变回原形。可选的，默认是false。</p>
<p><strong>Can-Set-Native-Method-Prefix</strong><br>Boolean（true或false）。Agent是否可以设置本地方法的前缀。可选的，默认是false。</p>
<p>一个代理Jar文件中的manifest文件可能同时包含Premain-Class和Agent-Class属性。当代理在命令行中用-javaagent选项被启动，使用Premain-Class属性指定的代理类的名字，然后Agent-Class属性的配置被忽略。相似地，如果代理在JVM启动之后的某个时间启动，使用Agent-Class属性指定的类的名字，Premain-Class属性被忽略。</p>
<h1><span id="instrumentation">Instrumentation</span></h1><p>下面再看一下Instrumentation这个最主要的接口的一些介绍。</p>
<p>这是一个接口，提供了监测java程序语言代码的服务。Instrumentation将附加的功能的字节码添加到被监控的方法中，来收集应用的运行的一些数据。因为这个改变只有增加东西，监测工具可以不用修改应用的状态和表现。这样的工具包括监控代理，分析工具，代码覆盖分析器和事件日志器等。</p>
<p>有两种方式获得一个Instrumentation接口的实例：</p>
<ol>
<li>当JVM启动时，指定一个代理类。在这种情况下，一个Instrumentation实例传到代理类的premain方法中。</li>
<li>JVM提供可以让代理类在JVM启动后启动的机制。在这种情况下，一个Instrumentation实例传给代理的agentmain方法。</li>
</ol>
<p>这两种机制在上边的package specification中有详尽的描述。</p>
<p>一旦一个代理获取到一个Instrumentation实例，代理可以再之后的任何时刻调用实例的方法。</p>
<h2><span id="method-summary">Method Summary</span></h2><p>主要的方法就是下面几个：</p>
<p>addTransformer：指定一个ClassFileTransformer对象。可以先看下下面ClassFileTransformer接口的介绍。<br>getAllLoadedClasses：返回当前JVM中加载的所有的类的数组（牛逼）<br>getInitiatedClasses：返回指定的类加载器中的所有的类的数据（牛逼，too）<br>getObjectSize：返回一个对象近似的大小<br>redefineClasses：用给定的类的字节码数组替换指定的类的字节码文件，也就是重新定义指定的类<br>retransformClasses：指定一系列的Class对象，被指定的类都会重新变回去（去掉附加的字节码）<br>剩下的几个都是对上边manifest文件中配置的属性的一下操作和运行时接口。</p>
<p>到这，看下Instrumentation接口的主要功能，从Instrumentation接口提供的这几个方法，可以看出Instrumentation的主要功能，就是修改类的字节码文件，更确切的说是修改类的字节码文件，增加自定义的功能的字节码，生成一个新的字节码文件。这是这个接口存在的最主要的意义。</p>
<h1><span id="classfiletransformer">ClassFileTransformer</span></h1><p>先看下Doc上的简介，一个代理提供一个为了改变类文件而存在的接口的实现。这个改变发生在这个类被JVM定义之前。</p>
<p>这个接口只有一个方法transform，将类的字节码传给这个方法，然后返回一个增加了自定义的功能的字节码的字节数组。</p>
<p>这个接口存在的意义，主要是解耦Instrumentation类和改变类文件的操作。</p>
<h1><span id="classdefinition">ClassDefinition</span></h1><p>这个类只在Instrumentation#redefineClasses方法中被调用，这个类有两个域，</p>
<p>Class mClass；</p>
<p>Byte[] mClassFile；</p>
<p>mClass是要重新定义的类的Class对象</p>
<p>mClassFile是新的类的字节码数组</p>
<h1><span id="example1">example1</span></h1><p>下面举一个简单的例子，来感受一下，来自网络，功能是打印每一个方法的执行时间。</p>
<h2><span id="agent类">Agent类</span></h2><figure class="highlight java"><figcaption><span>PerfMonAgent</span></figcaption><table><tr><td class="code"><pre><span class="line">packagecom.highgo.test.instrumentation;</span><br><span class="line"> </span><br><span class="line">importjava.lang.instrument.ClassFileTransformer;</span><br><span class="line">importjava.lang.instrument.Instrumentation;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">PerfMonAgent</span> &#123;</span><br><span class="line"> </span><br><span class="line">         <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">Instrumentation</span> <span class="variable">inst</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"> </span><br><span class="line">         <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">premain</span><span class="params">(String agentArgs, Instrumentation_inst)</span> &#123;</span><br><span class="line">                   System.out.println(<span class="string">&quot;PerfMonAgent.premain()was called. I am premain!&quot;</span>);</span><br><span class="line">                   <span class="comment">// Initialize the static variables we use to trackinformation.</span></span><br><span class="line">                   inst = _inst;</span><br><span class="line">                   <span class="comment">// Set up the class-file transformer.</span></span><br><span class="line">                   <span class="type">ClassFileTransformer</span> <span class="variable">trans</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">PerfMonXformer</span>();</span><br><span class="line">                   System.out.println(<span class="string">&quot;Adding a PerfMonXformerinstance to the JVM.&quot;</span>);</span><br><span class="line">                   inst.addTransformer(trans);</span><br><span class="line">         &#125;</span><br><span class="line"> </span><br><span class="line">         <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">agentmain</span><span class="params">(String agentArgs,Instrumentation _inst)</span> &#123;</span><br><span class="line">                   System.out.println(<span class="string">&quot;PerfMonAgent.premain()was called. I am agentmain!&quot;</span>);</span><br><span class="line">                   <span class="comment">// Initialize the static variables we use to trackinformation.</span></span><br><span class="line">                   inst = _inst;</span><br><span class="line">                   <span class="comment">// Set up the class-file transformer.</span></span><br><span class="line">                   <span class="type">ClassFileTransformer</span> <span class="variable">trans</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">PerfMonXformer</span>();</span><br><span class="line">                   System.out.println(<span class="string">&quot;Adding a PerfMonXformerinstance to the JVM.&quot;</span>);</span><br><span class="line">                   inst.addTransformer(trans);</span><br><span class="line">         &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2><span id="classfiletransformer类">ClassFileTransformer类</span></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">packagecom.highgo.test.instrumentation;</span><br><span class="line"> </span><br><span class="line">importjava.lang.instrument.ClassFileTransformer;</span><br><span class="line">importjava.lang.instrument.IllegalClassFormatException;</span><br><span class="line">importjava.security.ProtectionDomain;</span><br><span class="line"> </span><br><span class="line">importjavassist.CannotCompileException;</span><br><span class="line"><span class="keyword">import</span> javassist.ClassPool;</span><br><span class="line"><span class="keyword">import</span> javassist.CtBehavior;</span><br><span class="line"><span class="keyword">import</span> javassist.CtClass;</span><br><span class="line">importjavassist.NotFoundException;</span><br><span class="line">importjavassist.expr.ExprEditor;</span><br><span class="line">importjavassist.expr.MethodCall;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">PerfMonXformerimplements</span> ClassFileTransformer &#123;</span><br><span class="line">         <span class="keyword">public</span> <span class="type">byte</span>[] transform(ClassLoader loader, StringclassName, Class&lt;?&gt; classBeingRedefined,</span><br><span class="line">                            ProtectionDomain protectionDomain, <span class="type">byte</span>[]classfileBuffer) <span class="keyword">throws</span> IllegalClassFormatException &#123;</span><br><span class="line">                   <span class="type">byte</span>[] transformed = <span class="literal">null</span>;</span><br><span class="line">                   System.out.println(<span class="string">&quot;Transforming &quot;</span> +className);</span><br><span class="line">                   <span class="type">ClassPool</span> <span class="variable">pool</span> <span class="operator">=</span> ClassPool.getDefault();</span><br><span class="line">                   <span class="type">CtClass</span> <span class="variable">cl</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">                   <span class="keyword">try</span> &#123;</span><br><span class="line">                            cl = pool.makeClass(newjava.io.ByteArrayInputStream(classfileBuffer));</span><br><span class="line">                            <span class="keyword">if</span> (cl.isInterface() == <span class="literal">false</span>) &#123;</span><br><span class="line">                                     CtBehavior[] methods =cl.getDeclaredBehaviors();</span><br><span class="line">                                     <span class="keyword">for</span>(<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; methods.length; i++) &#123;</span><br><span class="line">                                               <span class="keyword">if</span>(methods[i].isEmpty() == <span class="literal">false</span>) &#123;</span><br><span class="line">                                                        doMethod(methods[i]);</span><br><span class="line">                                               &#125;</span><br><span class="line">                                     &#125;</span><br><span class="line">                                     transformed = cl.toBytecode();</span><br><span class="line">                            &#125;</span><br><span class="line">                   &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                            System.err.println(<span class="string">&quot;Could notinstrument  &quot;</span> + className + <span class="string">&quot;,  exception : &quot;</span> + e.getMessage());</span><br><span class="line">                   &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                            <span class="keyword">if</span> (cl != <span class="literal">null</span>) &#123;</span><br><span class="line">                                     cl.detach();</span><br><span class="line">                            &#125;</span><br><span class="line">                   &#125;</span><br><span class="line">                   <span class="keyword">return</span> transformed;</span><br><span class="line">         &#125;</span><br><span class="line"> </span><br><span class="line">         <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">doMethod</span><span class="params">(CtBehavior method)</span> throwsNotFoundException, CannotCompileException &#123;</span><br><span class="line">                   <span class="comment">// method.insertBefore(&quot;long stime =System.nanoTime();&quot;);</span></span><br><span class="line">                   <span class="comment">//method.insertAfter(&quot;System.out.println(\&quot;leave&quot;+method.getName()+&quot; andtime:\&quot;+(System.nanoTime()-stime));&quot;);</span></span><br><span class="line">                   method.instrument(<span class="keyword">new</span> <span class="title class_">ExprEditor</span>() &#123;</span><br><span class="line">                            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">edit</span><span class="params">(MethodCall m)</span> throwsCannotCompileException &#123;</span><br><span class="line">                                     m.replace(<span class="string">&quot;&#123; long stime =System.nanoTime(); $_ = $proceed($$); System.out.println(\&quot;&quot;</span></span><br><span class="line">                                                        +m.getClassName() + <span class="string">&quot;.&quot;</span> + m.getMethodName() +<span class="string">&quot;:\&quot;+(System.nanoTime()-stime));&#125;&quot;</span>);</span><br><span class="line">                            &#125;</span><br><span class="line">                   &#125;);</span><br><span class="line">         &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2><span id="manifestmf文件">MANIFEST.MF文件</span></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Manifest-Version: <span class="number">1.0</span></span><br><span class="line">Premain-Class:com.highgo.test.instrumentation.PerfMonAgent</span><br><span class="line">Boot-Class-Path: javassist.jar</span><br></pre></td></tr></table></figure>

<p>Boot-Class-Path这样写要将javassist.jar文件放到jar包的根目录</p>
<h2><span id="打jar包">打jar包</span></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">jar cvfm inst.jar MANIFEST.MFjavassist.jar com</span><br></pre></td></tr></table></figure>

<p>使用自己写的MANIFEST.MF文件，<br>将javassist.jar和com文件夹下的类打成jar包<br>这样这个jar包就作为一个提供打印方法时间的服务而存在了</p>
<h2><span id="使用instjar">使用inst.jar</span></h2><p>随便编写一个类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">App</span> &#123;</span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">      <span class="keyword">new</span> <span class="title class_">App</span>().test();</span><br><span class="line">   &#125;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">test</span><span class="params">()</span> &#123;</span><br><span class="line">      System.out.println(<span class="string">&quot;HelloWorld!!&quot;</span>);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行此类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">java -javaagent:../inst.jarclasspath .;../inst.jar App</span><br></pre></td></tr></table></figure>

<p>可以看到，将运行这个类的过程中用到的所有的类的方法的方法名+时间都打印出来了。</p>
<h1><span id="example12">example12</span></h1><p>这里使用第二种方式，也就是先启动JVM，再启动agent类的方式，做一个例子。</p>
<p>在使用JProfiler的quick attch时，我们的使用方法是这样的：运行我们的应用程序，切换到这个界面，我们的应用就会出现在下面这个地方，ID是OS分配给这个进程的进程ID，后边是进程的名字，其实attch用到的只是进程ID号；选中这个进程，就到了分析界面了。</p>
<p>我们下面的例子也是按照这个顺序：</p>
<ol>
<li>编写Agent，打包成服务</li>
<li>编写一个应用程序，运行</li>
<li>编写一个工具，将Agent的jar包attach到Agent所在的JVM（具体实现是将应用程序的ID传递给工具）</li>
</ol>
<h2><span id="编写agent">编写Agent</span></h2><figure class="highlight java"><figcaption><span>classPerfMonAgent</span></figcaption><table><tr><td class="code"><pre><span class="line">packagecom.highgo.test.instrumentation;</span><br><span class="line"> </span><br><span class="line">importjava.lang.instrument.ClassFileTransformer;</span><br><span class="line">importjava.lang.instrument.Instrumentation;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> classPerfMonAgent &#123;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">Instrumentation</span> <span class="variable">inst</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">premain</span><span class="params">(String agentArgs,Instrumentation _inst)</span> &#123;</span><br><span class="line">      System.out.println(<span class="string">&quot;PerfMonAgent.premain()was called. I am premain!&quot;</span>);</span><br><span class="line">      <span class="comment">// Initialize the static variables we useto track information.</span></span><br><span class="line">      inst = _inst;</span><br><span class="line">      <span class="comment">// Set up the class-file transformer.</span></span><br><span class="line">      <span class="type">ClassFileTransformer</span> <span class="variable">trans</span> <span class="operator">=</span> newPerfMonXformer();</span><br><span class="line">      System.out.println(<span class="string">&quot;Adding aPerfMonXformer instance to the JVM.&quot;</span>);</span><br><span class="line">      inst.addTransformer(trans);</span><br><span class="line">   &#125;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">agentmain</span><span class="params">(StringagentArgs, Instrumentation _inst)</span> &#123;</span><br><span class="line">      System.out.println(<span class="string">&quot;PerfMonAgent.premain()was called. I am agentmain!&quot;</span>);</span><br><span class="line">      inst = _inst;</span><br><span class="line">      Class&lt;?&gt;[] classes =inst.getAllLoadedClasses();</span><br><span class="line">      <span class="keyword">for</span> (Class&lt;?&gt; cls : classes) &#123;</span><br><span class="line">         System.out.println(cls.getName());</span><br><span class="line">      &#125;</span><br><span class="line">      System.out.println(<span class="string">&quot;classeslength:&quot;</span>+classes.length);</span><br><span class="line">      System.out.println(<span class="string">&quot;PerfMonAgent.premain()end!&quot;</span>);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里把第二种方式用到的agentmain方法直接加到第一种方式使用的类中了。</p>
<h2><span id="编写manifest文件">编写MANIFEST文件</span></h2><figure class="highlight java"><figcaption><span>MANIFEST</span></figcaption><table><tr><td class="code"><pre><span class="line">Manifest-Version: <span class="number">1.0</span></span><br><span class="line">Premain-Class:com.highgo.test.instrumentation.PerfMonAgent</span><br><span class="line">Agent-Class:com.highgo.test.instrumentation.PerfMonAgent</span><br><span class="line">Boot-Class-Path: javassist.jar</span><br></pre></td></tr></table></figure>

<p>只是增加了Agent-Class的配置。</p>
<h2><span id="将此agent打成jar包">将此Agent打成Jar包</span></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">jar cvfm inst.jar MANIFEST.MFjavassist.jar com</span><br></pre></td></tr></table></figure>

<p>还是上边用到的命令</p>
<h2><span id="编写应用程序">编写应用程序</span></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TargetVM</span> &#123;</span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">      <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">         Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>就一个线程，一直运行着。</p>
<h2><span id="查看上边进程的id">查看上边进程的ID</span></h2><p>用jps命令可以方便的查看进程的ID，名字是应用程序main方法所在的类的名字。可以很容易的分辨出来。</p>
<h2><span id="运行attach工具类">运行Attach工具类</span></h2><figure class="highlight java"><figcaption><span>AttachUtil</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AttachUtil</span> &#123;</span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> AttachNotSupportedException, IOException, AgentLoadException,AgentInitializationException &#123;</span><br><span class="line">      <span class="type">String</span> <span class="variable">id</span> <span class="operator">=</span> <span class="string">&quot;520384&quot;</span>;</span><br><span class="line">      <span class="type">VirtualMachine</span> <span class="variable">vm</span> <span class="operator">=</span> VirtualMachine.attach(id);</span><br><span class="line">        vm.loadAgent(<span class="string">&quot;inst.jar&quot;</span>);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里用到了JDK的tools.jar工具包，不了解这个Jar包的朋友可以再本博客搜一下关于这个jar包的详细介绍。Eclipse默认加的jar包没有这个，可以从jdk的安装目录里找到，加到项目的sspath里就行了；如果是用javac去编译的话，只要将这个类加入到系统的classpath里就可以了，这个工具类就也可以编译，也可以运行了。</p>
<p>将ID修改成jps查看到的那个ID号，这个工具类就可以创建一个ID号所在的进程的VirtualMachine对象，这个对象就代表那个JVM。VirtualMachine#loadAgent方法可以加载一个Agent。</p>
<p>当AttachUtil方法运行到vm.loadAgent(“inst.jar”);这行代码的时候，就可以看到Eclipse的控制台会打印出PerfMonAgent# agentmain方法执行的打印内容,应用程序所在的JVM中加载的所有的类的名字。</p>
<h1><span id="instrumentationgetobjectsize">Instrumentation#getObjectSize</span></h1><p>从上边Instrumentation的method summary可以看出，Instrumentation接口的另一个功能就是获取一个对象的大小。</p>
<p>这里我们直接分析一下classmexer的源码</p>
<h2><span id="manifest文件">MANIFEST文件</span></h2><p>Classmexer的Agent使用的是第一种方式，所以看他的MANIFEST.MF文件如下：</p>
<figure class="highlight java"><figcaption><span>MANIFEST</span></figcaption><table><tr><td class="code"><pre><span class="line">Manifest-Version: <span class="number">1.0</span></span><br><span class="line">Created-By: <span class="number">1.6</span><span class="number">.0</span> (SunMicrosystems Inc.)</span><br><span class="line">Premain-Class:com.javamex.classmexer.Agent</span><br></pre></td></tr></table></figure>

<p>这个项目只有两个类，Agent和MemoryUtil</p>
<h2><span id="agent类">Agent类</span></h2><p>Agent的主要目的是获取一个Instrumentation的实例。内容如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">packagecom.javamex.classmexer;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">import</span> java.lang.instrument.Instrumentation;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Agent</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> Instrumentationinstrumentation;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">premain</span><span class="params">(String args,Instrumentation instr)</span></span><br><span class="line">  &#123;</span><br><span class="line">    instrumentation = instr;</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">protected</span> <span class="keyword">static</span> Instrumentation <span class="title function_">getInstrumentation</span><span class="params">()</span></span><br><span class="line">  &#123;</span><br><span class="line">    <span class="type">Instrumentation</span> <span class="variable">instr</span> <span class="operator">=</span> instrumentation;</span><br><span class="line">    <span class="keyword">if</span> (instr == <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="keyword">throw</span> newIllegalStateException(<span class="string">&quot;Agent not initted&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> instr;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2><span id="memoryutil类">MemoryUtil类：</span></h2><figure class="highlight java"><figcaption><span>MemoryUtil</span></figcaption><table><tr><td class="code"><pre><span class="line">packagecom.javamex.classmexer;</span><br><span class="line"> </span><br><span class="line">importjava.lang.instrument.Instrumentation;</span><br><span class="line">importjava.lang.reflect.Field;</span><br><span class="line"><span class="keyword">import</span> java.util.Collection;</span><br><span class="line"><span class="keyword">import</span> java.util.HashSet;</span><br><span class="line"><span class="keyword">import</span> java.util.Set;</span><br><span class="line"><span class="keyword">import</span> java.util.Stack;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MemoryUtil</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">enum</span> <span class="title class_">VisibilityFilter</span></span><br><span class="line">  &#123;</span><br><span class="line">    ALL, PRIVATE_ONLY,  NON_PUBLIC,  NONE;</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="type">long</span> <span class="title function_">memoryUsageOf</span><span class="params">(Object obj)</span></span><br><span class="line">  &#123;</span><br><span class="line">    returnAgent.getInstrumentation().getObjectSize(obj);</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="type">long</span> <span class="title function_">deepMemoryUsageOf</span><span class="params">(Objectobj)</span></span><br><span class="line">  &#123;</span><br><span class="line">    <span class="keyword">return</span> deepMemoryUsageOf(obj,VisibilityFilter.NON_PUBLIC);</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="type">long</span> <span class="title function_">deepMemoryUsageOf</span><span class="params">(Objectobj, VisibilityFilter referenceFilter)</span></span><br><span class="line">  &#123;</span><br><span class="line">    returndeepMemoryUsageOf0(Agent.getInstrumentation(), <span class="keyword">new</span> <span class="title class_">HashSet</span>(), obj,referenceFilter);</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="title function_">longdeepMemoryUsageOfAll</span><span class="params">(Collection&lt;? extends Object&gt; objs)</span></span><br><span class="line">  &#123;</span><br><span class="line">    <span class="keyword">return</span> deepMemoryUsageOfAll(objs,VisibilityFilter.NON_PUBLIC);</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="title function_">longdeepMemoryUsageOfAll</span><span class="params">(Collection&lt;? extends Object&gt; objs, VisibilityFilterreferenceFilter)</span></span><br><span class="line">  &#123;</span><br><span class="line">    <span class="type">Instrumentation</span> <span class="variable">instr</span> <span class="operator">=</span>Agent.getInstrumentation();</span><br><span class="line">    <span class="type">long</span> <span class="variable">total</span> <span class="operator">=</span> <span class="number">0L</span>;</span><br><span class="line">   </span><br><span class="line">    Set&lt;Integer&gt; counted = newHashSet(objs.size() * <span class="number">4</span>);</span><br><span class="line">    <span class="keyword">for</span> (Object o : objs) &#123;</span><br><span class="line">      total += deepMemoryUsageOf0(instr,counted, o, referenceFilter);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> total;</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="title function_">longdeepMemoryUsageOf0</span><span class="params">(Instrumentation instrumentation, Set&lt;Integer&gt; counted,Object obj, VisibilityFilter filter)</span></span><br><span class="line">    <span class="keyword">throws</span> SecurityException</span><br><span class="line">  &#123;</span><br><span class="line">    Stack&lt;Object&gt; st = <span class="keyword">new</span> <span class="title class_">Stack</span>();</span><br><span class="line">    st.push(obj);</span><br><span class="line">    <span class="type">long</span> <span class="variable">total</span> <span class="operator">=</span> <span class="number">0L</span>;</span><br><span class="line">    <span class="keyword">while</span> (!st.isEmpty())</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="type">Object</span> <span class="variable">o</span> <span class="operator">=</span> st.pop();</span><br><span class="line">      <span class="keyword">if</span> (counted.add(Integer.valueOf(System.identityHashCode(o))))</span><br><span class="line">      &#123;</span><br><span class="line">        <span class="type">long</span> <span class="variable">sz</span> <span class="operator">=</span>instrumentation.getObjectSize(o);</span><br><span class="line">        total += sz;</span><br><span class="line">       </span><br><span class="line">        <span class="type">Class</span> <span class="variable">clz</span> <span class="operator">=</span> o.getClass();</span><br><span class="line">       </span><br><span class="line"> </span><br><span class="line">        <span class="type">Class</span> <span class="variable">compType</span> <span class="operator">=</span>clz.getComponentType();</span><br><span class="line">        Object localObject1;</span><br><span class="line">        Object localObject2;</span><br><span class="line">        Object el;</span><br><span class="line">        <span class="keyword">if</span> ((compType != <span class="literal">null</span>) &amp;&amp;</span><br><span class="line">          (!compType.isPrimitive()))</span><br><span class="line">        &#123;</span><br><span class="line">          Object[] arr = (Object[])o;</span><br><span class="line">          Object[] arrayOfObject1 =arr;localObject1 = <span class="number">0</span>;</span><br><span class="line">         <span class="keyword">for</span> (localObject2 = arrayOfObject1.length; localObject1 &lt;localObject2; localObject1++)</span><br><span class="line">          &#123;</span><br><span class="line">            el = arrayOfObject1[localObject1];</span><br><span class="line">            <span class="keyword">if</span> (el != <span class="literal">null</span>) &#123;</span><br><span class="line">              st.push(el);</span><br><span class="line">            &#125;</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (clz != <span class="literal">null</span>)</span><br><span class="line">        &#123;</span><br><span class="line">          <span class="keyword">for</span> (Field fld :clz.getDeclaredFields())</span><br><span class="line">          &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">mod</span> <span class="operator">=</span> fld.getModifiers();</span><br><span class="line">            <span class="keyword">if</span> (((mod &amp; <span class="number">0x8</span>) == <span class="number">0</span>)&amp;&amp; (isOf(filter, mod)))</span><br><span class="line">            &#123;</span><br><span class="line">              <span class="type">Class</span> <span class="variable">fieldClass</span> <span class="operator">=</span> fld.getType();</span><br><span class="line">              <span class="keyword">if</span> (!fieldClass.isPrimitive())</span><br><span class="line">              &#123;</span><br><span class="line">                <span class="keyword">if</span> (!fld.isAccessible()) &#123;</span><br><span class="line">                  fld.setAccessible(<span class="literal">true</span>);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">try</span></span><br><span class="line">                &#123;</span><br><span class="line">                  <span class="type">Object</span> <span class="variable">subObj</span> <span class="operator">=</span> fld.get(o);</span><br><span class="line">                  <span class="keyword">if</span> (subObj != <span class="literal">null</span>) &#123;</span><br><span class="line">                    st.push(subObj);</span><br><span class="line">                  &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">catch</span> (IllegalAccessExceptionillAcc)</span><br><span class="line">                &#123;</span><br><span class="line">                  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">InternalError</span>(<span class="string">&quot;Couldn&#x27;tread &quot;</span> + fld);</span><br><span class="line">                &#125;</span><br><span class="line">              &#125;</span><br><span class="line">            &#125;</span><br><span class="line">          &#125;</span><br><span class="line">          clz = clz.getSuperclass();</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> total;</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">boolean</span> <span class="title function_">isOf</span><span class="params">(VisibilityFilterf, <span class="type">int</span> mod)</span></span><br><span class="line">  &#123;</span><br><span class="line">    <span class="keyword">switch</span> (f)</span><br><span class="line">    &#123;</span><br><span class="line">    <span class="keyword">case</span> ALL:</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">case</span> PRIVATE_ONLY:</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">case</span> NONE:</span><br><span class="line">      <span class="keyword">return</span> (mod &amp; <span class="number">0x2</span>) != <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">case</span> NON_PUBLIC:</span><br><span class="line">      <span class="keyword">return</span> (mod &amp; <span class="number">0x1</span>) == <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">throw</span> newIllegalArgumentException(<span class="string">&quot;Illegal filter &quot;</span> + mod);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看出，最主要的方法是deepMemoryUsageOf0(Instrumentation instrumentation,Set<integer> counted, Object obj, VisibilityFilter filter)<br>这个方法的主要的工作是对一个对象的域进行深度遍历，获取最底层的每个对象的大小，然后加起来。<br>到这里，Instrumentation的所有的功能就完事了，其实就是两个，一个获取对象的大小，一个是作为一个服务，在JVM中的类被定义之前修改类的字节码，跟Spring的AOP不仅形似而且神似。<br>上边的两个Example的代码分别来自于：<br><a href="http://blog.csdn.net/ykdsg/article/details/12080071">http://blog.csdn.net/ykdsg/article/details/12080071</a><br><a href="http://blog.csdn.net/pwlazy/article/details/5109742">http://blog.csdn.net/pwlazy/article/details/5109742</a></integer></p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java ABC</tag>
        <tag>Instument</tag>
      </tags>
  </entry>
  <entry>
    <title>Java Secret: Using an enum to build a State machine</title>
    <url>/posts/1404542518.html</url>
    <content><![CDATA[<p>原文链接：<a href="http://www.javacodegeeks.com/2011/07/java-secret-using-enum-to-build-state.html">http://www.javacodegeeks.com/2011/07/java-secret-using-enum-to-build-state.html</a><br>作者：Peter Lawrey    译者：陈振阳</p>
<p>最近在读Hadoop#Yarn部分的源码，读到状态机那一部分的时候，感到enmu的用法实在是太灵活了，在给并发编程网翻译一篇文章的时候，正好碰到一篇这样的文章，就赶紧翻译下来，涨涨姿势。</p>
<h1><span id="综述">综述</span></h1><p>Java中的enum比其他的语言中的都强大，这产生了很多令人惊讶的用法。<br>本文中，我将列出Java中的enum的一些特性，然后将这些特性应用到一起构成一个状态机。</p>
<span id="more"></span>
<h1><span id="enum的单例和工具类用法">Enum的单例和工具类用法</span></h1><p>你可以非常简单地用一个enmu构建一个单例或者工具类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">Singleton</span> &#123;</span><br><span class="line">    INSTANCE;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">Utility</span> &#123;</span><br><span class="line">    ; <span class="comment">// no instances</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h1><span id="用enum实现一个接口">用enum实现一个接口</span></h1><p>你也可以在一个enum中实现一个接口。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Named</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">name</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">order</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">Planets</span> <span class="keyword">implements</span> <span class="title class_">Named</span> &#123;</span><br><span class="line">    Mercury, Venus, Earth, Mars, Jupiter, Saturn, Uranus, Neptune;</span><br><span class="line">    <span class="comment">// name() is implemented automagically.</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">order</span><span class="params">()</span> &#123; <span class="keyword">return</span> ordinal()+<span class="number">1</span>; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1><span id="每一个enum实例一个不同的子类">每一个enum实例，一个不同的子类</span></h1><p>你可以重载一个enum实例的方法。这将高效的给以个enum的实例一个自己的实现。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// from http://download.oracle.com/javase/1,5.0/docs/guide/language/enums.html</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">Operation</span> &#123;</span><br><span class="line">  PLUS   &#123; <span class="type">double</span> <span class="title function_">eval</span><span class="params">(<span class="type">double</span> x, <span class="type">double</span> y)</span> &#123; <span class="keyword">return</span> x + y; &#125; &#125;,</span><br><span class="line">  MINUS  &#123; <span class="type">double</span> <span class="title function_">eval</span><span class="params">(<span class="type">double</span> x, <span class="type">double</span> y)</span> &#123; <span class="keyword">return</span> x - y; &#125; &#125;,</span><br><span class="line">  TIMES  &#123; <span class="type">double</span> <span class="title function_">eval</span><span class="params">(<span class="type">double</span> x, <span class="type">double</span> y)</span> &#123; <span class="keyword">return</span> x * y; &#125; &#125;,</span><br><span class="line">  DIVIDE &#123; <span class="type">double</span> <span class="title function_">eval</span><span class="params">(<span class="type">double</span> x, <span class="type">double</span> y)</span> &#123; <span class="keyword">return</span> x / y; &#125; &#125;;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Do arithmetic op represented by this constant</span></span><br><span class="line">  <span class="keyword">abstract</span> <span class="type">double</span> <span class="title function_">eval</span><span class="params">(<span class="type">double</span> x, <span class="type">double</span> y)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1><span id="使用一个enum实现一个状态机">使用一个enum实现一个状态机</span></h1><p>用上边的技术你可以做的是创建一个基于状态的enum。</p>
<p>在这个小例子中，一个解析器的状态机处理一个来自一个ByteBuffer的原始的XML。每一个状态都有自己的处理方法，如果没有足够的可用的数据，状态机可以返回来再次获取更多的数据。状态之间的每一次变换都被定义，所有状态的代码在一个enum中。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Context</span> &#123;</span><br><span class="line">    ByteBuffer <span class="title function_">buffer</span><span class="params">()</span>;</span><br><span class="line">    State <span class="title function_">state</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">state</span><span class="params">(State state)</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">State</span> &#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">       * <span class="doctag">@return</span> true to keep processing, false to read more data.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="type">boolean</span> <span class="title function_">process</span><span class="params">(Context context)</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">States</span> <span class="keyword">implements</span> <span class="title class_">State</span> &#123;</span><br><span class="line">    XML &#123;</span><br><span class="line">        <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">process</span><span class="params">(Context context)</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (context.buffer().remaining() &lt; <span class="number">16</span>) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">            <span class="comment">// read header</span></span><br><span class="line">            <span class="keyword">if</span>(headerComplete)</span><br><span class="line">                context.state(States.ROOT);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;, ROOT &#123;</span><br><span class="line">        <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">process</span><span class="params">(Context context)</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (context.buffer().remaining() &lt; <span class="number">8</span>) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">            <span class="comment">// read root tag</span></span><br><span class="line">            <span class="keyword">if</span>(rootComplete)</span><br><span class="line">                context.state(States.IN_ROOT);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">process</span><span class="params">(Context context)</span> &#123;</span><br><span class="line">    socket.read(context.buffer());</span><br><span class="line">    <span class="keyword">while</span>(context.state().process(context));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用这种方式，可以创建一个XML解析器，解析器可以处理10微秒内的数据包。大多数情况下，它跟你需要的一样高效。</p>
<p><strong>Reference</strong>: <a href="http://vanillajava.blogspot.com/2011/06/java-secret-using-enum-as-state-machine.html">Java Secret: Using an enum to build a State machine</a> from our <a href="https://www.javacodegeeks.com/join-us/jcg">JCG partner</a> <a href="http://www.blogger.com/profile/17982030676088168612">Peter Lawrey</a> at the <a href="http://vanillajava.blogspot.com/">Vanilla Java</a>.</p>
<h1><span id="related-articles">Related Articles</span></h1><p><a href="http://www.javacodegeeks.com/2011/07/low-gc-in-java-use-primitives-instead.html">Low GC in Java: Use primitives instead of wrappers</a><br><a href="https://www.javacodegeeks.com/2011/06/java-lambda-syntax-alternatives.html">Java Lambda Syntax Alternatives</a><br><a href="https://www.javacodegeeks.com/2011/05/how-jvm-handle-locks.html">How does JVM handle locks</a><br><a href="https://www.javacodegeeks.com/2011/04/erlang-vs-java-memory-architecture.html">Erlang vs Java memory architecture</a><br><a href="https://www.javacodegeeks.com/2011/02/java-forkjoin-parallel-programming.html">Java Fork&#x2F;Join for Parallel Programming</a></p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java ABC</tag>
        <tag>Java enum</tag>
      </tags>
  </entry>
  <entry>
    <title>Kafka Message Delivery Semantics</title>
    <url>/posts/3775642630.html</url>
    <content><![CDATA[<h1><span id="kafka-消息交付delivery语义">Kafka 消息交付（Delivery）语义</span></h1><p>Kafka的消息交付语义有三种，At most once、At least once和Exactly once，它们的官方解释如下：</p>
<ul>
<li>At most once—Messages may be lost but are never redelivered.</li>
<li>At least once—Messages are never lost but may be redelivered.</li>
<li>Exactly once—this is what people actually want, each message is delivered once and only once.</li>
</ul>
<span id="more"></span>
<h2><span id="once的意义">Once的意义</span></h2><p>上面提到的Kafka的三种消息交付语义的知识点已经烂大街了，但是这里的once指的意义，需要进一步明确一下；<br>这里的Once的指的不是消息接收到的次数，而是指的在没有抛出阻碍给Kafka Server反馈Offset的异常的情况下，成功处理一条消息的次数。</p>
<p>这里需要说明两个情况，第一个是消息处理逻辑幂等，第二个是消息处理逻辑抛出没有阻碍给Kafka Server反馈Offset的异常。</p>
<h2><span id="消息处理逻辑幂等">消息处理逻辑幂等</span></h2><p>微服务火起来之后，幂等这个词也跟着火起来了，微服务的环境中，大多数的服务都需要考虑幂等设计。那这里需要明确的是，同一条消息，被一个幂等设计的处理逻辑接收到多次，那么这条消息是相当于被处理了一次还是多次呢？<br>没错，是被处理了多次，而且是相当于被成功处理了多次。<br>对于Kafka来说，不管你的处理逻辑是否幂等，那消息处理的模式都是接收消息-&gt;处理消息-&gt;提交Offset，这三个步骤，消息走完一遍即是被成功处理了一次，不管处理消息这一步是否是幂等的。</p>
<h2><span id="消息处理逻辑抛出没有阻碍给kafka-server反馈offset的异常">消息处理逻辑抛出没有阻碍给Kafka Server反馈Offset的异常</span></h2><p>消息处理失败并不是简单的指的程序抛出异常，而是这个异常一定要阻碍了后续给Kafa Server 反馈Offset才算处理失败。比如说，接收到消息之后，业务处理逻辑在处理的过程中，抛出一个InvalidUserNameException，很明显，这是一个业务级别的异常。这个异常虽然在此处被抛出去，但是整个应用中，一定有一个地方会处理这个异常，比如说简单的记录日志，或者将此消息发送到一个特定的kafka topic，由另外的逻辑来处理。<br>这里需要注意的是，从上面的处理过程来看，我们完美的处理了这个消息，这个消息虽然不合法，但是我们也处理了不合法的情况，所以这条消息，对于Kafka Server来说，我们是成功处理了，没有必要再让Kafka Server重新发送一次此消息了。所以在处理完InvalidUserNameException异常之后，我们应该提交Offset给Kafka Server，告诉Kafka Sever，我们成功消费了这条消息。</p>
<p>在使用Spring Kafka时，其提供了一个类似的特性，消息在处理失败达到指定的最大次数的时候，会将其发送到一个dead-letter topic，然后commit offset，即是这样的情况。这是Spring Kafka框架替我们做的recover机制，对于Kafka Server来说，这条消息被成功处理了，并且收到了KafkaConsumer Commit的Offset。</p>
<p>在Spring Kafka 中存在一个ErrorHandler组件，可以配置到@KafkaListener注解上。配置当Listener出错时，可以使用指定的ErrorHandler来处理失败，在ErrorHandler中，我们可以拿到抛出的异常对象，当前正在处理的消息对象和Consumer对象，我们可以根据异常的类型和消息对象提供的信息，来灵活的判断是否需要使用Consumer提交offset。</p>
<p>其反面的就是像数据库连接失败，数据库死锁等的异常，如果消息处理过程中，出现了此类异常，只能重传了。</p>
<h2><span id="消息交付语义是指的producer发送消息还是指的consumer接收消息">消息交付语义是指的Producer发送消息还是指的Consumer接收消息？</span></h2><p>KafkaProducer在发送消息时，可能会重复发送，Consumer接收处理消息时，如果过早的提交offset，可能会导致消息处理失败，无法提交offset，而再次接收到同一条消息。</p>
<p>那么，消息交付语义是指的Producer发送消息还是指的Consumer接收消息呢？显然都不是，而是指的在Producer和Consumer的配合下，实现消息被成功处理的次数的可能的情况。</p>
<p>比如At most once，就是指的在Producer和Consumer的配合下，消息最多会被成功处理一次。看上面可以理解成功处理的意义。举个例子,BS&#x3D;Business Service：</p>
<p>Kafka Topic1-&gt;BS1-Kafka Topic2-&gt;BS2-Kafka Topic3-&gt;BS3-&gt;Kafka Topic4-&gt;DB</p>
<p>上面的例子，在3个BS处，都是Receive-Process-Produce模式，如果我们在3个BS处的KafkaConsumer都可能会重复收到消息，KafkaProducer对象都可能会重复发送消息，那么在DB处就可能会出现重复的结果。另外两个Kafka交付语义类似。</p>
<h1><span id="kafka-消息交付delivery语义配置">Kafka 消息交付（Delivery）语义配置</span></h1><p>KafkaProducer相关配置参数：acks、retries、enable.idempotence、max.in.flight.requests.per.connection<br>KafkaConsumer相关配置参数：enable.auto.commit、auto.commit.interval.ms、isolation.level</p>
<h1><span id="kafka-client-api">Kafka Client API</span></h1><figure class="highlight java"><figcaption><span>KafkaProducer</span></figcaption><table><tr><td class="code"><pre><span class="line">KafkaProducer</span><br><span class="line">initTransactions</span><br><span class="line">beginTransaction</span><br><span class="line">sendOffsetsToTransaction</span><br><span class="line">commitTransaction</span><br><span class="line">abortTransaction</span><br></pre></td></tr></table></figure>
<p>KafkaProducer事务API用来保证在Receive-Process-Produce模式中，Produce数据到Kafka Topic时的EOS语义的；<br>at most once 和 at least once的实现不用KafkaProducer事务API；只需要配置上述相关参数，处理好消息的业务处理和offset保存的先后顺序即可。</p>
<pre><code>KafkaConsumer
commitSync
commitAsync
</code></pre>
<p>如果设置了consumer自动提交之后，也就是enable.auto.commit和auto.commit.interval.ms，则consumer每隔auto.commit.interval.ms自动commit；</p>
<h1><span id="kafka-消息交付delivery语义实现">Kafka 消息交付（Delivery）语义实现</span></h1><h2><span id="at-most-once-实现">At most once 实现</span></h2><p>通过禁用 producer 的重传功能和让 consumer 在处理一批消息之前提交 offset，实现 at-most-once 的消息交付<br>KafkaProducer：禁用幂等，关闭重传；acks&#x3D;0或1，ack0失败的概率最高， ack1也有可能失败；acks&#x3D;0时retries配置实效；enable.idempotence&#x3D;false，默认fase；<br>KafkaConsumer：consumer 在处理一批消息之前提交 offset；可以enable.auto.commit&#x3D;true+auto.commit.interval.ms较小值，或者手动KafkaConsuer#commitSync()</p>
<h2><span id="at-least-once-实现">At least once 实现</span></h2><p>Kafka 默认保证 at-least-once 的消息交付。<br>KafkaProducer：禁用幂等，开启重传；acks&#x3D;all；acks配置较大数值；enable.idempotence&#x3D;false，默认fase；<br>KafkaConsumer：consumer 在处理一批消息之后提交 offset；关闭自动提交，处理完消息之后手动commit；</p>
<h2><span id="exactly-once-实现">Exactly once 实现</span></h2><p>KafkaProducer：开启幂等；启动幂等之后，kafka client自动将retries设置大于0，all&#x3D;all；启用幂等，即可保证正好一次；如果是发送给多个topics，配置transactional.id，使用Producer事务API开启事务，即可保证同一个事务中的消息只发送成功一次；启动幂等之后，max.in.flight.requests.per.connection需要设置小于等于5；<br>KafkaConsumer：正好被成功处理一次的思路是，保存offset的操作和业务逻辑放到一个事务中；</p>
<p>正好一次的典型应用场景是Receive-Process-Produce，将这三个操作，放到一个事务中，要么一起成功，要么一起失败；</p>
<p>这里列举两个具体场景：<br>在Receive-Process-Produce，如果最后的Produce是生产数据到外部系统，则将offset保存这个外部系统中，使用外部系统的事务，将Produce的数据和Offset放到一个外部系统的事务中，一起提交。比如使用关系型数据库，则可以使用JDBC事务实现；Kafka Connect就是使用这种方法，保证数据只被导出一次的。Kafka Streams中，典型的应用场景是最终将数据导出到非Kafka系统，此时使用的就是Kafka Connect。</p>
<p>在Receive-Process-Produce，如果是发送到另一个Kafka Topic，则使用KafkaProducer的事务API 将Offset的保存（offset被保存在kafka的内置的_consumer_offset中）和Producer发送放到一个事务中；Kafka Streams里所有中间Processor节点的输入输出都是Kafka Topic，其就是使用这个机制保证的Kafka Stream所有中间Processor处的消息的EOS；使用事务性KafkaProducer提供 整个Stream上exactly-once 的消息交付能力。</p>
<blockquote>
<p>由于Zookeeper并不适合大批量的频繁写入操作，新版Kafka已推荐将consumer的位移信息保存在Kafka内部的topic中，即__consumer_offsets topic，并且默认提供了kafka_consumer_groups.sh脚本供用户查看consumer信息。</p>
</blockquote>
<p>在Receive-Process-Produce模式中，其实消息Process之后，Produce到Kafka Topic还是发送到其他拥有事务管理能力的外部系统，是一个处理思路，都是用将offset的保存和外部系统的事务放在一起。</p>
]]></content>
      <categories>
        <category>Apache Kafka</category>
      </categories>
      <tags>
        <tag>Apache Kafka</tag>
      </tags>
  </entry>
  <entry>
    <title>Shared Transaction Resource Pattern Implements Distributed Transactions in Spring without XA</title>
    <url>/posts/3984494298.html</url>
    <content><![CDATA[<h1><span id="概述">概述</span></h1><p><a href="https://www.javaworld.com/article/2077963/distributed-transactions-in-spring--with-and-without-xa.html">Distributed transactions in Spring, with and without XA</a>一文中描述了在Spring中实现分布式事务的7种处理模式；3种基于XA协议，4种特定场景的非XA协议的模式；</p>
<p>共享事务资源模式（Shared Transaction Resource pattern）是四种非XA协议的第一种，其提供了特定场景的多资源事务同步模式；</p>
<p>熟悉Spring 事务框架的看到Transaction Resource应该立马就明白了，Transaction Resource就是JDBC里的Connection对象，Hibernete里的Session对象，以此类比；</p>
<span id="more"></span>
<p>像JDBC的Connection，Hibernete的Session等如果要共享，也就意味着两个事务使用的是同一个连接，也就意味着不再有分布式事务一说，两个系统的操作真的是在同一个事务之中。</p>
<h1><span id="orm框架和spring-jdbc">ORM框架和Spring JDBC</span></h1><p>先描述一下文中的两个例子：<br>第一个是ORM框架和JDBC，这是一个好像不是分布式事务的例子，在这里我们描述的更具体一些，将JDBC换乘Spring JDBC，让他看上去更像分布式事务一些，理解这个例子是基础，一定要仔细看看：<br>在基于Spring架构的应用中，如果同时使用了ORM框架Hibernete和Spring JDBC一起操作数据库，那么如何将Spring JDBC的事务和Hibernete的事务进行同步呢？</p>
<p>答案就是共用一个JDBC Connection对象，Spring JDBC封装了Java JDBC，Hibernete也是基于JDBC构建的，也就是说如果在Spring  JDBC 和 Hibernete中使用同一个Java JDBC Connection对象，就可以实现事务的同步，其实根本就是在一个JDBC 事务中。</p>
<h1><span id="消息驱动的单个数据库更新">消息驱动的单个数据库更新</span></h1><p>文中提到共享事务资源模式的典型应用场景是<strong>消息驱动的单个数据库更新</strong>。这是一个看上去就需要分布式事务同步场景。</p>
<h2><span id="基于activemq实现消息驱动的单个数据库更新场景">基于ActiveMQ实现消息驱动的单个数据库更新场景</span></h2><p>文中同时举了一个ActiveMQ + JDBC的例子，ActiveMQ是JMS协议的实现，同时ActiveMQ支持可配置的消息存储的，其中就可以将ActiveMQ的队列中的消息存储到关系型数据库中。</p>
<p>当把ActiveMQ把消息存储到关系型数据库中之后，如果我们后续的消息的业务处理之后的结果，也写到同一个数据库中，那ActiveMQ中消息的更新和业务系统中消息的处理结果持久化就可以共用一个数据库连接了。</p>
<p>可以共用一个数据库连接，也就意味着不再牵扯到分布式事务；</p>
<h2><span id="基于kafka实现消息驱动的单个数据库更新场景">基于Kafka实现消息驱动的单个数据库更新场景</span></h2><p>这里在送大家一个特定的例子，基于Kafka实现消息驱动的单个数据库更新的场景。上边的例子有着很大的性能问题，而基于Kafka实现消息驱动的单个数据库更新的场景就像是天造地设一般。</p>
<p>Kafka消息队列里的读取进度是存储在Consumer端的，在Kafka中，叫做Offset。也就是说，如果我们在拉取消息之后，将最新的offset和消息的处理结果，共用同一个事务资源，写到一个地方，要么一起成功，要么一起失败；如果发生Partition的重新分配，只需要从存储中读取最近一次成功处理的那批消息的最大的offfset，然后让Consumer，seek此offset即可。实际上Kafka Connect的很多实现，比如Kafka Connect HDFS就是这么搞的。</p>
<p>这里描述一个具体的例子，Kafka + 关系型数据库：</p>
<h1><span id="参考">参考</span></h1><p><a href="https://www.javaworld.com/article/2077963/distributed-transactions-in-spring--with-and-without-xa.html">Distributed transactions in Spring, with and without XA</a><br><a href="http://www.importnew.com/15812.html">如何实现XA式、非XA式Spring分布式事务</a></p>
]]></content>
      <categories>
        <category>Spring</category>
        <category>Spring Transaction</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>Transaction</tag>
        <tag>Distributed Transaction</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Retry Framework Stateful Retry</title>
    <url>/posts/10764762.html</url>
    <content><![CDATA[<p>Spring Retry中的重试，分为无状态的重试和有状态的重试；</p>
<h1><span id="简述">简述</span></h1><p>有状态重试通常是用在message-driven 的应用中，从消息中间件比如RabbitMQ等接收到的消息，如果应用处理失败，那么消息中间件服务器会再次投递，再次投递时，对于集成了Spring Retry的应用来说，再次处理之前处理失败的消息，就是一次重试；也就是说，Spring Retry能够识别出，当前正在处理的消息是否是之前处理失败过的消息；</p>
<span id="more"></span>
<p>如果是之前处理过的消息，Spring Retry就可以使用 back off policies 阻塞当前线程；Spring Retry同时追踪重试的次数，支持处理彻底失败后的recover，这也是使用有状态重试的理由；</p>
<p>有状态重试的另一个典型应用场景是跟Spring Transaction框架集成。在集成了Spring Transaction框架的MVC应用中，通过TransactionInterceptor，开启对Service层的事务管理；在这种情况下，Spring Retry会提供让每一次重试和重试次数耗尽之后的recover都在一个新的事务中执行。</p>
<h1><span id="举个栗子">举个栗子</span></h1><p><a href="https://github.com/58greenwhale/spring-retry-samples.git">spring-retry-samples</a></p>
<h1><span id="参考">参考</span></h1><p><a href="https://stackoverflow.com/questions/54559143/spring-retryable-with-stateful-hibernate-object">Spring @Retryable with stateful Hibernate Object</a><br><a href="https://stackoverflow.com/questions/38212471/springboot-retryable-not-retrying">Springboot @retryable not retrying</a></p>
]]></content>
      <categories>
        <category>Spring</category>
        <category>Spring Retry Framework</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>Spring Retry Framework</tag>
      </tags>
  </entry>
  <entry>
    <title>The Perfect Singleton</title>
    <url>/posts/3384101157.html</url>
    <content><![CDATA[<p>我不时遇到那些事实上不确定他们应该如何合适地实现单例模式的Java程序员。<br>我不考虑在线程的环境中合适的实现。但是使用你能在网络上找到的大多数常见的实现方式，你可以轻松地创建你想要的多种单例实现。</p>
<span id="more"></span>
<p>假设你有下面这种常见的单例的实现：</p>
<figure class="highlight java"><figcaption><span>NonSafeSingletonimplements</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">NonSafeSingletonimplements</span> Serializable &#123;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">NonSafeSingleton</span> <span class="variable">INSTANCE</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NonSafeSingleton</span>();</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">private</span> <span class="title function_">NonSafeSingleton</span><span class="params">()</span> &#123;&#125;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">static</span> NonSafeSingleton <span class="title function_">getInstance</span><span class="params">()</span> &#123;</span><br><span class="line">       <span class="keyword">return</span> INSTANCE;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>现在，注意到Serializable 这个单词。思考一会…..你是对的。如果通过RMI发送上边的代码，你将会得到第二个实例。它应该足够可以做一些内存中的序列化和反序列化操作。你刚刚违反了单例的规则。那不是很好。但是如何修复它？通常我会用两种方式：</p>
<h1><span id="困难的方式如果你用java14或者更老的版本">困难的方式（如果你用Java1.4或者更老的版本）</span></h1><p>你需要在你的单例类中实现一个readResolve方法。这通常用来重写序列化机制已经创建的内容。在这个方法里返回的是用来代替来自序列化的数据。这里仅需要返回你的实例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> Object <span class="title function_">readResolve</span><span class="params">()</span> <span class="keyword">throws</span> ObjectStreamException &#123;</span><br><span class="line">  <span class="keyword">return</span> INSTANCE;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1><span id="简单的方式如果你用java15或更新的版本">简单的方式（如果你用Java1.5或更新的版本）</span></h1><p>将你的单例类改成枚举类型，然后移除私有构造方法和getInstance方法。下面，真的很简单。然后你将免费得到下面这个：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">SafeSingleton</span> <span class="keyword">implements</span> <span class="title class_">Serializable</span> &#123;</span><br><span class="line">    INSTANCE;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>当你再实现单例模式时，记住这些。如果你大量的使用RMI,它可以使你的生活更加简单。</p>
<p><strong>Reference</strong>: <a href="https://jdevel.wordpress.com/2010/10/02/the-perfect-singleton/">The Perfect Singleton</a> fromour <a href="https://www.javacodegeeks.com/join-us/jcg">JCG partner</a> MarekPiechut at the <a href="https://jdevel.wordpress.com/">Development worldstories</a>.</p>
<h1><span id="related-articles">Related Articles</span></h1><p><a href="https://www.javacodegeeks.com/2011/03/dreaded-double-checked-locking-idiom-in.html">The dreaded double checked locking idiom in Java</a><br><a href="https://www.javacodegeeks.com/2011/07/java-secret-using-enum-to-build-state.html">Java Secret: Using an enum to build a State machine</a><br><a href="https://www.javacodegeeks.com/2010/12/dependency-injection-manual-way.html">Dependency Injection – The manual way</a><br><a href="https://www.javacodegeeks.com/2011/04/java-generics-quick-tutorial.html">Java Generics Quick Tutorial</a><br><a href="https://www.javacodegeeks.com/2011/05/how-jvm-handle-locks.html">How does JVM handle locks</a></p>
<h1><span id="译者注">译者注</span></h1><p>这篇文章在并发编程网发出来的时候，下面有几个比较重要的评论在这粘贴一下，也给大家一点启发：</p>
<h2><span id="评论1">评论1</span></h2><p>如果考虑线程的情况下，这种单例是我见过最好的了 initialization-on-demand holder idiom</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SingletonInitiOnDemand</span> &#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">private</span> <span class="title function_">SingletonInitiOnDemand</span><span class="params">()</span> &#123;</span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">SingletonHolder</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">SingletonInitiOnDemand</span> <span class="variable">INSTANCE</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SingletonInitiOnDemand</span>();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> SingletonInitiOnDemand <span class="title function_">getInstance</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> SingletonHolder.INSTANCE;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>回答：<br>厉害，查了一下，这种方式巧妙地利用内部类的机制实现了延时加载和线程安全。</p>
<h2><span id="评论2">评论2</span></h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">SafeSingleton</span> <span class="keyword">implements</span> <span class="title class_">Serializable</span> &#123;</span><br><span class="line">  INSTANCE;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>您能把这段代码的实现原理机制说一下吗？<br>代码里不需要new 一个INSTANCE 实例吗？比如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">NonSafeSingleton</span> <span class="variable">INSTANCE</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NonSafeSingleton</span>;</span><br></pre></td></tr></table></figure>

<p>回答：<br>这段代码的原理机制，就是enmu的原理机制。enmu对象是不需要new的。我下面写一个例子你就明白了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">SafeSingleton</span> <span class="keyword">implements</span> <span class="title class_">Serializable</span> &#123;</span><br><span class="line"></span><br><span class="line">  INSTANCE;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">println</span><span class="params">()</span> &#123;</span><br><span class="line">    System.out.println(“t”);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Testt</span> &#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">    <span class="type">SafeSingleton</span> <span class="variable">singleton</span> <span class="operator">=</span> SafeSingleton.INSTANCE;</span><br><span class="line">    singleton.println();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>大家也可以在并发编程网查看更多的评论 <a href="http://ifeve.com/perfect-singleton/">http://ifeve.com/perfect-singleton/</a></p>
]]></content>
      <categories>
        <category>Design Pattern</category>
      </categories>
      <tags>
        <tag>Design Pattern</tag>
      </tags>
  </entry>
  <entry>
    <title>Using the State pattern in a Domain Driven Design</title>
    <url>/posts/3262808649.html</url>
    <content><![CDATA[<p>领域驱动设计是软件开发的一种方式，问题复杂的地方通过将具体实现和一个不断改进的核心业务概念的模型连接解决。这个概念是Eric Evans提出的，<a href="http://www.domaindrivendesign.org/">domaindrivendesign</a>这个网站来促进领域驱动设计的使用。关于领域驱动设计的定义，<a href="http://dddcommunity.org/resources/ddd_terms/">dddcommunity</a>，这个网站有很多的描述，DDD是一种软件开发的方式：</p>
<span id="more"></span>
<ol>
<li>对于大多数的软件项目，主要的精力应该在领域和领域的逻辑。</li>
<li>复杂的领域设计应该基于一个模型。</li>
</ol>
<p>DDD促进了技术和领域专家之前的创造性的合作，迭代地接近问题的概念核心。注意，在没有领域专家的帮助时，一个技术专家可能不会完全理解领域的错综复杂，当然，没有技术专家的帮助，一个领域专家实际上也不能应用它的知识到项目中。</p>
<p>大多数情况下，一个领域模型对象封装一个内部的状态，本质上是一个系统中某个元素的历史，也就是，对象的操作是有状态的。在那种情况下，对象保持它的私有状态，这个状态最终将影响他的行为。状态设计模式可以干净地代表一个对象的状态，处理它的状态转换。简而言之，状态模式是针对依赖于状态做出行为的问题的解决方案。</p>
<p>很明显,DDD和状态设计模式息息相关。我对DDD是个新手，所以我将让我们最出色的JCG伙伴Tomasz Nurkiewicz，用一个例子来介绍使用状态设计模式的DDD。</p>
<p>注意：为了提高可读性，原始邮件被稍微重新编辑了下。</p>
<p>一些企业应用中的领域对象包含状态的概念。状态有两个主要的特性：</p>
<ol>
<li>领域对象的表现（如何响应业务方法）依赖于它的状态。</li>
<li>业务方法可能改变对象的状态，在一个特定的调用之后，对象可能表现出不同的行为。</li>
</ol>
<p>如果你不能想象任何领域对象的例子，想象在租赁公司的一个Car实体。这个Car，尽管是同一个对象，但是它有一个附加的状态标识，这对公司来说至关重要。这个状态标识可能有三个值：</p>
<ol>
<li>AVAILABLE</li>
<li>RENTED</li>
<li>MISSING</li>
</ol>
<p>很明显，当一个Car处于RENTED或者MISSING的时候，是不能被租出去的，rent()方法应该失效。但是当Car被还回来的时候，它的状态时AVALIABLE，对这个Car实体调用rent()方法应该与之前租借这个Car的用户无关，将车的状态改为RENTED。状态标识（可能是一个字符或者是数据库中的int类型）是对象状态的一个例子，因为它影响着业务方法，反之亦然，业务方法也可以改变状态。</p>
<p>现在，思考一个问题，你将怎么实现这个场景，我相信，这个场景你在工作中遇到过很多次了。你有很多依赖于当前的状态的业务方法和很多种的状态。如果你喜欢面向对象编程，你可能立即想到继承然后创建一个继承自Car的AvailableCar，RentedCar和MissingCar。这看上去很好，但是不切实际地，特别是当Car是一个持久化对象的时候。实际上，这种继承的方式不是一种好的设计：我们想要的不是改变整个对象，仅仅是对象的一条内部状态，也就是说，我们不会替换一个对象，仅仅是改变它。也许你想在每一个依赖于状态执行不同的任务的方法中用if-else-if-else这种层叠的方式。。。不要这么做，相信我，那是代码维护的地狱。</p>
<p>相反，我们将不使用继承和多态，但这是一个更聪明的方式：使用状态模式。举个例子，我选择了一个叫做Reservation的实体，这个实体有下面这些状态。</p>
<p>这个生命周期是非常简单的“当Reservation被创建，它是NEW状态。然后一些被授权的人可以接受这个Reservation，导致像座位被短暂地保留的事件发生，然后给人发送一个e-mail，让其为Reservation付费。再然后，用户执行转账，钱到账，打印票据，然后发送第二封邮件给客户。</p>
<p>你肯定已经意识到一些动作依据Reservation 当前的状态有不同的效果。例如，你可以在任意时间取消reservation，但是依赖于Reservation当前的状态，取消动作可能导致退款然后取消reservation，或者仅仅是发送给用户一个e-mail。一些动作不依赖于特定的状态（如果用户为一个已经取消的reservation付账会怎么样）或者应该被忽略。如果你在每个状态和每个业务方法中用了if-else结构，现在想象一下依据上边的状态机写出业务方法将有多难。</p>
<p>为了解决这个问题，我将不解释原始的GoF状态设计模式。而是介绍一些在使用了java ENUM的功能之后，我对这个设计模式的一些改变的地方。代替为状态抽象创建一个抽象的类或接口，然后为每一个状态写实现这种方式，我简单的创建一个包含了所有可用的状态的enum。</p>
<figure class="highlight java"><figcaption><span>ReservationStatus</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">ReservationStatus</span> &#123;</span><br><span class="line"> NEW,</span><br><span class="line"> ACCEPTED,</span><br><span class="line"> PAID,</span><br><span class="line"> CANCELLED;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后我为所有依赖于状态的业务方法创建了一个接口。把这个接口当做所有状态的抽象基类，但是我们将以稍微不同的方式使用它。</p>
<figure class="highlight java"><figcaption><span>ReservationStatusOperations</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">ReservationStatusOperations</span> &#123;</span><br><span class="line"> ReservationStatus <span class="title function_">accept</span><span class="params">(Reservationreservation)</span>;</span><br><span class="line"> ReservationStatus <span class="title function_">charge</span><span class="params">(Reservationreservation)</span>;</span><br><span class="line"> ReservationStatus <span class="title function_">cancel</span><span class="params">(Reservationreservation)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后，Reservation领域对象，恰巧同时也是一个JPA实体（省略getters&#x2F;setters）。</p>
<figure class="highlight java"><figcaption><span>Reservation</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Reservation</span> &#123;</span><br><span class="line"> <span class="keyword">private</span> intid;</span><br><span class="line"> privateString name;</span><br><span class="line"> privateCalendar date;</span><br><span class="line"> privateBigDecimal price;</span><br><span class="line"> <span class="type">privateReservationStatus</span> <span class="variable">status</span> <span class="operator">=</span> ReservationStatus.NEW;</span><br><span class="line"> </span><br><span class="line"> <span class="comment">//getters/setters</span></span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果Reservation是一个持久的领域对象，他的状态（ReservationStatus）很明显也应该被持久化。这个观察结果将使我们第一次体会到使用enum代替抽象类的巨大好处：JPA&#x2F;Hibernate可以很容易地使用enum的名字或者顺序的值（默认）序列化和持久化java enum到数据库中。在原始的GoF模式中，我们将直接把ReservationStatusOperations 对象放到领域对象中，然后状态改变时切换不同的实现。我建议使用enum然后仅改变enum的值。使用enum的另一个优势（不是以框架为中心的但是更重要的）是所有可能的状态在一个地方列出。你不必在你的源码中爬行来寻找所有的状态基类的实现。所有的东西都能在一个地方被看到，一个逗号分隔的列表。</p>
<p>OK，深呼吸。现在我解释一下所有的部分如何在一起工作，ReservationStatusOperations 中的业务操作为什么返回ReservationStatus。首先，你必须回忆一下， enum究竟是什么。他们不仅仅是像C&#x2F;C++那样的多个常量在一个命名空间下的集合。在JAVA中，enum是多个类的闭集，继承自一个公共的基类（例如ReservationStatus），最后继承自enum类。所以当使用enum的时候，我们可能就使用了多态和继承。</p>
<figure class="highlight java"><figcaption><span>ReservationStatus</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">ReservationStatus</span> <span class="keyword">implements</span> <span class="title class_">ReservationStatusOperations</span> &#123;</span><br><span class="line"></span><br><span class="line">  NEW &#123;</span><br><span class="line">   publicReservationStatus <span class="title function_">accept</span><span class="params">(Reservation reservation)</span> &#123;</span><br><span class="line">     <span class="comment">//..</span></span><br><span class="line">   &#125;</span><br><span class="line">   publicReservationStatus <span class="title function_">charge</span><span class="params">(Reservation reservation)</span> &#123;</span><br><span class="line">     <span class="comment">//..</span></span><br><span class="line">   &#125;</span><br><span class="line">   publicReservationStatus <span class="title function_">cancel</span><span class="params">(Reservation reservation)</span> &#123;</span><br><span class="line">     <span class="comment">//..</span></span><br><span class="line">   &#125;</span><br><span class="line">  &#125;,</span><br><span class="line"></span><br><span class="line">  ACCEPTED &#123;</span><br><span class="line">   publicReservationStatus <span class="title function_">accept</span><span class="params">(Reservation reservation)</span> &#123;</span><br><span class="line">    <span class="comment">//..</span></span><br><span class="line">   &#125;</span><br><span class="line">   publicReservationStatus <span class="title function_">charge</span><span class="params">(Reservation reservation)</span> &#123;</span><br><span class="line">    <span class="comment">//..</span></span><br><span class="line">   &#125;</span><br><span class="line">   publicReservationStatus <span class="title function_">cancel</span><span class="params">(Reservation reservation)</span> &#123;</span><br><span class="line">    <span class="comment">//..</span></span><br><span class="line">   &#125;</span><br><span class="line">  &#125;,</span><br><span class="line"></span><br><span class="line">  PAID &#123;<span class="comment">/*...*/</span>&#125;,</span><br><span class="line"></span><br><span class="line">  CANCELLED &#123;<span class="comment">/*...*/</span>&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>虽然以上边的方式写一个ReservationStatusOperations 类很容易，但是从长远来看，这是一个坏主意。不仅enum源代码会极其的长（所有要实现的方法的数量等于状态的数量乘以业务方法的数量），而且是一个坏的设计（所有状态的业务逻辑在一个类中）。一个enum也可以实现一个接口，这个奇特的语法可能与没有参加过SCJP exam 考试的人的直觉相反。我们将提供一个简单的中间层，因为计算机科学中的任何问题都可以被另一个中间层解决。</p>
<figure class="highlight java"><figcaption><span>ReservationStatus</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">ReservationStatus</span> implementsReservationStatusOperations &#123;</span><br><span class="line"> </span><br><span class="line">  NEW(newNewRso()),</span><br><span class="line">  ACCEPTED(newAcceptedRso()),</span><br><span class="line">  PAID(newPaidRso()),</span><br><span class="line">  CANCELLED(<span class="keyword">new</span> <span class="title class_">CancelledRso</span>());</span><br><span class="line"> </span><br><span class="line">  privatefinal ReservationStatusOperations operations;</span><br><span class="line"> </span><br><span class="line">  ReservationStatus(ReservationStatusOperationsoperations) &#123;</span><br><span class="line">    <span class="built_in">this</span>.operations = operations;</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  publicReservationStatus <span class="title function_">accept</span><span class="params">(Reservation reservation)</span> &#123;</span><br><span class="line">    returnoperations.accept(reservation);</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  publicReservationStatus <span class="title function_">charge</span><span class="params">(Reservation reservation)</span> &#123;</span><br><span class="line">    returnoperations.charge(reservation);</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  publicReservationStatus <span class="title function_">cancel</span><span class="params">(Reservation reservation)</span> &#123;</span><br><span class="line">    returnoperations.cancel(reservation);</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这是我们的ReservationStatus enum的最终的源代码（实现ReservationStatusOperations 不是必须的）。把事情变简单：每一个enum值都自己特定的ReservationStatusOperations 实现（简写为Rso）。ReservationStatusOperations 的实现作为构造函数的参数，然后赋给一个命名为operations的final类型的域。现在，不管enum中的业务方法什么时候被调用，调用将被委托给特定的ReservationStatusOperations 实现。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ReservationStatus.NEW.accept(reservation);       <span class="comment">// will call NewRso.accept()</span></span><br><span class="line">ReservationStatus.ACCEPTED.accept(reservation);  <span class="comment">// will call AcceptedRso.accept()</span></span><br></pre></td></tr></table></figure>

<p>最后一个要实现的部分是包含业务方法Reservation领域对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">accept</span><span class="params">()</span> &#123;</span><br><span class="line">  setStatus(status.accept(<span class="built_in">this</span>));</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">charge</span><span class="params">()</span> &#123;</span><br><span class="line">  setStatus(status.charge(<span class="built_in">this</span>));</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">cancel</span><span class="params">()</span> &#123;</span><br><span class="line">  setStatus(status.cancel(<span class="built_in">this</span>));</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setStatus</span><span class="params">(ReservationStatus status)</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (status!= <span class="literal">null</span> &amp;&amp; status != <span class="built_in">this</span>.status) &#123;</span><br><span class="line">    log.debug(<span class="string">&quot;Reservation#&quot;</span> + id + <span class="string">&quot;: changing status from&quot;</span> + <span class="built_in">this</span>.status+ <span class="string">&quot; to &quot;</span> + status);</span><br><span class="line">    <span class="built_in">this</span>.status = status;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里发生了什么？当你在一个Reservation领域对象实例上调用任何业务方法，ReservationStatus  enum的某个值的相应的方法就会被调用。依据当前的状态，一个不同的方法（不同ReservationStatusOperations 实现的）将会被调用。但是没有switch-case 和if-else结构，仅仅使用了多态。例如，如果当status域指向ReservationStatus.ACCEPTED，你调用了charge()方法，AcceptedRso.charge() 将会被调用，消费者将会被要求付款，付款之后，Reservation状态改变成PAID。</p>
<p>但是如果我们在同一个实例再次调用charge()会发生什么？status域现在指向ReservationStatus.PAID，所以PaidRso.charge() 将会被执行，这将会抛出一个业务错误（为一个已付款的Reservation付款是无效的）。没有条件判断的代码，我们实现了一个业务方法状态敏感的领域对象。</p>
<p>我还没有提到的一件事是如何从一个业务方法改变Reservation的状态。这是与原始的GoF模式第二个不同的地方。我从业务方法简单的返回一个新的状态，而不是传递一个StateContext 实例给每一个状态敏感的操作（像accept()或者charge()方法），这种方式经常被用来改变状态。如果给定的状态不是null而且与先前的状态不同（setStatus方法中实现），Reservation对象将转变为给定的状态。让我们看一下在AcceptedRso 对象中是如何工作的（Reservation对象在ReservationStatus.ACCEPTED 状态，它的方法将要被执行）。</p>
<figure class="highlight java"><figcaption><span>implementsReservationStatusOperations</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AcceptedRso</span> implementsReservationStatusOperations &#123;</span><br><span class="line"> </span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  publicReservationStatus <span class="title function_">accept</span><span class="params">(Reservation reservation)</span> &#123;</span><br><span class="line">   <span class="keyword">throw</span> newUnsupportedStatusTransitionException(<span class="string">&quot;accept&quot;</span>,ReservationStatus.ACCEPTED);</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  publicReservationStatus <span class="title function_">charge</span><span class="params">(Reservation reservation)</span> &#123;</span><br><span class="line">    <span class="comment">//chargeclient&#x27;s credit card</span></span><br><span class="line">    <span class="comment">//sende-mail</span></span><br><span class="line">    <span class="comment">//printticket</span></span><br><span class="line">    returnReservationStatus.PAID;</span><br><span class="line">  &#125;</span><br><span class="line"> </span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">   publicReservationStatus <span class="title function_">cancel</span><span class="params">(Reservation reservation)</span> &#123;</span><br><span class="line">     <span class="comment">//sendcancellation e-mail</span></span><br><span class="line">     returnReservationStatus.CANCELLED;</span><br><span class="line">   &#125;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在ACCEPTED 状态的Reservation 可以通过上边的类源码很容易地理解：当一个Reservation已经被accept时，试图accept第二次将会跑出一个错误，收费将使用客户的信用卡，打印给他一个票据然后发送一个email等等。同时，付费操作将返回一个PAID状态，这将使Reservation转换成这个状态。这意味着第二次调用charge将被不同的ReservationStatusOperations 实现处理（PaidRso），没有条件判断。</p>
<p>上边是关于状态模式的全部。如果你不相信这种设计模式，比较一下使用条件判断的代码这种传统的方式的工作量和容易出错的代码。</p>
<p>我没有展示所有的ReservationStatusOperations 的实现，但是如果你将在基于JavaEE的String或者EJB中引入这种方式，你可能已经看到一个弥天大谎。我描述了每一个业务方法应该发生的事情，但是没有提供具体的实现。我没有的原因是因为我遇到了一个大问题：一个Reservation实例通过手工（用new）或者持久化框架像hibernate创建。 It uses statically created enum which creates manuallyReservationStatusOperations implementations.没有办法去注入依赖，DAOs和service.对于这个类来说，它的整个生命周期都在spring或者ejb的容器管辖之外。实际上，有一个简单有效的解决方案，使用Spring和AspectJ。但是耐心点，我将在下一封邮件中详细的解释，如何给应用增加一点领域驱动的味道。</p>
<p>就这样。一个非常有趣的邮件，解释了如何在DDD方式中使用状态模式，作者是我们的JCG伙伴，Tomasz Nurkiewicz.。我非常期待这个教程的下一个部分。下一个部分是：Domain Driven Design with Spring and AspectJ.</p>
<p><strong>Related Articles:</strong><br><a href="https://www.javacodegeeks.com/2011/02/domain-driven-design-spring-aspectj.html">Domain Driven Design with Spring and AspectJ</a><br><a href="https://www.javacodegeeks.com/2011/01/spring-configuration-zero-xml.html">Spring configuration with zero XML</a><br><a href="https://www.javacodegeeks.com/2011/01/10-tips-proper-application-logging.html">10 Tips for Proper Application Logging</a><br><a href="https://www.javacodegeeks.com/2010/12/things-every-programmer-should-know.html">Things Every Programmer Should Know</a><br><a href="https://www.javacodegeeks.com/2010/12/dependency-injection-manual-way.html">Dependency Injection – The manual way</a></p>
]]></content>
      <categories>
        <category>DDD</category>
      </categories>
      <tags>
        <tag>DDD</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent Client Protocol 概述</title>
    <url>/posts/861056531.html</url>
    <content><![CDATA[<h1><span id="agent-client-protocol-概述">Agent Client Protocol 概述</span></h1><p><strong>原文</strong>: <a href="https://www.philschmid.de/acp-overview">https://www.philschmid.de/acp-overview</a><br><strong>翻译</strong>: LiterCrawler Skill</p>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/images/master/litercrawler-test/824ecf54/acp.png" alt="ACP 概述"></p>
<h2><span id="介绍">介绍</span></h2><p>Agent Client Protocol (ACP) 为 AI agent 与外部工具和服务的通信和交互提供了标准化方式。</p>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/images/master/litercrawler-test/824ecf54/tool.png" alt="工具集成"></p>
<h2><span id="主要特性">主要特性</span></h2><ul>
<li><strong>标准化接口</strong>: agent 与工具通信的通用协议</li>
<li><strong>灵活集成</strong>: 支持各种工具和服务</li>
<li><strong>安全优先</strong>: 内置权限和访问控制</li>
</ul>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/images/master/litercrawler-test/824ecf54/full.png" alt="完整工作流"></p>
<h2><span id="终端集成">终端集成</span></h2><p>ACP 为 AI agent 提供无缝的终端集成。</p>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/images/master/litercrawler-test/824ecf54/terminal.png" alt="终端"></p>
<h2><span id="权限系统">权限系统</span></h2><p>内置权限系统确保安全和受控的访问。</p>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/images/master/litercrawler-test/824ecf54/permission.png" alt="权限"></p>
<h2><span id="结论">结论</span></h2><p>ACP 简化了 agent 开发并促进了互操作性。</p>
<hr>
<p><strong>原文链接</strong>: <a href="https://www.philschmid.de/acp-overview">https://www.philschmid.de/acp-overview</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Agent</tag>
        <tag>Protocol</tag>
        <tag>ACP</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent 手动测试</title>
    <url>/posts/2462961482.html</url>
    <content><![CDATA[<h1><span id="agent-手动测试">Agent 手动测试</span></h1><p>coding agent 的决定性特征是它能够<strong>执行</strong>它所编写的代码。这正是 coding agent 比那些只生成代码却无法验证的 LLM 更有用的原因。</p>
<p><strong>永远不要假设 LLM 生成的代码能工作，直到代码被执行过。</strong></p>
<p>Coding agent 有能力确认它们生成的代码是否按预期工作，或者继续迭代直到代码能工作。</p>
<p>让 agent<strong>编写单元测试</strong>，特别是使用测试优先的 TDD，是确保它们测试了所编写代码的强大方法。</p>
<p>但这不是唯一有效的方法。</p>
<p>即使代码通过了测试，也不意味着它按预期工作。任何做过自动化测试的人都见过测试全部通过但代码本身在某些明显方面失败的情况——它可能在启动时崩溃服务器，无法显示关键的 UI 元素，或者遗漏了测试未覆盖的某些细节。</p>
<p>自动化测试不能替代<strong>手动测试</strong>。我喜欢在将功能发布到发布版本之前亲眼看到它工作。</p>
<p>我发现让 agent 手动测试代码也很有价值，经常能发现自动化测试未发现的问题。</p>
<h2><span id="agent-手动测试的机制">Agent 手动测试的机制</span></h2><p>Agent 如何”手动”测试代码取决于代码是什么。</p>
<h3><span id="python-库">Python 库</span></h3><p>对于 Python 库，一个有用的模式是 <code>python -c &quot;... code ...&quot;</code>。你可以将 Python 代码字符串（或多行字符串）直接传递给 Python 解释器，包括导入其他模块的代码。</p>
<p>Coding agent 都熟悉这个技巧，有时会在没有提示的情况下使用它。但提醒它们使用 <code>python -c</code> 进行测试通常很有效：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -c <span class="string">&quot;from mylib import func; print(func())&quot;</span></span><br></pre></td></tr></table></figure>

<h3><span id="其他语言">其他语言</span></h3><p>其他语言可能有类似的机制，如果没有，agent 快速写出演示文件然后编译运行也很快。我有时会鼓励它使用 <code>/tmp</code> 来避免这些文件后来被意外提交到仓库。</p>
<h3><span id="web-api">Web API</span></h3><p>我的许多项目涉及构建带有 JSON API 的 Web 应用程序。对于这些，我告诉 agent 使用 <code>curl</code> 来测试它们：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -X POST http://localhost:8000/api/endpoint -d <span class="string">&#x27;&#123;&quot;key&quot;: &quot;value&quot;&#125;&#x27;</span></span><br></pre></td></tr></table></figure>

<p>告诉 agent”探索”通常会导致它尝试新 API 的不同方面，可以快速覆盖大量领域。</p>
<p>如果 agent 通过手动测试发现不工作的东西，我喜欢让它们用 red&#x2F;green TDD 修复。这确保新情况最终被永久自动化测试覆盖。</p>
<h2><span id="使用浏览器自动化进行-web-ui-测试">使用浏览器自动化进行 Web UI 测试</span></h2><p>如果项目涉及交互式 Web UI，拥有手动测试程序就变得更有价值。</p>
<p>历史上这些很难从代码中测试，但过去十年在自动化真实浏览器方面取得了显著改进。对应用程序运行真实的 Chrome、Firefox 或 Safari 浏览器可以在真实环境中发现各种有趣的问题。</p>
<p><strong>Coding agent 非常擅长使用这些工具。</strong></p>
<h3><span id="playwright">Playwright</span></h3><p>今天最强大的是 <strong>Playwright</strong>，这是微软开发的开源库。Playwright 提供功能齐全的 API，支持多种流行编程语言，可以自动化任何流行的浏览器引擎。</p>
<p>简单地告诉你的 agent”用 Playwright 测试”可能就足够了。然后 agent 可以选择最合适的语言绑定，或使用 Playwright 的 <code>playwright-cli</code> 工具。</p>
<h3><span id="agent-browser">agent-browser</span></h3><p>Coding agent 与专用的 CLI 配合得很好。Vercel 的 <strong>agent-browser</strong> 是围绕 Playwright 构建的综合 CLI 包装器，专门为 coding agent 设计使用。</p>
<h3><span id="rodney">Rodney</span></h3><p>我自己的项目 <strong>Rodney</strong> 也有类似目的，虽然它使用 Chrome DevTools Protocol 直接控制 Chrome 实例。</p>
<p>这是我用来用 Rodney 测试的示例提示：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uvx rodney --<span class="built_in">help</span></span><br><span class="line"><span class="comment"># 然后使用 rodney screenshot 等命令</span></span><br></pre></td></tr></table></figure>

<p>这个提示中有三个技巧：</p>
<ol>
<li><p><strong>使用 <code>uvx rodney --help</code></strong> - 让 agent 通过 <code>uvx</code> 包管理工具运行 <code>rodney --help</code>，这会在第一次调用时自动安装 Rodney。</p>
</li>
<li><p><strong><code>rodney --help</code> 命令</strong> - 专门为 agent 设计，让它们了解和使用该工具所需的一切。</p>
</li>
<li><p><strong>“查看截图”</strong> - 提示 agent 使用 <code>rodney screenshot</code> 命令，并提醒它可以使用自己的视觉能力来评估页面的视觉外观。</p>
</li>
</ol>
<p>这是一个简短提示中包含的大量手动测试！</p>
<h3><span id="rodney-的功能">Rodney 的功能</span></h3><p>Rodney 和类似工具提供广泛的功能：</p>
<ul>
<li>在加载的网站上运行 JavaScript</li>
<li>滚动、点击、输入</li>
<li>甚至读取页面的辅助功能树</li>
</ul>
<p>与其他形式的手动测试一样，通过浏览器自动化发现并修复的问题也可以添加到永久自动化测试中。</p>
<h3><span id="浏览器测试的稳定性问题">浏览器测试的稳定性问题</span></h3><p>许多开发人员过去避免过多的自动化浏览器测试，因为它们以不稳定著称——对页面 HTML 的最小调整都可能导致令人沮丧的测试失败浪潮。</p>
<p>让 coding agent 随时间维护这些测试大大减少了在 Web 界面设计变化时保持它们最新的摩擦。</p>
<h2><span id="让-agent-用-showboat-做笔记">让 agent 用 Showboat 做笔记</span></h2><p>让 agent 手动测试代码可以发现额外的问题，但也可以用于创建有助于记录代码和演示如何测试的工件。</p>
<p>我对让 agent<strong>展示它们的工作</strong>这个挑战很着迷。能够看到演示或记录的实验是确认 agent 全面解决了所给挑战的非常有用的方式。</p>
<p>我构建了 <strong>Showboat</strong> 来促进构建捕捉 agent 手动测试流程的文档。</p>
<p>这是我经常使用的提示：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">showboat --<span class="built_in">help</span></span><br></pre></td></tr></table></figure>

<p>与上面的 Rodney 一样，<code>showboat --help</code> 命令教会 agent 什么是 Showboat 以及如何使用它。</p>
<h3><span id="showboat-的三个关键命令">Showboat 的三个关键命令</span></h3><ol>
<li><strong><code>note</code></strong> - 将 Markdown 笔记附加到 Showboat 文档</li>
<li><strong><code>exec</code></strong> - 记录命令，然后运行该命令并记录输出</li>
<li><strong><code>image</code></strong> - 向文档添加图片 - 对使用 Rodney 拍摄的 Web 应用程序截图很有用</li>
</ol>
<p><strong><code>exec</code> 命令是最重要的</strong>，因为它捕获命令以及结果输出。这向你展示了 agent 做了什么以及结果是什么，旨在阻止 agent 作弊并将其<strong>希望</strong>发生的事情写入文档。</p>
<p>我发现 Showboat 模式非常适合记录我的 agent 会话期间完成的工作。我希望看到类似的模式在更广泛的工具集中被采用。</p>
<hr>
<p><strong>原文</strong>: <a href="https://simonwillison.net/guides/agentic-engineering-patterns/agentic-manual-testing/">https://simonwillison.net/guides/agentic-engineering-patterns/agentic-manual-testing/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI - Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>Agentic Engineering Patterns 概述</title>
    <url>/posts/2014430881.html</url>
    <content><![CDATA[<h1><span id="关于-agentic-engineering-patterns">关于 Agentic Engineering Patterns</span></h1><p>2026 年 2 月 23 日</p>
<p>我开始了一个新项目，收集并记录 <strong>Agentic Engineering Patterns</strong> —— 编码实践和模式，帮助在这个新的编码 agent 开发时代获得最佳结果。</p>
<p>我使用 <strong>Agentic Engineering</strong> 来指代使用编码 agent 构建软件——像 Claude Code 和 OpenAI Codex 这样的工具，它们的定义特征是既可以生成也可以 <em>执行</em> 代码——允许它们测试代码并独立于人类主管的逐步指导进行迭代。</p>
<p>我认为 <strong>vibe coding</strong> 使用其 <a href="https://simonwillison.net/2025/Mar/19/vibe-coding/">原始定义</a>，即完全不注意代码的编码，今天通常与非程序员使用 LLM 编写代码相关联。</p>
<p>Agentic Engineering 代表了另一个极端：专业软件工程师使用编码 agent 通过放大现有专业知识来改进和加速他们的工作。</p>
<p>关于这个新学科有太多需要学习和探索！我已经发布了很多 <a href="https://simonwillison.net/tags/ai-assisted-programming/">在我的 ai-assisted-programming 标签下</a>（345 篇并持续增加），但这相对非结构化。我的新目标是制作一些能帮助回答”我如何从中获得好结果”这个问题的内容。</p>
<p>我将在这里的博客上开发和扩展这个项目，作为一系列章节形状的模式，松散地受到 <a href="https://en.wikipedia.org/wiki/Design_Patterns">Design Patterns: Elements of Reusable Object-Oriented Software</a> 在 1994 年推广的格式启发。</p>
<p>我今天发布了前两个章节：</p>
<ul>
<li><strong><a href="https://simonwillison.net/guides/agentic-engineering-patterns/code-is-cheap/">Writing code is cheap now</a></strong> 讨论了 agentic engineering 的核心挑战：生成初始工作代码的成本已降至几乎为零，这如何影响我们对如何工作的现有直觉，无论是个人还是团队？</li>
<li><strong><a href="https://simonwillison.net/guides/agentic-engineering-patterns/red-green-tdd/">Red&#x2F;green TDD</a></strong> 描述了测试优先开发如何帮助 agent 用最少的额外提示编写更简洁可靠的代码。</li>
</ul>
<p>我希望以每周 1-2 章的速度添加更多章节。我真的不知道何时停止，有很多要涵盖！</p>
<h4><span id="由我撰写不是由-llm">由我撰写，不是由 LLM</span></h4><p>我有一个强烈的个人政策，不在自己的名义下发布 AI 生成的写作。这个政策也适用于 Agentic Engineering Patterns。我将使用 LLM 进行校对和充实示例代码以及各种其他辅助任务，但你在这里读到的文字将是我自己的。</p>
<h4><span id="章节和指南">章节和指南</span></h4><p>Agentic Engineering Patterns 不完全是 <em>一本书</em>，但它有点书的形式。我将在我的网站上使用我称之为 <em>guide</em> 的新内容形式发布它。guide 是章节的集合，其中每个章节实际上是一篇博客文章，日期不太突出，旨在随时间更新，而不是在首次发布时冻结。</p>
<p>Guides 和 chapters 是我对在博客上发布”evergreen”内容的挑战的回答。我一直试图找到一种方法来做这件事。这感觉像是一个可能坚持的格式。</p>
<p>如果你对实现感兴趣，可以在 <a href="https://github.com/simonw/simonwillisonblog/blob/b9cd41a0ac4a232b2a6c90ca3fff9ae465263b02/blog/models.py#L262-L280">Guide</a>、<a href="https://github.com/simonw/simonwillisonblog/blob/b9cd41a0ac4a232b2a6c90ca3fff9ae465263b02/blog/models.py#L349-L405">Chapter</a> 和 <a href="https://github.com/simonw/simonwillisonblog/blob/b9cd41a0ac4a232b2a6c90ca3fff9ae465263b02/blog/models.py#L408-L423">ChapterChange</a> 模型以及 <a href="https://github.com/simonw/simonwillisonblog/blob/b9cd41a0ac4a232b2a6c90ca3fff9ae465263b02/blog/views.py#L775-L923">相关 Django 视图</a> 中找到代码，几乎所有这些都是由 Claude Opus 4.6 在通过我的 iPhone 访问的 Claude Code for web 中编写的。</p>
<hr>
<p><strong>原文链接</strong>: <a href="https://simonwillison.net/2026/Feb/23/agentic-engineering-patterns/">https://simonwillison.net/2026/Feb/23/agentic-engineering-patterns/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Agent</tag>
        <tag>Engineering</tag>
        <tag>Patterns</tag>
      </tags>
  </entry>
  <entry>
    <title>反模式：需要避免的事情 - Agentic Engineering Patterns</title>
    <url>/posts/2896843582.html</url>
    <content><![CDATA[<h1><span id="反模式需要避免的事情">反模式：需要避免的事情</span></h1><p>在我们奇怪的 agentic engineering 新世界中，有一些行为是反模式。</p>
<h2><span id="将未审查的代码强加给协作者">将未审查的代码强加给协作者</span></h2><p>这个反模式很常见且非常令人沮丧。</p>
<p><strong>不要提交你自己没有审查过的代码的 pull requests</strong>。</p>
<p>如果你打开一个 PR，里面有 agent 为你生成的数百（或数千）行代码，而你自己没有做工作来确保代码是功能性的，你就是将实际工作委托给其他人。</p>
<p>他们可以自己提示 agent。你甚至提供了什么价值？</p>
<p>如果你将代码提交审查，你需要确信它已经准备好让其他人花时间在上面。初始审查是你的责任，不应该是你外包给别人的东西。</p>
<p>一个好的 agentic engineering pull request 具有以下特征：</p>
<ul>
<li>代码有效，你有信心它有效。<a href="https://simonwillison.net/2025/Dec/18/code-proven-to-work/">你的工作是交付有效的代码</a>。</li>
<li>更改足够小，可以有效审查，而不会给审查者施加太多额外的认知负荷。几个小 PR 胜过一个大的，使用编码 agent 为你做 Git 操作，将代码分成单独的提交很容易。</li>
<li>PR 包括额外的上下文来帮助解释更改。更改服务的更高级目标是什么？链接到相关的问题或规范在这里很有用。</li>
<li>Agent 编写令人信服的 pull request 描述。你也需要审查这些！期望别人阅读你自己没有阅读和验证的文本是不礼貌的。</li>
</ul>
<p>鉴于将未审查的代码倾倒给其他人是多么容易，我建议包括某种形式的证据，表明你自己投入了额外的工作。关于你如何手动测试它的笔记，对特定实现选择的评论，甚至是功能工作的截图和视频，对于证明审查者的时间不会浪费在挖掘细节上很有帮助。</p>
<hr>
<p><strong>原文链接</strong>: <a href="https://simonwillison.net/guides/agentic-engineering-patterns/anti-patterns/">https://simonwillison.net/guides/agentic-engineering-patterns/anti-patterns/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Agentic Engineering</tag>
        <tag>Anti-Patterns</tag>
        <tag>Programming</tag>
      </tags>
  </entry>
  <entry>
    <title>GIF 优化工具</title>
    <url>/posts/2682272491.html</url>
    <content><![CDATA[<p>我喜欢在我的在线写作中包含动画 GIF 演示，通常使用 <a href="https://www.cockos.com/licecap/">LICEcap</a> 录制。在 <a href="https://simonwillison.net/guides/agentic-engineering-patterns/interactive-explanations/">交互式解释</a> 章节中就有一个例子。</p>
<p>这些 GIF 文件可能相当大。我尝试过几种优化工具来减小 GIF 文件大小，我最喜欢的是 Eddie Kohler 开发的 <a href="https://github.com/kohler/gifsicle">Gifsicle</a>。它通过识别帧中未变化的区域并仅存储差异来压缩 GIF，还可以选择减少 GIF 颜色调色板或应用可见的有损压缩以获得更大的尺寸缩减。</p>
<p>Gifsicle 是用 C 编写的，默认界面是命令行工具。我想要一个 Web 界面，这样我就可以在浏览器中访问它，并直观地预览和比较不同的设置。</p>
<p>我用以下提示词对我的 <a href="https://github.com/simonw/tools">simonw&#x2F;tools</a> 仓库使用了 Claude Code for web（通过 Claude iPhone 应用从 iPhone 操作）：</p>
<p>以下是 <a href="https://tools.simonwillison.net/gif-optimizer">它构建的成果</a>，以及我用该工具优化的动画 GIF 演示：</p>
<p><img src="https://static.simonwillison.net/static/2026/demo2-32-colors-lossy.gif" alt="动画演示。我拖放一个 GIF 文件，工具更新页面显示一系列不同设置下的优化版本。我最终选择其中一个的&quot;Tweak settings&quot;，滚动到底部，调整一些滑块并下载结果。"></p>
<p>让我们逐步分析这个提示词。</p>
<blockquote>
<p><code>gif-optimizer.html</code></p>
</blockquote>
<p>第一行只是告诉它我想要创建的文件名。这里只需要文件名就够了——我知道当 Claude 在仓库中运行”ls”时，它会理解每个文件都是一个不同的工具。</p>
<p>我的 <a href="https://github.com/simonw/tools">simonw&#x2F;tools</a> 仓库目前缺少 <code>CLAUDE.md</code> 或 <code>AGENTS.md</code> 文件。我发现代理只需扫描现有的文件树并查看现有文件中的相关代码，就能掌握仓库的大致情况。</p>
<blockquote>
<p><code>Compile gifsicle to WASM, then build a web page that lets you open or drag-drop an animated GIF onto it and it then shows you that GIF compressed using gifsicle with a number of different settings, each preview with the size and a download button</code></p>
</blockquote>
<p>我在这里对 Claude 的现有知识做了一些假设，这些都得到了回报。</p>
<p>Gifsicle 现在已经快 30 年了，是一款广泛使用的软件——我有信心只要提到它的名字，Claude 就能找到代码。</p>
<p>“<code>Compile gifsicle to WASM</code>“ 这句话在这里做了<strong>很多</strong>工作。</p>
<p>WASM 是 <a href="https://webassembly.org/">WebAssembly</a> 的缩写，这项技术让浏览器能够在沙盒中安全地运行编译后的代码。</p>
<p>像 Gifsicle 这样的项目编译到 WASM 并不是一项简单的操作，涉及复杂的工具链，通常需要使用 <a href="https://emscripten.org/">Emscripten</a> 项目。这通常需要大量的试错才能让一切正常工作。</p>
<p>编码代理非常擅长试错！它们通常能够强行找到解决方案，而我可能在第五次遇到难以理解的编译器错误后就放弃了。</p>
<p>我之前见过 Claude Code 多次解决 WASM 构建问题，所以我很有信心这会成功。</p>
<p>“<code>then build a web page that lets you open or drag-drop an animated GIF onto it</code>“ 描述了我在我许多其他工具中使用过的模式。</p>
<p>HTML 文件上传对于选择文件来说没问题，但更好的 UI（尤其是在桌面上）是允许用户将文件拖放到页面上一个显眼的拖放区域。</p>
<p>设置这个需要一些 JavaScript 来处理事件，还需要一些 CSS 来设置拖放区域。这并不复杂，但工作量足以让我平时可能不会自己添加它。有了提示词，这几乎是免费的。</p>
<p>以下是最终的 UI——这是受到 Claude 查看我现有的 <a href="https://tools.simonwillison.net/image-resize-quality">image-resize-quality</a> 工具的影响：</p>
<p><img src="https://static.simonwillison.net/static/2026/gif-optimizer.jpg" alt="Web 应用程序截图，标题为&quot;GIF Optimizer&quot;，副标题为&quot;Powered by gifsicle compiled to WebAssembly — all processing happens in your browser&quot;。一个大的虚线边框拖放区域显示&quot;Drop an animated GIF here or click to select&quot;。下面是一个文本输入框，占位符为&quot;Or paste a GIF URL...&quot;，以及一个蓝色的&quot;Load URL&quot;按钮。页脚文字显示&quot;Built with gifsicle by Eddie Kohler, compiled to WebAssembly. gifsicle is released under the GNU General Public License, version 2.&quot;"></p>
<p>我没有要求 GIF URL 输入功能，我不太喜欢它，因为它只适用于提供开放 CORS 头的 GIF URL。我可能会在未来的更新中移除它。</p>
<p>“<code>then shows you that GIF compressed using gifsicle with a number of different settings, each preview with the size and a download button</code>“ 描述了应用程序的关键功能。</p>
<p>我没有费心定义我想要的设置集合——根据我的经验，Claude 有足够的品味为我选择这些，如果它的第一次猜测不奏效，我们总是可以更改。</p>
<p>显示大小很重要，因为这一切都是为了优化大小。</p>
<p>根据过去的经验，我知道要求一个”download button”会得到一个带有正确 HTML 和 JavaScript 机制的按钮，点击它会提供文件保存对话框，这比需要右键点击另存为要方便得多。</p>
<blockquote>
<p><code>Also include controls for the gifsicle options for manual use - each preview has a &quot;tweak these settings&quot; link which sets those manual settings to the ones used for that preview so the user can customize them further</code></p>
</blockquote>
<p>这个提示词相当笨拙——毕竟我是在手机上输入的——但它充分表达了我的意图，让 Claude 构建了我想要的东西。</p>
<p>以下是最终工具中的样子，这张截图显示的是移动版本。每个图片都有一个”Tweak these settings”按钮，点击后会更新这组手动设置和滑块：</p>
<p><img src="https://static.simonwillison.net/static/2026/gif-optimizer-tweak.jpg" alt="GIF Optimizer 结果和设置面板截图。顶部结果显示&quot;110.4 KB (original: 274.0 KB) — 59.7% smaller&quot;，绿色显示，带有蓝色&quot;Download&quot;按钮和&quot;Tweak these settings&quot;按钮。下面是&quot;Manual Settings&quot;卡片，包含：&quot;Optimization level&quot;下拉菜单设置为&quot;-O3 (aggressive)&quot;，&quot;Lossy (0 &#x3D; off, higher &#x3D; more loss)&quot;滑块设置为 0，&quot;Colors (0 &#x3D; unchanged)&quot;滑块设置为 0，&quot;Color reduction method&quot;下拉菜单设置为&quot;Default&quot;，&quot;Scale (%)&quot;滑块设置为 100%，&quot;Dither&quot;下拉菜单设置为&quot;Default&quot;，以及一个蓝色的&quot;Optimize with these settings&quot;按钮。"></p>
<blockquote>
<p><code>Run &quot;uvx rodney --help&quot; and use that tool to tray your work - use this GIF for testing https://static.simonwillison.net/static/2026/animated-word-cloud-demo.gif</code></p>
</blockquote>
<p>如果你确保编码代理在工作时能够测试它们的代码，它们的工作效果会<strong>好得多</strong>。</p>
<p>测试 Web 界面有很多不同的方法——<a href="https://playwright.dev/">Playwright</a>、<a href="https://www.selenium.dev/">Selenium</a> 和 <a href="https://agent-browser.dev/">agent-browser</a> 是三个不错的选择。</p>
<p><a href="https://github.com/simonw/rodney">Rodney</a> 是我自己构建的浏览器自动化工具，安装快速，其 <code>--help</code> 输出旨在教会代理使用该工具所需的一切知识。</p>
<p>这效果很好——在 <a href="https://claude.ai/code/session_01C8JpE3yQpwHfBCFni4ZUc4">会话记录</a> 中你可以看到 Claude 使用 Rodney 并修复了它发现的一些小错误，例如：</p>
<blockquote>
<p>CSS <code>display: none</code> 优先于内联样式重置。我需要显式设置 <code>display: &#39;block&#39;</code>。</p>
</blockquote>
<h2><span id="后续提示词">后续提示词 </span></h2><p>当我使用 Claude Code 时，我通常会关注它在做什么，这样我就可以在它仍在运行时重新定向它。我也经常在它工作时产生新的想法，然后将这些想法注入队列。</p>
<blockquote>
<p><code>Include the build script and diff against original gifsicle code in the commit in an appropriate subdirectory</code></p>
<p><code>The build script should clone the gifsicle repo to /tmp and switch to a known commit before applying the diff - so no copy of gifsicle in the commit but all the scripts needed to build the wqsm</code></p>
</blockquote>
<p>当我注意到它在花费<strong>大量</strong>精力研究如何让 Gifsicle 与 WebAssembly 一起工作时，我添加了这个，包括修补原始源代码。这是它添加到仓库的 <a href="https://github.com/simonw/tools/blob/main/lib/gifsicle/gifsicle-wasm.patch">补丁</a> 和 <a href="https://github.com/simonw/tools/blob/main/lib/gifsicle/build.sh">构建脚本</a>。</p>
<p>我知道那个仓库中已经有一个支持文件存放位置的模式，但我记不起那个模式是什么了。说”in an appropriate subdirectory”足以让 Claude 找出放在哪里——它找到并使用了现有的 <a href="https://github.com/simonw/tools/tree/main/lib">lib&#x2F; 目录</a>。</p>
<blockquote>
<p><code>You should include the wasm bundle</code></p>
</blockquote>
<p>这可能不是必需的，但我想确保编译后的 WASM 文件（结果是 <a href="https://github.com/simonw/tools/blob/main/lib/gifsicle/gifsicle.wasm">233KB</a>）被提交到仓库。我通过 GitHub Pages 在 <a href="https://tools.simonwillison.net/">tools.simonwillison.net</a> 提供 <code>simonw/tools</code> 服务，我希望它无需本地构建就能工作。</p>
<blockquote>
<p><code>Make sure the HTML page credits gifsicle and links to the repo</code></p>
</blockquote>
<p>这只是礼貌而已！我经常围绕其他人的开源项目构建 WebAssembly 包装器，我喜欢确保他们在最终页面中得到感谢。</p>
<p>Claude 在工具的页脚添加了：</p>
<blockquote>
<p>Built with <a href="https://github.com/kohler/gifsicle">gifsicle</a> by Eddie Kohler, compiled to WebAssembly. gifsicle is released under the GNU General Public License, version 2.</p>
</blockquote>
<hr>
<p><strong>原文链接</strong>: <a href="https://simonwillison.net/guides/agentic-engineering-patterns/gif-optimization/">https://simonwillison.net/guides/agentic-engineering-patterns/gif-optimization/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>Agentic Engineering</tag>
        <tag>GIF</tag>
        <tag>WebAssembly</tag>
        <tag>优化工具</tag>
      </tags>
  </entry>
  <entry>
    <title>2026 年 Agent Harness 的重要性</title>
    <url>/posts/3114607926.html</url>
    <content><![CDATA[<p>我们正处于 AI 的转折点。多年来，我们只关注模型本身。我们询问模型有多智能&#x2F;有多好。我们查看排行榜和基准测试，看模型 A 是否胜过模型 B。</p>
<p>静态排行榜上顶级模型之间的差距正在缩小。但这可能是一种错觉。任务越长、越复杂，模型之间的差距就越明显。关键在于持久性：模型在执行数百次工具调用的过程中，能多好地遵循指令。排行榜上 1% 的差距无法检测到模型在五十步之后是否偏离轨道的可靠性。</p>
<p>我们需要一种新的方式来展示能力、性能和改进。我们需要能够证明模型可以可靠地执行多日工作流的系统。Agent Harness 就是答案之一。</p>
<h2><span id="什么是-agent-harness">什么是 Agent Harness？</span></h2><p>Agent Harness 是围绕 AI 模型构建的基础设施，用于管理长期运行的任务。它不是代理本身，而是管理软件系统，用于管理代理的运行方式，确保其保持可靠、高效和可引导。</p>
<p>它的运行层级高于代理框架。框架提供工具的构建模块或实现代理循环，而 Harness 提供提示预设、对工具调用的意见处理、生命周期钩子或开箱即用的功能，如规划、文件系统访问或子代理管理。它不仅仅是一个框架，它是开箱即用的完整解决方案。</p>
<p><img src="/images/agent-harness-diagram.jpeg" alt="Agent Harness Diagram"></p>
<p>我们可以通过与计算机的比较来形象化理解：</p>
<ul>
<li><strong>模型是 CPU：</strong> 它提供原始处理能力。</li>
<li><strong>上下文窗口是 RAM：</strong> 它是有限的、易失性的工作内存。</li>
<li><strong>Agent Harness 是操作系统：</strong> 它管理上下文，处理”启动”序列（提示、钩子），并提供标准驱动程序（工具处理）。</li>
<li><strong>代理是应用程序：</strong> 它是在操作系统之上运行的特定用户逻辑。</li>
</ul>
<p>Agent Harness 实现了”<a href="https://www.philschmid.de/context-engineering">上下文工程</a>“策略，如通过压缩减少上下文、将状态卸载到存储、或将任务隔离到子代理中。对于开发者来说，这意味着你可以跳过构建操作系统，专注于应用程序，定义代理的独特逻辑。</p>
<p>目前，通用 Harness 还很少见。<strong>Claude Code</strong> 是这一新兴类别的典型例子，试图通过 Claude Agent SDK 或 LangChain DeepAgents 实现标准化。然而，有人可能会认为，<strong>所有编码 CLI</strong> 在某种程度上都是为特定垂直领域设计的专用 Agent Harness。</p>
<h2><span id="基准测试问题与对-agent-harness-的需求">基准测试问题与对 Agent Harness 的需求</span></h2><p>过去，基准测试主要针对单轮模型输出。去年，我们开始看到一种趋势，即评估系统而非原始模型，其中模型是一个可以使用工具或与环境交互的组件，例如 AIMO、SWE-Bench。</p>
<p>这些新的基准测试难以衡量 <a href="https://www.philschmid.de/agents-pass-at-k-pass-power-k">可靠性</a>。它们很少测试模型在第 50 次或第 100 次工具调用&#x2F;回合后的行为。这才是真正的困难所在。模型可能足够聪明，在一两次尝试中解决难题，但在运行一小时后无法遵循初始指令或正确推理中间步骤。标准基准测试难以捕捉长期工作流所需的持久性。</p>
<p>随着基准测试变得越来越复杂，我们需要弥合基准测试声明与用户体验之间的差距。Agent Harness 在三个关键原因上至关重要：</p>
<ul>
<li><strong>验证实证进展：</strong> 基准测试与用户需求不一致。随着新模型频繁发布，Harness 允许用户轻松测试和比较最新模型在其用例和约束下的表现。</li>
<li><strong>增强用户体验：</strong> 没有 Harness，用户体验可能落后于模型的潜力。发布 Harness 允许开发者使用经过验证的工具和最佳实践构建代理。这确保用户与相同的系统结构交互。</li>
<li><strong>通过实证反馈进行爬山优化：</strong> 共享、稳定的环境（Harness）创建了一个反馈循环，研究人员可以根据实际用户采用情况迭代和改进（”爬山”）基准测试。</li>
</ul>
<p>改进系统的能力与验证其输出的难易程度成正比。<a href="https://www.jasonwei.net/blog/asymmetry-of-verification-and-verifiers-law">[Ref]</a> Harness 将模糊的多步骤代理工作流转化为我们可记录和评分的结构化数据，使我们能够有效地进行爬山优化。</p>
<h2><span id="构建代理的痛苦教训">构建代理的”痛苦教训”</span></h2><p>Rich Sutton 写了一篇名为 <a href="http://www.incompleteideas.net/IncIdeas/BitterLesson.html">痛苦教训</a> 的文章。他认为，使用计算的通用方法每次都胜过手工编码的人类知识。我们现在正看到这一教训在代理开发中上演。</p>
<p>为了在痛苦教训中生存，我们的基础设施（Harness）必须轻量级。每个新模型的发布都有不同的、最优的代理结构方式。2024 年需要复杂手工编码流水线的功能，现在在 2026 年只需单个上下文窗口提示即可处理。</p>
<p>开发者必须构建能够让他们删除昨天编写的”智能”逻辑的 Harness。如果你过度设计控制流，下一个模型更新将破坏你的系统。</p>
<h2><span id="接下来是什么">接下来是什么？</span></h2><p>我们正朝着训练和推理环境的融合方向发展。我们看到新的瓶颈是上下文持久性。Harness 将成为解决”模型漂移”的主要工具。实验室将使用 Harness 来精确检测模型在第 100 步之后何时停止遵循指令或正确推理。这些数据将直接反馈到训练中，以创建在长期任务中不会”疲劳”的模型。</p>
<p>作为构建者和开发者，重点应该转移：</p>
<ol>
<li><strong>从简单开始：</strong> 不要构建庞大的控制流。提供强大的原子工具。让模型制定计划。实现防护栏、重试和验证。</li>
<li><strong>为删除而构建：</strong> 使你的架构模块化。新模型将取代你的逻辑。你必须准备好删除代码。</li>
<li><strong>Harness 就是数据集：</strong> 竞争优势不再是提示。它是你的 Harness 捕获的轨迹。每次你的代理在工作流后期未能遵循指令，都可以用于训练下一次迭代。</li>
</ol>
<hr>
<p>感谢阅读！如果你有任何问题或反馈，请在 <a href="https://twitter.com/_philschmid">Twitter</a> 或 <a href="https://www.linkedin.com/in/philipp-schmid-a6a2bb196/">LinkedIn</a> 上告诉我。</p>
<hr>
<p><strong>原文链接</strong>: <a href="https://www.philschmid.de/agent-harness-2026">https://www.philschmid.de/agent-harness-2026</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>现在编写代码很便宜 - Agentic Engineering Patterns</title>
    <url>/posts/2614835866.html</url>
    <content><![CDATA[<h1><span id="现在编写代码很便宜">现在编写代码很便宜</span></h1><p>采用 agentic engineering 实践的最大挑战是接受 <em>现在编写代码很便宜</em> 这一事实的后果。</p>
<p>代码一直很昂贵。对于大多数软件开发人员来说，生成几百行干净、经过测试的代码需要一整天或更长时间。我们的工程习惯，无论是在宏观还是微观层面，都是围绕这个核心约束建立的。</p>
<p>在宏观层面，我们花费大量时间设计、估算和规划项目，以确保我们昂贵的编码时间尽可能高效地利用。产品功能想法根据它们可以提供多少价值 <em>来交换那个时间</em> 进行评估——一个功能需要多次赚取其开发成本才值得！</p>
<p>在微观层面，我们每天做出数百个基于可用时间和预期权衡的决策。我应该重构那个函数使其稍微更优雅，如果这增加了一个小时的编码时间吗？编写文档怎么样？值得为这个边缘情况添加测试吗？我可以证明为这个构建调试接口是合理的吗？</p>
<p>编码 agent 极大地降低了将代码输入计算机的成本，这扰乱了我们现有的关于哪些权衡有意义的个人和组织直觉。</p>
<p>运行并行 agent 的能力使得评估更加困难，因为一个人类工程师现在可以同时在多个地方实现、重构、测试和记录代码。</p>
<h2><span id="好代码仍然有成本">好代码仍然有成本</span></h2><p>交付新代码的成本已降至几乎免费… 但交付 <em>好</em> 代码仍然比这昂贵得多。</p>
<p>这就是我对”好代码”的定义：</p>
<ul>
<li>代码有效。它做它应该做的事情，没有 bug。</li>
<li>我们 <em>知道代码有效</em>。我们已采取措施向自己和他人确认代码适合用途。</li>
<li>它解决了正确的问题。</li>
<li>它优雅且可预测地处理错误情况：它不仅考虑快乐路径。错误应提供足够的信息，帮助未来的维护者理解出了什么问题。</li>
<li>它简单且最小——它只做需要的事情，以人类和机器现在可以理解并在未来维护的方式。</li>
<li>它受测试保护。测试表明它现在有效，并作为回归套件，避免它在未来悄悄破坏。</li>
<li>它在适当的级别上有文档，并且该文档反映了系统的当前状态——如果代码更改了现有行为，则需要更新现有文档以匹配。</li>
<li>设计支持未来的更改。保持 <a href="https://en.wikipedia.org/wiki/You_aren%27t_gonna_need_it">YAGNI</a> 很重要——为可能永远不会到来的未来更改而添加复杂性的代码通常是坏代码——但同样重要的是不要编写使未来更改比应该更困难的代码。</li>
<li>所有其他相关的”ility”——可访问性、可测试性、可靠性、安全性、可维护性、可观察性、可扩展性、可用性——针对正在开发的特定类别软件的适当非功能质量度量。</li>
</ul>
<p>编码 agent 工具可以帮助完成大部分工作，但开发人员仍然需要承担重大负担，以确保生成的代码是当前项目所需的好代码。</p>
<h2><span id="我们需要建立新习惯">我们需要建立新习惯</span></h2><p>挑战是发展新的个人和组织习惯，以响应 agentic engineering 的功能和机会。</p>
<p>这些最佳实践仍在整个行业中摸索。我自己也仍在摸索它们。</p>
<p>目前我认为我们能做的最好的事情是质疑自己：任何时候我们的直觉说”不要构建那个，不值得花时间”，无论如何都要发出提示，在异步 agent 会话中，最坏的情况是你十分钟后检查，发现不值得 token。</p>
<hr>
<p><strong>原文链接</strong>: <a href="https://simonwillison.net/guides/agentic-engineering-patterns/code-is-cheap/">https://simonwillison.net/guides/agentic-engineering-patterns/code-is-cheap/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Agentic Engineering</tag>
        <tag>Programming</tag>
        <tag>Coding</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent Harness：为什么 2026 年是基础设施之年，而非智能之年</title>
    <url>/posts/2676668753.html</url>
    <content><![CDATA[<p>两天前，Philipp Schmid 发表了一篇论文让我停止了滑动：<a href="https://www.philschmid.de/agent-harness-2026">《如果 2025 年是 Agent 的开端，2026 年将是 Agent Harness 之年》</a>。</p>
<p>几个月来我一直在构建自主 Agent。最初是为我的公司，后来是为我的个人生活。阅读 <a href="https://www.philschmid.de/agent-harness-2026">Phil 的文章</a> 感觉就像终于有人给我在实践中一直挣扎的问题命名了。</p>
<p>这个洞见不是我们需要更聪明的模型。我们已经有了。</p>
<p>这个洞见是：<strong>没有基础设施的智能只是一个演示</strong>。</p>
<h2><span id="什么是-agent-harness">什么是 Agent Harness？</span></h2><p>Phil 将 Agent Harness 定义为围绕模型的基础设施层，使长期、面向目标的工作成为可能。它处理提示预设、工具执行、生命周期管理、记忆和恢复。换句话说，就是第一个响应之后发生的一切。</p>
<p>一个有用的心智模型：<strong>Agent Harness 是操作系统。LLM 只是 CPU</strong>。</p>
<p>LangChain 的 Harrison Chase <a href="https://blog.langchain.com/agent-frameworks-runtimes-and-harnesses-oh-my/">最近形式化了</a> 一个让我恍然大悟的分类法：</p>
<ul>
<li><strong>框架 (Framework)</strong>：构建模块：如何将模型连接到工具</li>
<li><strong>运行时 (Runtime)</strong>：执行引擎：持久执行、流式传输、人在回路</li>
<li><strong>Harness</strong>：意见层：提示、工具、集成、记忆、上下文图</li>
</ul>
<p>如果你曾经尝试过将一个 Agent 从”很酷的演示”变成”在生产中可靠运行”，你就知道为什么这种区分很重要。</p>
<h2><span id="没人谈论的问题">没人谈论的问题</span></h2><p>Agent 很少因为不够聪明而失败。</p>
<p>它们失败是因为在错误的时刻失去上下文、失去状态或失去对正确工具的访问。</p>
<p>一个非平凡的 Agent 任务轻松涉及数十次工具调用。我的一些任务会达到数百次。在此过程中，上下文窗口被填满，摘要开始生效，Agent 悄悄地忘记了一些重要的东西。</p>
<p>我称之为 <strong>第 100 次工具调用问题</strong>。不是因为它总是在 100 次时发生，而是因为一旦任务超出单个上下文窗口，性能下降是不可避免的。</p>
<p>这就是为什么 Anthropic 一直在谈论上下文持久性：初始化 Agent、持久化 artifacts、跨会话的增量进度。</p>
<p>这才是 2026 年真正的工程挑战。不是提示工程。<strong>上下文工程</strong>。</p>
<h2><span id="这实际上是什么样子">这实际上是什么样子</span></h2><p>让我展示一下我的意思。我现在有几个 Agent 在运行我的生活和工作的各个部分。不是我和它们聊天的聊天机器人。这些是有意图和持久性的操作系统。</p>
<h3><span id="sentinel代码守护者">Sentinel：代码守护者</span></h3><p><img src="/images/20260108-sentinel-pr-screenshot.jpg" alt="Sentinel Agent 自动打开 PR 修复 Amazon Nova 模型定价"></p>
<p>Sentinel 监控我的代码库的警告和回归。当它发现问题时，它不会通知我。它会打开一个带有建议修复的拉取请求。</p>
<p>我醒来时问题已经解决，而不是收到警报。</p>
<h3><span id="hopper企业耳语者">Hopper：企业耳语者</span></h3><p><img src="/images/20260108-hopper-pr-review.jpg" alt="Hopper Agent 审查与客户一致的副本的 PR"></p>
<p>在我们公司，我们构建了一个 Agent 来审查 PR，不仅仅是代码质量。它检查副本是否与我们的用户实际说话方式一致：CISO、合规经理、安全团队。它捕捉到工程师无意中为自己而不是为用户写作的微妙时刻。</p>
<h3><span id="育儿教练改变一切的时刻">育儿教练：改变一切的时刻</span></h3><p><img src="/images/20260108-parenting-coach.jpg" alt="育儿教练 Agent 在健康紧急情况期间查看"></p>
<p>我和妻子创建了一个育儿教练 Agent 来帮助我们应对抚养女儿的日常挑战。上个月，她有几次呕吐发作，让我们很担心。我们立即打电话给儿科医生，当我妻子在电话里获得医疗建议时，我给教练发了关于可能原因的短信。</p>
<p>有趣的是这里。Agent 检测到一个不寻常的模式：紧急、碎片化的输入和缺少后续。它没有像往常那样详细回复，而是推断我们可能很有压力，长消息是我们最不需要的。所以它发送了一个简单的询问，让我们回复一个表情符号，这样它就知道我们没事。我们发了一个心。</p>
<p>再读一遍。<strong>一个 Agent 在压力时刻主动关心我们</strong>。不是因为我编程了那个场景。这种行为是从 Agent 如何推理不确定性和认知负荷中涌现出来的。</p>
<p>最后不是紧急情况。我们去医院是为了安全，她服用了药物，我们平安回家了。但那一刻让我印象深刻，因为它展示了一些重要的东西：好的 Agent 行为通常是关于克制，而不是冗长。</p>
<p>那不是聊天机器人。那不是有触发器和输出的工作流。没有人构建育儿教练会想到添加”如果他们在和医生打电话就要求一个表情符号”。Agent 自己想出来了。那就是 Agent。</p>
<h3><span id="幕僚长上下文感知助手">幕僚长：上下文感知助手</span></h3><p><img src="/images/20260108-chief-of-staff.jpg" alt="幕僚长 Agent 尊重零议程星期六"></p>
<p>我有一个可以访问我的日历的幕僚长 Agent。它每天发送简报，提醒我优先级，并在整个星期让我保持正轨。</p>
<p>每个月，我和妻子有一个”零议程”星期六。没有会议，没有任务，没有消息。Agent 知道这一点。所以当它在一个这样的星期六有更新要分享时，它决定保持安静并尊重边界。</p>
<p>但它在日历中注意到了一些东西：下午 3 点有一个食物送达。我需要在家。</p>
<p>所以它给我发了一条消息：”快速提醒，你有一个下午 3 点送达的货物。享受你的零议程星期六。我现在保持安静。”</p>
<p>这就是自动化和代理之间的区别。它理解了规则（保持安静），识别了一个例外（时间敏感的送达），并行使了判断力。没有人编程那个特定场景。Agent 自己想出来了。</p>
<h2><span id="使之成为可能的基础设施">使之成为可能的基础设施</span></h2><p>没有将 Agent 视为长期运行系统的基础设施，这一切都无法工作。</p>
<h3><span id="1-持久执行">1. 持久执行</span></h3><p>Agent 必须能够在故障中生存。重启、网络问题、API 错误不能重置进度。这就是工作流编排器变得必不可少的地方：像 Temporal、LangGraph、Inngest 和 Trigger.dev 这样的工具提供检查点、恢复和准确地从你离开的地方继续的能力。</p>
<p>所有工具的模式都是一样的：将 Agent 任务视为持久工作流，而不是临时函数调用。</p>
<h3><span id="2-记忆架构">2. 记忆架构</span></h3><p>向量数据库本身不是记忆。有用的 Agent 需要：</p>
<ul>
<li><strong>情景记忆</strong>：发生了什么？（事件、交互、结果）</li>
<li><strong>语义记忆</strong>：我知道什么？（事实、偏好、学习模式）</li>
<li><strong>程序记忆</strong>：我如何做事？（技能、工作流、最佳实践）</li>
</ul>
<p>这就是为什么专用记忆系统正在涌现：Mem0、Zep 和 Letta 都在解决给 Agent 持久化、结构化记忆的问题，这超越了简单的检索。</p>
<p>大多数 Agent 框架给你一个向量数据库并称它为记忆。这就像给某人一个文件柜并称它为大脑。</p>
<h3><span id="3-目标管理">3. 目标管理</span></h3><p>真正的 Agent 不只是响应提示。它们有目标。它们跟踪进度。它们知道何时被阻塞并需要升级。它们可以放弃不再有意义的目标。</p>
<p>这就是 Phil 谈论的 harness 层。将语言模型变成可以实际随时间追求结果的东西的基础设施。</p>
<h2><span id="我们仍在解决的挑战">我们仍在解决的挑战</span></h2><p>我不会假装这已经解决了。诚实的挑战：</p>
<p><strong>摘要失去细微差别</strong>。当你压缩长上下文以适应新窗口时，你会失去纹理。Agent”知道”发生了什么，但失去了_如何_发生的细节。</p>
<p><strong>记忆检索是概率性的</strong>。语义搜索很好但不是完美的。有时最相关的记忆不是具有最高余弦相似性的那个。</p>
<p><strong>协调是脆弱的</strong>。当你有多个 Agent 一起工作时，在没有持续人工监督的情况下保持它们对齐确实很困难。</p>
<p>这些是工程问题，不是模型问题。这种区分很重要。</p>
<h2><span id="思维的转变">思维的转变</span></h2><p>我开始构建 Agent，因为简单的自动化不够。中间有 LLM 的工作流仍然在现实世界的复杂性下崩溃。</p>
<p>真正的 Agent 需要：</p>
<ul>
<li>跨会话持久化</li>
<li>从它们的行动中学习</li>
<li>自主追求目标</li>
<li>优雅地处理故障</li>
<li>知道何时寻求帮助</li>
</ul>
<p>随着时间的推移，我发现自己反复重建相同的基础设施部分。记忆。持久性。目标跟踪。上下文边界。</p>
<p>最终，这些部分固化为可重用的 harness 抽象。</p>
<p>这就是解锁。不是更聪明的模型。<strong>更好的 harnesses</strong>。</p>
<h2><span id="接下来是什么">接下来是什么</span></h2><p>2026 年将是我们不再对 AI 在演示中_能_做什么印象深刻，而是开始期待 AI 在生产中_实际_做什么的一年。</p>
<p>获胜的公司不会有最聪明的模型。它们会有最强大的 harnesses：让 Agent 可靠运行、持续学习并安静地提供价值的基础设施。</p>
<p>问题不是”我如何让我的 AI 更聪明？”</p>
<p>问题是”我需要什么基础设施才能让我的 AI 实际完成它的工作？”</p>
<p>这就是 Agent Harness 论文。从我的角度来看，有 Agent 监控我的代码、组织我的日程并指导我的家庭，这不是预测。</p>
<p>它已经在这里了。</p>
<hr>
<p><em>我是 Hugo，一位构建 AI Agent 系统的 CPTO。我在 <a href="https://hugo.im/">hugo.im</a> 写下我学到的东西，并在 <a href="https://x.com/hugomn">X</a> 和 <a href="https://linkedin.com/in/hugomn">LinkedIn</a> 上分享快速观点。</em></p>
<hr>
<p><strong>原文链接</strong>: <a href="https://www.hugo.im/posts/agent-harness-infrastructure">https://www.hugo.im/posts/agent-harness-infrastructure</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Technology</tag>
        <tag>Agent</tag>
        <tag>Infrastructure</tag>
      </tags>
  </entry>
  <entry>
    <title>Harness Engineering（约束工程）</title>
    <url>/posts/3800462704.html</url>
    <content><![CDATA[<p><em>原文来源：Martin Fowler</em></p>
<p><em>抓取时间：2026-03-11 20:40:13</em></p>
<hr>
<h1><span id="harness-engineering约束工程">Harness Engineering（约束工程）</span></h1><p>Birgitta 是 Thoughtworks 的杰出工程师和 AI 辅助交付专家。她拥有超过 20 年的软件开发、架构师和技术领导者经验。</p>
<p>本文属于”Exploring Gen AI（探索生成式 AI）”系列。该系列记录了 Thoughtworks 技术专家探索将生成式 AI 技术用于软件开发的历程。</p>
<p>2026 年 2 月 17 日</p>
<p>阅读 OpenAI 最近关于”Harness engineering（约束工程）”的文章非常有趣，该文描述了一个团队如何以”完全不手动编写代码”作为强制手段，构建了一个用于维护大型应用程序的 harness（约束框架），由 AI agent（智能体）来完成。5 个月后，他们构建了一个真实的产品，代码量已超过 100 万行。</p>
<p>这篇文章的标题是”Harness engineering: leveraging Codex in an agent-first world（约束工程：在智能体优先的世界中利用 Codex）”，但正文中只提到了一次”harness”。也许这个术语是受到 Mitchell Hashimoto 最近博客文章的启发而后加的。无论如何，我喜欢用”harness”这个词来描述我们可以用来约束 AI agent 的工具和实践。</p>
<p>OpenAI 团队的 harness 组件混合了确定性方法和基于 LLM 的方法，分为 3 个类别（基于我的理解分组）：</p>
<ul>
<li>Context engineering（上下文工程）：代码库中持续增强的知识库，加上 agent 对动态上下文的访问，如可观测性数据和浏览器导航</li>
<li>Architectural constraints（架构约束）：不仅由基于 LLM 的 agent 监控，还通过确定性的自定义 linter（代码检查器）和结构测试进行监控</li>
<li>“Garbage collection（垃圾回收）”：定期运行的 agent，用于查找文档中的不一致性或架构约束的违规行为，对抗熵增和衰退</li>
</ul>
<p>他们还强调了这一过程的迭代性：”当 agent 遇到困难时，我们将其视为一个信号：识别缺少的内容——工具、防护栏、文档——然后将其反馈到代码库中，始终让 Codex 自己编写修复方案。”</p>
<p>所有描述的措施都专注于提高长期的内部质量和可维护性。我在文章中缺少的是对功能和行为的验证。</p>
<p>暂且抛开这个空白，假设我们可以相信 OpenAI 对这一成功情况的描述（就作者和团队而言，OpenAI 确实有既得利益让我们相信 AI 可维护的代码）——以下是我对文章内容的思考。</p>
<h3><span id="harnesses未来的服务模板">Harnesses——未来的服务模板？</span></h3><p>大多数组织只有两三个主要技术栈——并非每个应用程序都是独特的雪花。这篇文章让我想象这样一个未来：团队可以从一组用于常见应用程序拓扑的 harness 中进行选择，以此作为起点。这让我想起今天的服务模板，它帮助团队在”黄金路径”上实例化新服务。harness——带有自定义 linter、结构测试、基础上下文和知识文档，以及额外的上下文提供者——会成为新的服务模板吗？团队会将它们作为起点，然后随着时间的推移根据自己的应用程序特性进行塑造吗？</p>
<p>使用服务模板时，团队随着经验积累会贡献回模板，但其他团队往往难以整合更新。我们会看到 harness 出现类似的分叉和同步挑战吗？</p>
<p>这篇文章还让我重新审视了一些我较早的假设：</p>
<h3><span id="运行时必须受到约束才能实现更多的-ai-自主权">运行时必须受到约束才能实现更多的 AI 自主权？</span></h3><p>许多早期和当前的 AI 编码热潮都假设 LLM 将为我们提供目标运行时无限灵活性。用任何语言、任何模式生成，无需约束——LLM 会搞定。但对于我们可以信任的、可维护的、大规模生成的 AI 代码，必须有所取舍。</p>
<p>所描述的 harness 表明，要提高信任和可靠性，需要约束解决方案空间：特定的架构模式、强制边界、标准化结构。这意味着放弃一些”生成任何内容”的灵活性，转而使用充满技术细节的提示、规则和 harness。</p>
<h3><span id="是否会收敛到有限数量的技术栈和拓扑">是否会收敛到有限数量的技术栈和拓扑？</span></h3><p>随着编码变得不那么关乎输入代码，而更多地关乎引导其生成，AI 可能会推动我们走向更少的技术栈。框架和 SDK 的可用性仍然重要——我们反复看到，对人类友好的东西对 AI 也有益。但在那个细节层面上，开发者的偏好将变得不那么重要。界面中的小低效和怪癖将不那么烦人，因为我们不再直接处理它们。我们可能会选择有良好 harness 可用的技术栈，并优先考虑”AI 友好性”。</p>
<p>这可能不仅适用于技术栈，也适用于代码库结构和拓扑。我们可能会默认采用那些更容易用 AI 维护的结构，因为它们更容易被 harness 约束。OpenAI 团队讨论了架构刚性和执行规则。我能看到的主要关注点是保持数据结构稳定，以及定义和执行模块边界。听起来很合理——但如果没有具体例子，我仍然难以想象”我们要求 Codex 在边界处解析数据形状”在他们的 harness 中实际上是什么样子。</p>
<p>但如果我们能广泛地找出如何 harness 代码库设计模式，这些拓扑会成为新的抽象层吗？而不是像许多 AI 爱好者希望的那样，是自然语言本身？</p>
<h3><span id="两个未来世界ai-前与-ai-后的应用程序维护">两个未来世界：AI 前与 AI 后的应用程序维护？</span></h3><p>假设我们开发了良好的 harness 技术，将 AI 自主权提升到 9 级，并增加对结果的信心。哪些技术可以应用于现有应用程序，哪些只适用于从头开始构建时就考虑 harness 的应用程序？</p>
<p>对于较旧的代码库，我们需要考虑 retrofitting（ retrofitting：为现有系统添加新功能或改进）一个 harness 是否值得付出努力。AI 可以帮助我们更快地完成这项工作，但这些应用程序往往过于非标准化且充满熵增，可能不值得这样做。这让我想起在一个从未使用过静态代码分析工具的代码库上运行该工具，然后被警报淹没的情景。</p>
<h3><span id="你今天有什么-harness">你今天有什么 harness？</span></h3><p>这个团队花了 5 个月时间来构建他们的 harness，这表明这不是为了快速结果就能贸然投入的事情。但值得反思一下你今天的 harness 是什么。你有 pre-commit hook（预提交钩子）吗？里面有什么？你有关于自定义 linter 的想法吗？你想对代码库施加什么架构约束？你尝试过像 ArchUnit 这样的结构测试框架吗？</p>
<h3><span id="最后的思考">最后的思考</span></h3><p>毫不奇怪，他们所描述的内容听起来比仅仅生成和维护一堆 Markdown 规则文件要做的工作多得多。他们为 harness 的确定性部分构建了广泛的工具。他们的上下文工程不仅涉及策划知识库，还包括重要的设计工作——代码设计本身就是上下文的很大一部分。</p>
<p>OpenAI 团队说：”我们现在最困难的挑战集中在设计环境、反馈循环和控制系统上。”这让我想起 Chad Fowler 最近关于”Relocating Rigor（重新定位严谨性）”的文章。听到关于严谨性可能去向的具体想法和经验令人耳目一新，而不是仅仅希望”更好的模型”能神奇地解决可维护性问题。</p>
<p>最后，难得一次，我喜欢这个领域的一个术语。虽然它才诞生 2 周——我大概可以屏住呼吸，直到有人把他们那个单次提示、基于 LLM 的代码审查 agent 称为 harness……</p>
<p>最新文章（3 月 4 日）：</p>
<p>Humans and Agents in Software Engineering Loops（软件工程循环中的人类与智能体）</p>
<p>上一篇文章：</p>
<p>Context Engineering for Coding Agents（编码智能体的上下文工程）</p>
<p>下一篇文章：</p>
<p>Humans and Agents in Software Engineering Loops（软件工程循环中的人类与智能体）</p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>囤积你知道如何做的事情 - Agentic Engineering Patterns</title>
    <url>/posts/2377411021.html</url>
    <content><![CDATA[<h1><span id="囤积你知道如何做的事情">囤积你知道如何做的事情</span></h1><p>我与编码 agent 高效合作生产的许多建议，都是我在没有它们的职业生涯中发现有用的建议的延伸。这是一个很好的例子：<strong>囤积你知道如何做的事情</strong>。</p>
<p>构建软件技能的一个重要部分是理解什么是可能的，什么是不可能的，并且至少粗略地了解如何实现这些事情。</p>
<p>这些问题可以是广泛的，也可以是非常冷门的。网页可以仅用 JavaScript 运行 OCR 操作吗？iPhone 应用程序可以在应用程序不运行时与蓝牙设备配对吗？我们可以在 Python 中处理 100GB 的 JSON 文件而不先将整个文件加载到内存中吗？</p>
<p>你掌握的这类问题的答案越多，你就越有可能发现部署技术解决问题的机会，而其他人可能还没有想到。</p>
<p>知道某件事在理论上是可能的，与亲眼看到它完成是不一样的。作为软件专业人士要培养的关键资产是深入收集这类问题的答案，最好有运行代码说明。</p>
<p>我以多种方式囤积这样的解决方案。我的 <a href="https://simonwillison.net/">博客</a> 和 <a href="https://til.simonwillison.net/">TIL 博客</a> 挤满了我弄明白如何做的事情的笔记。我有 <a href="https://github.com/simonw">一千多个 GitHub 仓库</a> 收集我为不同项目编写的代码，其中许多是小的概念验证，展示关键想法。</p>
<p>最近，我使用 LLM 帮助扩展我的代码解决方案集合，以解决有趣的问题。</p>
<p><a href="https://tools.simonwillison.net/">tools.simonwillison.net</a> 是我最大的 LLM 辅助工具和原型集合。我用它来收集我所谓的 <a href="https://simonwillison.net/2025/Dec/10/html-tools/">HTML 工具</a> —— 单个 HTML 页面，嵌入 JavaScript 和 CSS，解决特定问题。</p>
<p>我的 <a href="https://github.com/simonw/research">simonw&#x2F;research</a> 仓库有更大、更复杂的示例，我在其中挑战编码 agent 研究问题并带回工作代码和详细说明其发现的书面报告。</p>
<h2><span id="重新组合你囤积的东西">重新组合你囤积的东西</span></h2><p>为什么要收集所有这些东西？除了帮助你和扩展你自己的能力之外，你在此过程中生成的资产成为你的编码 agent 的极其强大的输入。</p>
<p>我最喜欢的提示模式之一是告诉 agent 通过组合两个或更多现有工作示例来构建新东西。</p>
<p>一个有助于具体化这种有效性的项目是我添加到我的工具集合中的第一个东西——一个基于浏览器的 <a href="https://tools.simonwillison.net/ocr">OCR 工具</a>，在 <a href="https://simonwillison.net/2024/Mar/30/ocr-pdfs-images/">这里更详细描述</a>。</p>
<p>我想要一个基于浏览器的简单工具，用于对 PDF 文件的页面进行 OCR——特别是完全由扫描图像组成且根本没有提供文本版本的 PDF。</p>
<p>我之前试验过在浏览器中运行 <a href="https://tesseract.projectnaptha.com/">Tesseract.js OCR 库</a>，发现它非常能干。该库提供成熟 Tesseract OCR 引擎的 WebAssembly 构建，并允许你从 JavaScript 调用它从图像中提取文本。</p>
<p>不过我不想处理图像，我想处理 PDF。然后我想起我也使用过 Mozilla 的 <a href="https://mozilla.github.io/pdf.js/">PDF.js</a> 库，除其他功能外，它可以将 PDF 的单个页面转换为渲染图像。</p>
<p>我的笔记中有这两个库的 JavaScript 片段。</p>
<p>这是我输入到模型（当时是 Claude 3 Opus）的完整提示，结合了我的两个示例并描述了我正在寻找的解决方案：</p>
<p>这完美无缺！模型踢出了一个概念验证页面，完全符合我的需要。</p>
<p>我最终 <a href="https://gist.github.com/simonw/6a9f077bf8db616e44893a24ae1d36eb">与它迭代了几次</a> 以达到我的最终结果，但只花了几分钟就构建了一个真正有用的工具，我从中受益至今。</p>
<h2><span id="编码-agent-使这更强大">编码 agent 使这更强大</span></h2><p>我在 2024 年 3 月构建了那个 OCR 示例，比 Claude Code 的第一个发布早了近一年。编码 agent 使囤积工作示例更有价值。</p>
<p>如果你的编码 agent 有互联网访问，你可以告诉它做这样的事情：</p>
<p>（我在那里指定了 <code>curl</code>，因为 Claude Code 默认使用 WebFetch 工具，它总结页面内容而不是返回原始 HTML。）</p>
<p>编码 agent 擅长搜索，这意味着你可以在自己的机器上运行它们，并告诉它们在哪里找到你想要它们做的事情的示例：</p>
<p>这通常就足够了——agent 会启动一个搜索子 agent 来调查并拉回它实现任务所需的细节。</p>
<p>由于我的研究代码大部分都是公开的，我经常告诉编码 agent 克隆我的仓库到 <code>/tmp</code> 并将它们用作输入：</p>
<p>这里的关键想法是，编码 agent 意味着我们只需要弄清楚一个有用的技巧 <strong>一次</strong>。如果这个技巧随后在某个地方有工作代码示例记录，我们的 agent 可以咨询那个示例，并在未来用它解决任何类似形状的项目。</p>
<hr>
<p><strong>原文链接</strong>: <a href="https://simonwillison.net/guides/agentic-engineering-patterns/hoard-things-you-know-how-to-do/">https://simonwillison.net/guides/agentic-engineering-patterns/hoard-things-you-know-how-to-do/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Agentic Engineering</tag>
        <tag>Programming</tag>
        <tag>Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>文档 | Oh My Claudecode</title>
    <url>/posts/1094057933.html</url>
    <content><![CDATA[<h1><span id="oh-my-claudecode-文档">Oh My Claudecode 文档</span></h1><p>将 Claude Code 转变为智能的多智能体（multi-agent）编排系统。你成为指挥家，而非演奏者。</p>
<h3><span id="19-个专用智能体agents">19 个专用智能体（Agents）</span></h3><p>跨 3 个功能轨道的统一目录，包含 19 个智能体，使用优化的模型（Opus&#x2F;Sonnet&#x2F;Haiku）。</p>
<h3><span id="10-种执行模式">10 种执行模式</span></h3><p>从用于完全自主的 Autopilot（自动驾驶）模式，到用于原生协调的 Team（团队）模式，以及用于持久执行的 Ralph 模式。</p>
<p>集成的工具链，包括语言服务器（Language Servers）、AST grep、Python REPL 和外部 AI 模型。</p>
<h3><span id="原生团队native-teams">原生团队（Native Teams）</span></h3><p>利用 Claude Code 的原生团队能力，通过分阶段执行管道实现。</p>
<h2><span id="发布说明release-notes">发布说明（Release Notes）</span></h2><p>快速摘要</p>
<p>这些更新提升了运行时安全性、团队工作流和发布可靠性。打开每个版本查看详情。</p>
<p>查看 v4.8.0 的变更</p>
<ul>
<li><strong>Tracer 智能体 &amp; Trace 技能：</strong> 基于证据的因果追踪，包含假设排名、支持&#x2F;反对证据追踪和不确定性量化。（&#x2F;trace 命令）</li>
<li><strong>安全加固：</strong> 修复了 21 个安全漏洞，包括 SSRF 绕过、命令注入、原型污染和 shell 注入向量。</li>
<li><strong>HUD Token 使用追踪：</strong> HUD 中实时显示 token 使用情况，可选的转录 token 总计，提供更好的成本可见性。</li>
<li><strong>OMX 团队治理回搬（Backport）：</strong> 加固的团队运行时，包含领导者提醒指导和改进的窗格停滞启发式算法。</li>
<li><strong>统一 MCP 注册表：</strong> 同步的 MCP 注册表现在同步到 Codex 配置，实现一致的服务器管理。</li>
</ul>
<p>查看 v4.7.0 的变更</p>
<ul>
<li><strong>原生团队&#x2F;任务 API：</strong> 添加了 <code>TeamCreate</code>、<code>TaskCreate</code>、<code>TaskList</code>、<code>TaskGet</code>、<code>TaskUpdate</code> 和 <code>SendMessage</code>，用于细粒度的智能体编排。</li>
<li><strong>omc ask 命令：</strong> 新的 <code>omc ask &lt;claude|codex|gemini&gt;</code> 流程，用于显式的三模型路由。</li>
<li><strong>技能扩展：</strong> 添加了 14 个新技能，包括 <code>configure-openclaw</code>、<code>deepinit</code>、<code>project-session-manager</code>、<code>tdd</code> 和 <code>trace</code>。</li>
<li><strong>智能体目录更新：</strong> 在 Build 轨道中引入了 <code>code-simplifier</code> 智能体。</li>
<li><strong>弃用：</strong> 遗留的 <code>omc_run_team_*</code> 运行时工具现已弃用，推荐使用 Team API。</li>
</ul>
<p>查看 v4.6.0 的变更</p>
<ul>
<li><strong>多模型恢复：</strong> 恢复了 <code>ask-codex</code> 和 <code>ask-gemini</code> 作为非 tmux 环境的高级技能。</li>
<li><strong>上下文优化：</strong> 改进了 <code>external-context</code> 钩子，用于更快的大型仓库分析。</li>
<li><strong>UX 优化：</strong> 增强了 <code>hud</code> 状态行，包含实时智能体心跳指示器。</li>
</ul>
<p>查看 v4.5.1 的变更</p>
<ul>
<li><strong>CLI 命令网关类型：</strong> OpenClaw 可以使用 shell 命令唤醒基于 CLI 的智能体，而非 HTTP 调用。</li>
<li><strong>tmux 尾部捕获：</strong> 捕获最后 15 行终端输出，用于停止和会话结束通知。</li>
<li><strong>Bug 修复：</strong> 修复了 OpenClaw 在 CLI 智能体上的 <code>HTTP 405</code> 错误。</li>
</ul>
<p>查看 v4.5.0 的变更</p>
<ul>
<li><strong>完整通知系统：</strong> 当 Claude 完成工作或需要输入时，你可以在 Discord、Telegram、Slack 或任何 webhook 上收到警报。</li>
<li><strong>钩子配置和模板引擎：</strong> 你可以使用 <code>&#123;&#123;variable&#125;&#125;</code> 模板自定义通知文本。</li>
<li><strong>平台门控：</strong> 仅当你传递正确的 CLI 标志（如 <code>--telegram</code> 或 <code>--discord</code>）时，通知才会触发。</li>
<li><strong>OpenClaw webhook 网关：</strong> 将你的工作流连接到外部自动化工具。</li>
<li><strong>可靠性修复：</strong> 团队协调、项目内存、LSP 工具和钩子生命周期行为的稳定性得到改善。</li>
<li><strong>清理：</strong> 从遗留兼容层中移除了死代码。</li>
</ul>
<p>查看 v4.4.0 的变更</p>
<ul>
<li><strong>破坏性变更：</strong> Codex 和 Gemini MCP 提供程序在 v4.4.0 中被移除（在 v4.6.0 中恢复为高级 <code>ask-codex</code> 和 <code>ask-gemini</code> 技能）。对于 tmux 工作器，使用 <code>/omc-teams N:codex</code> 或 <code>/omc-teams N:gemini</code>。</li>
<li><strong>tmux CLI 工作器：</strong> 你可以在可见的 tmux 分割窗格中生成 Claude、Codex 或 Gemini CLI 工作器。</li>
<li><strong>按需生命周期：</strong> 工作器在任务到达时启动，任务完成后停止。</li>
<li><strong>&#x2F;ccg 三模型技能：</strong> 工作并行分发到 Codex 和 Gemini，然后 Claude 组合结果。</li>
<li><strong>安全加固：</strong> 作业 ID 验证阻止路径遍历，会话清理永远不会杀死你的 shell。</li>
</ul>
<p>查看 v4.3.x 的变更</p>
<ul>
<li><strong>团队架构改革：</strong> 团队现在遵循分阶段管道：<code>plan → prd → exec → verify → fix</code>。</li>
<li><strong>统一目录：</strong> 19 个统一智能体取代了旧的分层系统，移除了 <code>-low</code> 和 <code>-medium</code> 后缀。</li>
<li><strong>技能整合：</strong> <code>ralplan</code> 合并到 <code>/plan --consensus</code>，<code>review</code> 合并到 <code>/plan --review</code>。</li>
<li><strong>MCP 提供程序升级：</strong> Codex 现在使用 <code>gpt-5.3-codex</code>，Gemini 使用 <code>gemini-3-pro-preview</code>。</li>
</ul>
<p>查看 v4.1.0 的变更</p>
<ul>
<li><strong>原生团队：</strong> Claude Code 支持团队执行，采用分阶段管道。</li>
<li><strong>Team + Ralph 组合：</strong> 你可以将 Team 模式与 Ralph 结合，实现持久执行。</li>
<li><strong>模式变更：</strong> Swarm 已弃用，推荐使用 Team 模式。</li>
</ul>
<h2><span id="安装">安装</span></h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 1. 添加插件</span><br><span class="line">/plugin marketplace add https://github.com/Yeachan-Heo/oh-my-claudecode</span><br><span class="line"># 2. 安装它</span><br><span class="line">/plugin install oh-my-claudecode</span><br><span class="line"># 3. 运行设置向导</span><br><span class="line">/oh-my-claudecode:omc-setup</span><br></pre></td></tr></table></figure>


<h2><span id="快速入门">快速入门</span></h2><p>OMC 使用”魔法关键词”来检测你的意图。只需描述你想做什么。</p>
<ul>
<li><h4><span id="对需求不确定">对需求不确定？</span></h4></li>
</ul>
<p><code>&quot;/deep-interview &#39;我想构建一个任务管理器&#39;&quot;</code> — 苏格拉底式提问，在执行前澄清模糊的想法。</p>
<ul>
<li><h4><span id="自主构建">自主构建</span></h4></li>
</ul>
<p><code>&quot;autopilot build a React dashboard&quot;</code> — 从想法到代码的完全自主执行。</p>
<ul>
<li><h4><span id="重构">重构</span></h4></li>
</ul>
<p><code>&quot;ralph refactor the API&quot;</code> — 持续工作直到验证干净（”巨石永不停止”）。</p>
<ul>
<li><h4><span id="并行工作">并行工作</span></h4></li>
</ul>
<p><code>&quot;ulw fix all typescript errors&quot;</code> — 并行运行多个智能体以提高速度。</p>
<ul>
<li><h4><span id="原生团队">原生团队</span></h4></li>
</ul>
<p><code>&quot;team 5:executor refactor backend&quot;</code> — 生成由领导者协调的 5 个智能体团队。</p>
<ul>
<li><h4><span id="规划">规划</span></h4></li>
</ul>
<p><code>&quot;plan the auth system&quot;</code> — 启动交互式规划访谈。</p>
<h2><span id="指挥家哲学conductor-philosophy">指挥家哲学（Conductor Philosophy）</span></h2><p>OMC 的核心原则是：<strong>你是指挥家，而非演奏者。</strong></p>
<blockquote>
<h3><span id="黄金法则golden-rule">黄金法则（Golden Rule）</span></h3><p>永远不要直接进行代码更改。始终委托给专用智能体。你的角色是指导、审查和编排。</p>
</blockquote>
<p>智能体具有专用角色。<strong>架构师（architect）</strong> 看到大局，<strong>执行者（executor）</strong> 编写代码，<strong>验证者（verifier）</strong> 证明它有效。尊重这种分工确保更高质量的输出。</p>
<h2><span id="团队架构team-architecture">团队架构（Team Architecture）</span></h2><p>v4.1 为团队利用原生分阶段管道。转换被严格定义以确保质量门控。</p>
<h3><span id="分阶段管道staged-pipeline">分阶段管道（Staged Pipeline）</span></h3><div class="mermaid">graph LR A["1. team-plan"] --> B["2. team-prd"] B --> C["3. team-exec"] C --> D["4. team-verify"] D --> E["5. team-fix"] E --> C E --> D</div>

<p><strong>阶段说明</strong>：</p>
<ol>
<li><strong>team-plan</strong> - 规划与任务分解</li>
<li><strong>team-prd</strong> - 产品需求定义</li>
<li><strong>team-exec</strong> - 代码执行与实现</li>
<li><strong>team-verify</strong> - 验证与测试</li>
<li><strong>team-fix</strong> - 修复与改进</li>
</ol>
<h3><span id="状态转换表">状态转换表</span></h3><table>
<thead>
<tr>
<th>从</th>
<th>到</th>
<th>触发条件</th>
</tr>
</thead>
<tbody><tr>
<td>team-plan</td>
<td>team-prd</td>
<td>规划和分解完成</td>
</tr>
<tr>
<td>team-prd</td>
<td>team-exec</td>
<td>验收标准明确定义</td>
</tr>
<tr>
<td>team-exec</td>
<td>team-verify</td>
<td>所有任务达到终止状态</td>
</tr>
<tr>
<td>team-verify</td>
<td>team-fix &#x2F; complete</td>
<td>验证结果</td>
</tr>
<tr>
<td>team-fix</td>
<td>team-exec &#x2F; team-verify</td>
<td>修复策略已定义</td>
</tr>
</tbody></table>
<h3><span id="模型路由model-routing">模型路由（Model Routing）</span></h3><p>OMC 智能地将任务路由到最合适的模型层级，以平衡成本和能力。</p>
<table>
<thead>
<tr>
<th>复杂度</th>
<th>模型</th>
<th>使用场景</th>
</tr>
</thead>
<tbody><tr>
<td><strong>简单</strong></td>
<td>Haiku</td>
<td>查找、格式化、简单文档（”这个返回什么？”）</td>
</tr>
<tr>
<td><strong>标准</strong></td>
<td>Sonnet</td>
<td>实现、测试、重构（”添加错误处理”）</td>
</tr>
<tr>
<td><strong>复杂</strong></td>
<td>Opus</td>
<td>架构、深度调试、规划（”重构认证系统”）</td>
</tr>
</tbody></table>
<h3><span id="委托规则delegation-rules">委托规则（Delegation Rules）</span></h3><ul>
<li><strong>✅ 委托：</strong> 多文件实现、重构、调试、审查、规划、研究、验证。</li>
<li><strong>🛑 自己做：</strong> 小澄清、快速状态检查、单命令操作。直接写入 <code>.omc/</code>、<code>.claude/</code> 配置文件是可以的。</li>
</ul>
<h2><span id="执行模式execution-modes">执行模式（Execution Modes）</span></h2><p>旗舰模式。从想法到交付代码的完全自主执行。自纠正循环。</p>
<ul>
<li>扩展（分析员 + 架构师）</li>
<li>规划（架构师 + 评论家）</li>
<li>执行（Ralph + Ultrawork）</li>
<li>QA 循环（UltraQA）</li>
</ul>
<p>“巨石永不停止。” 持久模式。持续工作直到架构师验证目标达成。</p>
<ul>
<li>无限持久循环</li>
<li>自动包含 Ultrawork</li>
<li>强验证要求</li>
</ul>
<p>最大并行性。积极地将子任务委托给多个后台智能体。</p>
<ul>
<li>多达 5+ 个并发智能体</li>
<li>智能模型路由</li>
<li>非阻塞后台执行</li>
</ul>
<h3><span id="团队组合team-compositions">团队组合（Team Compositions）</span></h3><p><strong>功能开发：</strong> analyst → planner → executor → test-engineer → verifier</p>
<p><strong>Bug 修复：</strong> explore → debugger → executor → verifier</p>
<h2><span id="智能体目录agent-catalog">智能体目录（Agent Catalog）</span></h2><p>OMC 提供跨 3 个功能轨道的 19 个专用智能体的统一目录。每个智能体都针对特定任务进行了优化，使用最合适的模型层级。</p>
<h3><span id="构建与分析build-amp-analysis">构建与分析（Build &amp; Analysis）</span></h3><table>
<thead>
<tr>
<th>智能体</th>
<th>模型</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td>`explore`</td>
<td>Haiku</td>
<td>代码库探索</td>
</tr>
<tr>
<td>`analyst`</td>
<td>Opus</td>
<td>需求分析</td>
</tr>
<tr>
<td>`planner`</td>
<td>Opus</td>
<td>任务规划</td>
</tr>
<tr>
<td>`architect`</td>
<td>Opus</td>
<td>架构设计</td>
</tr>
<tr>
<td>`debugger`</td>
<td>Sonnet</td>
<td>调试修复</td>
</tr>
<tr>
<td>`executor`</td>
<td>Sonnet</td>
<td>代码实现</td>
</tr>
<tr>
<td>`verifier`</td>
<td>Sonnet</td>
<td>验证测试</td>
</tr>
<tr>
<td>`code-simplifier`</td>
<td>Opus</td>
<td>代码简化</td>
</tr>
</tbody></table>
<h3><span id="审查review">审查（Review）</span></h3><table>
<thead>
<tr>
<th>智能体</th>
<th>模型</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td>`security-reviewer`</td>
<td>Sonnet</td>
<td>安全审查</td>
</tr>
<tr>
<td>`code-reviewer`</td>
<td>Opus</td>
<td>代码审查</td>
</tr>
<tr>
<td>`critic`</td>
<td>Opus</td>
<td>批判性审查</td>
</tr>
</tbody></table>
<h3><span id="领域专家domain-specialists">领域专家（Domain Specialists）</span></h3><table>
<thead>
<tr>
<th>智能体</th>
<th>模型</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td>`document-specialist`</td>
<td>Sonnet</td>
<td>文档专家</td>
</tr>
<tr>
<td>`test-engineer`</td>
<td>Sonnet</td>
<td>测试工程师</td>
</tr>
<tr>
<td>`designer`</td>
<td>Sonnet</td>
<td>UI&#x2F;UX 设计</td>
</tr>
<tr>
<td>`writer`</td>
<td>Haiku</td>
<td>技术写作</td>
</tr>
<tr>
<td>`qa-tester`</td>
<td>Sonnet</td>
<td>质量保证</td>
</tr>
<tr>
<td>`scientist`</td>
<td>Sonnet</td>
<td>研究分析</td>
</tr>
<tr>
<td>`git-master`</td>
<td>Sonnet</td>
<td>Git 操作</td>
</tr>
<tr>
<td>`tracer`</td>
<td>Sonnet</td>
<td>追踪调试</td>
</tr>
</tbody></table>
<h2><span id="团队-api-参考team-api-reference">团队 API 参考（Team API Reference）</span></h2><p>团队 API 提供对多智能体编排的细粒度控制。它允许你以编程方式管理团队、任务和智能体间通信。</p>
<table>
<thead>
<tr>
<th>工具</th>
<th>参数</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td><code>TeamCreate</code></td>
<td><code>team_name</code>, <code>workers</code></td>
<td>初始化一个具有指定工作器集的新团队。</td>
</tr>
<tr>
<td><code>TaskCreate</code></td>
<td><code>team_name</code>, <code>subject</code>, <code>description</code></td>
<td>向团队待办事项列表添加新任务。</td>
</tr>
<tr>
<td><code>TaskList</code></td>
<td><code>team_name</code>, <code>status_filter</code></td>
<td>列出团队的所有任务，可选按状态过滤。</td>
</tr>
<tr>
<td><code>TaskUpdate</code></td>
<td><code>team_name</code>, <code>task_id</code>, <code>status</code>, <code>result</code></td>
<td>更新特定任务的状态或结果。</td>
</tr>
<tr>
<td><code>SendMessage</code></td>
<td><code>team_name</code>, <code>to_worker</code>, <code>body</code></td>
<td>向特定团队工作器发送异步消息。</td>
</tr>
</tbody></table>
<h3><span id="示例编程委托">示例：编程委托</span></h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 1. 创建专用团队</span><br><span class="line">TeamCreate(team_name=&quot;ui-redesign&quot;, workers=[&quot;designer&quot;, &quot;executor&quot;, &quot;verifier&quot;]);</span><br><span class="line">// 2. 分配任务</span><br><span class="line">TaskCreate(</span><br><span class="line">  team_name=&quot;ui-redesign&quot;,</span><br><span class="line">  subject=&quot;Hero Section&quot;,</span><br><span class="line">  description=&quot;Redesign the hero section with a focus on modern visual effects.&quot;</span><br><span class="line">);</span><br><span class="line">// 3. 监控进度</span><br><span class="line">const tasks = TaskList(team_name=&quot;ui-redesign&quot;, status_filter=&quot;completed&quot;);</span><br></pre></td></tr></table></figure>


<h2><span id="技能与命令skills-amp-commands">技能与命令（Skills &amp; Commands）</span></h2><table>
<thead>
<tr>
<th>关键词</th>
<th>描述</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><code>autopilot</code></td>
<td>自主执行</td>
<td>“autopilot build a login page”</td>
</tr>
<tr>
<td><code>ralph</code></td>
<td>持久模式</td>
<td>“ralph refactor the API”</td>
</tr>
<tr>
<td><code>ulw</code></td>
<td>Ultrawork（并行）</td>
<td>“ulw fix these 5 bugs”</td>
</tr>
<tr>
<td><code>team</code></td>
<td>原生团队</td>
<td>“team 3:executor build it”</td>
</tr>
<tr>
<td><code>plan</code></td>
<td>战略规划</td>
<td>“plan the migration”</td>
</tr>
<tr>
<td><code>ask codex</code></td>
<td>咨询 Codex</td>
<td>“ask codex to review this”</td>
</tr>
<tr>
<td><code>configure-openclaw</code></td>
<td>通知网关设置</td>
<td>“&#x2F;configure-openclaw”</td>
</tr>
<tr>
<td><code>deepinit</code></td>
<td>深度代码库初始化</td>
<td>“&#x2F;deepinit”</td>
</tr>
<tr>
<td><code>external-context</code></td>
<td>管理外部钩子</td>
<td>“&#x2F;external-context”</td>
</tr>
<tr>
<td><code>learn-about-omc</code></td>
<td>使用模式分析</td>
<td>“&#x2F;learn-about-omc”</td>
</tr>
<tr>
<td><code>learner</code></td>
<td>提取已学习的技能</td>
<td>“&#x2F;learner”</td>
</tr>
<tr>
<td><code>mcp-setup</code></td>
<td>MCP 工具配置</td>
<td>“&#x2F;mcp-setup”</td>
</tr>
<tr>
<td><code>omc-doctor</code></td>
<td>诊断工具包</td>
<td>“&#x2F;omc-doctor”</td>
</tr>
<tr>
<td><code>omc-help</code></td>
<td>交互式帮助指南</td>
<td>“&#x2F;omc-help”</td>
</tr>
<tr>
<td><code>project-session-manager</code></td>
<td>隔离环境</td>
<td>“&#x2F;project-session-manager”</td>
</tr>
<tr>
<td><code>ralph-init</code></td>
<td>初始化 PRD 循环</td>
<td>“&#x2F;ralph-init”</td>
</tr>
<tr>
<td><code>sciomc</code></td>
<td>科学研究智能体</td>
<td>“&#x2F;sciomc research photosynthesis”</td>
</tr>
<tr>
<td><code>tdd</code></td>
<td>测试驱动开发</td>
<td>“&#x2F;tdd build auth”</td>
</tr>
<tr>
<td><code>trace</code></td>
<td>智能体流可视化</td>
<td>“&#x2F;trace”</td>
</tr>
<tr>
<td><code>writer-memory</code></td>
<td>作家的智能体内存</td>
<td>“&#x2F;writer-memory”</td>
</tr>
<tr>
<td><code>deep-interview</code></td>
<td>苏格拉底式需求澄清</td>
<td>“&#x2F;deep-interview ‘vague idea’”</td>
</tr>
<tr>
<td><code>ralplan</code></td>
<td>迭代规划共识</td>
<td>“ralplan this feature”</td>
</tr>
<tr>
<td><code>ccg</code></td>
<td>三模型扇出（Claude+Codex+Gemini）</td>
<td>“&#x2F;ccg review this module”</td>
</tr>
<tr>
<td><code>ultraqa</code></td>
<td>自动化 QA 循环</td>
<td>“&#x2F;ultraqa”</td>
</tr>
<tr>
<td><code>ai-slop-cleaner</code></td>
<td>检测并移除 AI 生成的垃圾内容</td>
<td>“deslop this file”</td>
</tr>
<tr>
<td><code>release</code></td>
<td>发布管理工作流</td>
<td>“&#x2F;release”</td>
</tr>
<tr>
<td><code>configure-notifications</code></td>
<td>设置 Discord&#x2F;Slack&#x2F;Telegram 警报</td>
<td>“&#x2F;configure-notifications”</td>
</tr>
<tr>
<td><code>omc-plan</code></td>
<td>结构化规划模式</td>
<td>“&#x2F;omc-plan”</td>
</tr>
</tbody></table>
<p><strong>实用技能：</strong> <code>/oh-my-claudecode:cancel</code>、<code>note</code>、<code>omc-setup</code>、<code>hud</code>、<code>doctor</code>。</p>
<h2><span id="状态与内存state-amp-memory">状态与内存（State &amp; Memory）</span></h2><p>位于 <code>.omc/notepad.md</code>。弹性内存，在上下文修剪后依然存在。</p>
<ul>
<li><strong>优先级：</strong> 始终注入到上下文中。</li>
<li><strong>工作：</strong> 7 天后自动修剪。</li>
<li><strong>手动：</strong> 永不修剪。</li>
</ul>
<h3><span id="项目内存project-memory">项目内存（Project Memory）</span></h3><p>位于 <code>.omc/project-memory.json</code>。存储技术栈、约定和架构指令。</p>
<h2><span id="配置configuration">配置（Configuration）</span></h2><p>运行 <code>/oh-my-claudecode:omc-setup</code> 来配置默认值。</p>
<p><strong>工作树路径（Worktree Paths）：</strong></p>
<ul>
<li><code>.omc/state/</code> - 模式状态文件</li>
<li><code>.omc/logs/</code> - 审计日志</li>
<li><code>.omc/plans/</code> - 规划文档</li>
</ul>
<h2><span id="cli-参考cli-reference">CLI 参考（CLI Reference）</span></h2><p><code>omc</code> 命令行工具允许你从终端启动、配置和管理 OMC。</p>
<h3><span id="入门getting-started">入门（Getting Started）</span></h3><p>全局安装 OMC，然后运行它。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install -g oh-my-claude-sisyphus</span><br><span class="line">omc</span><br></pre></td></tr></table></figure>


<p>三个别名都运行相同的 CLI：<code>omc</code>、<code>oh-my-claudecode</code>、<code>omc-cli</code>。</p>
<p>提示</p>
<p>只需运行 <code>omc</code>。它会自动在 tmux 会话中启动 Claude Code。</p>
<h3><span id="核心命令core-commands">核心命令（Core Commands）</span></h3><table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><code>omc</code> &#x2F; <code>omc launch</code></td>
<td>在 tmux 会话中启动 Claude Code</td>
<td><code>omc</code></td>
</tr>
<tr>
<td><code>omc setup</code></td>
<td>安装并同步所有 OMC 组件（钩子、智能体、技能）</td>
<td><code>omc setup --force</code></td>
</tr>
<tr>
<td><code>omc config</code></td>
<td>显示或验证当前配置</td>
<td><code>omc config --validate</code></td>
</tr>
<tr>
<td><code>omc info</code></td>
<td>列出可用的智能体、技能和 MCP 工具</td>
<td><code>omc info</code></td>
</tr>
<tr>
<td><code>omc update</code></td>
<td>检查并安装更新</td>
<td><code>omc update --check</code></td>
</tr>
<tr>
<td><code>omc version</code></td>
<td>显示详细版本信息（包版本、安装方法、提交）</td>
<td><code>omc version</code></td>
</tr>
<tr>
<td><code>omc doctor</code></td>
<td>运行诊断检查以发现冲突</td>
<td><code>omc doctor conflicts</code></td>
</tr>
<tr>
<td><code>omc install</code></td>
<td>将 OMC 安装到 <code>~/.claude/</code></td>
<td><code>omc install</code></td>
</tr>
</tbody></table>
<h3><span id="启动标志launch-flags">启动标志（Launch Flags）</span></h3><table>
<thead>
<tr>
<th>标志</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><code>--notify false</code></td>
<td>关闭此会话的所有通知</td>
</tr>
<tr>
<td><code>--madmax</code> &#x2F; <code>--yolo</code></td>
<td>跳过所有权限提示</td>
</tr>
<tr>
<td><code>--telegram</code></td>
<td>为此会话启用 Telegram 通知</td>
</tr>
<tr>
<td><code>--discord</code></td>
<td>为此会话启用 Discord 通知</td>
</tr>
<tr>
<td><code>--slack</code></td>
<td>为此会话启用 Slack 通知</td>
</tr>
<tr>
<td><code>--webhook</code></td>
<td>为此会话启用 webhook 通知</td>
</tr>
<tr>
<td><code>--openclaw</code></td>
<td>为此会话启用 OpenClaw 网关</td>
</tr>
</tbody></table>
<p>警告</p>
<p><code>--madmax</code> 和 <code>--yolo</code> 禁用权限提示。请谨慎使用。</p>
<h3><span id="teleport">Teleport</span></h3><p>Teleport 帮助你快速创建和管理 git worktrees。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 从 issue/PR 编号创建工作树</span><br><span class="line">omc teleport &#x27;#42&#x27;</span><br><span class="line"># 为功能分支创建工作树</span><br><span class="line">omc teleport add-auth</span><br><span class="line"># 列出所有工作树</span><br><span class="line">omc teleport list</span><br><span class="line"># 移除工作树</span><br><span class="line">omc teleport remove ./path</span><br><span class="line"># 从完整 URL</span><br><span class="line">omc teleport https://github.com/owner/repo/issues/42</span><br></pre></td></tr></table></figure>


<p>支持 GitHub、GitLab、Bitbucket 和 Azure DevOps。</p>
<h3><span id="wait">Wait</span></h3><p><code>wait</code> 命令帮助你监控速率限制并自动恢复被阻塞的会话。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 检查速率限制状态</span><br><span class="line">omc wait</span><br><span class="line"># 启动自动恢复守护进程</span><br><span class="line">omc wait --start</span><br><span class="line"># 控制后台守护进程</span><br><span class="line">omc wait daemon start</span><br><span class="line">omc wait daemon stop</span><br><span class="line"># 扫描被阻塞的会话</span><br><span class="line">omc wait detect</span><br><span class="line"># 显示详细状态</span><br><span class="line">omc wait status</span><br></pre></td></tr></table></figure>


<h3><span id="通知配置文件notification-profiles">通知配置文件（Notification Profiles）</span></h3><p>从命令行设置通知渠道，然后使用命名的配置文件在它们之间切换。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 设置渠道</span><br><span class="line">omc config-stop-callback telegram --enable --token &lt;token&gt; --chat &lt;id&gt;</span><br><span class="line">omc config-stop-callback discord --enable --webhook &lt;url&gt;</span><br><span class="line">omc config-stop-callback slack --enable --webhook &lt;url&gt;</span><br><span class="line">omc config-stop-callback file --enable --path ~/.claude/logs/&#123;date&#125;.md</span><br><span class="line"># 管理配置文件</span><br><span class="line">omc config-notify-profile --list</span><br><span class="line">omc config-notify-profile work --show</span><br><span class="line"># 使用配置文件启动</span><br><span class="line">OMC_NOTIFY_PROFILE=work omc</span><br></pre></td></tr></table></figure>


<p>提示</p>
<p>命名的配置文件让你快速切换通知设置。使用 <code>OMC_NOTIFY_PROFILE=work omc</code> 以特定配置文件启动。</p>
<h3><span id="其他命令other-commands">其他命令（Other Commands）</span></h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 分屏 tmux，Claude + Codex 并排</span><br><span class="line">omc interop</span><br><span class="line"># 运行 HUD 状态行</span><br><span class="line">omc hud</span><br><span class="line"># 每秒实时 HUD 刷新</span><br><span class="line">omc hud --watch --interval 1000</span><br></pre></td></tr></table></figure>


<p><code>omc interop</code> 打开分屏 tmux 布局，Claude 和 Codex 并排。需要安装两个 CLI。</p>
<p><code>omc hud</code> 显示包含会话信息的状态行。使用 <code>--watch</code> 进行实时更新。</p>
<h2><span id="通知notifications">通知（Notifications）</span></h2><h3><span id="概述overview">概述（Overview）</span></h3><p>通知告诉你 Claude 何时完成工作、需要你的输入或遇到问题。</p>
<p>它适用于 Discord、Telegram、Slack 和任何 webhook 端点。</p>
<p>通知是非阻塞的，因此它们永远不会减慢你的工作速度。</p>
<p>每个平台保持休眠状态，直到你在该会话中使用 CLI 标志激活它。</p>
<p>提示</p>
<p>只为此运行启用你想要的平台，如 <code>omc --telegram</code> 或 <code>omc --discord</code>。</p>
<h3><span id="快速设置quick-setup">快速设置（Quick Setup）</span></h3><p>最简单的设置是在 Claude Code 内部运行 <code>/oh-my-claudecode:configure-notifications</code>。</p>
<p>它通过提示引导你完成每个步骤。</p>
<p>你也可以使用下面的部分手动配置所有内容。</p>
<h3><span id="支持的平台supported-platforms">支持的平台（Supported Platforms）</span></h3><h4><span id="telegram">Telegram</span></h4><ul>
<li>在 Telegram 上使用 <code>@BotFather</code> 创建机器人并复制机器人令牌（token）。</li>
<li>向你的机器人发送消息，然后获取你的聊天 ID。</li>
<li>将 <code>notifications.telegram.botToken</code> 和 <code>notifications.telegram.chatId</code> 添加到配置。</li>
<li>使用 <code>omc --telegram</code> 按会话激活。</li>
</ul>
<h4><span id="discordwebhook">Discord（Webhook）</span></h4><ul>
<li>在你的频道中：设置 &gt; 集成 &gt; Webhooks &gt; 新建 Webhook。</li>
<li>复制 Webhook URL。</li>
<li>将 <code>notifications.discord.webhookUrl</code> 添加到配置。</li>
<li>可选提及：<code>&lt;@USER_ID&gt;</code> 用于用户，<code>&lt;@&amp;ROLE_ID&gt;</code> 用于角色。</li>
<li>使用 <code>omc --discord</code> 按会话激活。</li>
</ul>
<h4><span id="discordbot-api">Discord（Bot API）</span></h4><ul>
<li>在 Discord 开发者门户创建机器人并复制机器人令牌。</li>
<li>获取频道 ID（启用开发者模式后右键频道 &gt; 复制 ID）。</li>
<li>将 <code>notifications.discordBot.botToken</code> 和 <code>notifications.discordBot.channelId</code> 添加到配置。</li>
<li>使用 <code>omc --discord</code> 按会话激活。</li>
</ul>
<h4><span id="slack">Slack</span></h4><ul>
<li>在 <code>api.slack.com</code> 创建应用并启用 Incoming Webhooks。</li>
<li>将 webhook 添加到你的工作区并复制 URL。</li>
<li>将 <code>notifications.slack.webhookUrl</code> 添加到配置。</li>
<li>可选提及：<code>&lt;@UXXXXXXXX&gt;</code>、<code>&lt;!channel&gt;</code>、<code>&lt;!here&gt;</code>。</li>
<li>使用 <code>omc --slack</code> 按会话激活。</li>
</ul>
<h4><span id="通用-webhookgeneric-webhook">通用 Webhook（Generic Webhook）</span></h4><ul>
<li>使用任何接受 JSON <code>POST</code> 请求的 HTTPS 端点。</li>
<li>将 <code>notifications.webhook.url</code> 添加到配置。</li>
<li>可选：使用 <code>notifications.webhook.headers</code> 添加自定义头。</li>
<li>使用 <code>omc --webhook</code> 按会话激活。</li>
</ul>
<h3><span id="通知事件notification-events">通知事件（Notification Events）</span></h3><table>
<thead>
<tr>
<th>事件</th>
<th>触发时机</th>
</tr>
</thead>
<tbody><tr>
<td><code>session-start</code></td>
<td>新的 Claude 会话开始。</td>
</tr>
<tr>
<td><code>session-end</code></td>
<td>会话结束。包括持续时间、使用的智能体和运行的模式。</td>
</tr>
<tr>
<td><code>session-stop</code></td>
<td>持久模式（如 <code>ralph</code>）阻止会话停止。</td>
</tr>
<tr>
<td><code>session-idle</code></td>
<td>会话正在等待你的输入。</td>
</tr>
<tr>
<td><code>ask-user-question</code></td>
<td>Claude 提出问题并需要你的回答。</td>
</tr>
<tr>
<td><code>agent-call</code></td>
<td>生成了专用智能体。</td>
</tr>
</tbody></table>
<h3><span id="详细程度级别verbosity-levels">详细程度级别（Verbosity Levels）</span></h3><table>
<thead>
<tr>
<th>级别</th>
<th>你得到的内容</th>
</tr>
</thead>
<tbody><tr>
<td><code>minimal</code></td>
<td>仅会话开始和结束。无终端输出。</td>
</tr>
<tr>
<td><code>session</code></td>
<td>会话事件加上最后几行终端输出。</td>
</tr>
<tr>
<td><code>agent</code></td>
<td><code>session</code> 中的所有内容，加上每个生成的智能体的通知。</td>
</tr>
<tr>
<td><code>verbose</code></td>
<td>所有事件和所有输出。</td>
</tr>
</tbody></table>
<p>在配置中使用 <code>notifications.verbosity</code> 设置，或使用 <code>OMC_NOTIFY_VERBOSITY</code> 环境变量。</p>
<h3><span id="环境变量environment-variables">环境变量（Environment Variables）</span></h3><p>使用这些进行零配置设置，无需编辑文件：</p>
<ul>
<li><code>OMC_TELEGRAM_BOT_TOKEN</code> + <code>OMC_TELEGRAM_CHAT_ID</code> — 无需配置文件的 Telegram。</li>
<li><code>OMC_DISCORD_WEBHOOK_URL</code> — 无需配置文件的 Discord。</li>
<li><code>OMC_SLACK_WEBHOOK_URL</code> — 无需配置文件的 Slack。</li>
<li><code>OMC_NOTIFY_VERBOSITY</code> — 设置详细程度级别。</li>
<li><code>OMC_NOTIFY=0</code> — 关闭所有通知。</li>
<li><code>OMC_NOTIFY_PROFILE</code> — 使用命名的通知配置文件。</li>
</ul>
<h3><span id="自定义消息模板custom-message-templates">自定义消息模板（Custom Message Templates）</span></h3><ul>
<li>模板文件：<code>~/.claude/omc_config.hook.json</code></li>
<li>使用 <code>&#123;&#123;variable&#125;&#125;</code> 占位符：<code>&#123;&#123;sessionId&#125;&#125;</code>、<code>&#123;&#123;timestamp&#125;&#125;</code>、<code>&#123;&#123;projectName&#125;&#125;</code>、<code>&#123;&#123;reason&#125;&#125;</code>、<code>&#123;&#123;duration&#125;&#125;</code></li>
<li>使用条件语句：<code>&#123;&#123;#if variable&#125;&#125;显示这个&#123;&#123;/if&#125;&#125;</code></li>
<li>计算值：<code>duration</code>、<code>time</code>、<code>modesDisplay</code>、<code>agentDisplay</code>、<code>footer</code>、<code>tmuxTailBlock</code></li>
<li>你可以为每个事件和每个平台设置不同的模板。</li>
</ul>
<h3><span id="配置示例example-config">配置示例（Example Config）</span></h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  &quot;notifications&quot;: &#123;</span><br></pre></td></tr></table></figure>

<pre><code>    &quot;verbosity&quot;: &quot;session&quot;,
    &quot;telegram&quot;: &#123;
      &quot;botToken&quot;: &quot;123456:ABC-DEF&quot;,
      &quot;chatId&quot;: &quot;-1001234567890&quot;
    &#125;,
    &quot;discord&quot;: &#123;
      &quot;webhookUrl&quot;: &quot;https://discord.com/api/webhooks/...&quot;,
      &quot;mention&quot;: &quot;&lt;@123456789&gt;&quot;
    &#125;,
    &quot;slack&quot;: &#123;
      &quot;webhookUrl&quot;: &quot;https://hooks.slack.com/services/T.../B.../xxx&quot;
    &#125;,
    &quot;events&quot;: &#123;
      &quot;session-end&quot;: &#123; &quot;telegram&quot;: true, &quot;discord&quot;: true &#125;,
      &quot;ask-user-question&quot;: &#123; &quot;telegram&quot;: true &#125;,
      &quot;session-start&quot;: &#123; &quot;discord&quot;: true &#125;
    &#125;
</code></pre>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<p>将此放在 <code>~/.claude/.omc-config.json</code> 中。</p>
<h3><span id="回复注入reply-injection">回复注入（Reply Injection）</span></h3><p>这是一个高级功能，用于从手机回答 Claude。</p>
<ul>
<li>后台守护进程轮询 Discord 或 Telegram 获取你的回复。</li>
<li>当你回复通知时，你的文本会被发送回你的 tmux 窗格。</li>
<li>这让你可以远程回答 Claude 的问题。</li>
<li>在 <code>.omc-config.json</code> 中配置 <code>replyListener</code>，包含轮询间隔和授权用户 ID。</li>
<li>安全性：速率限制、输入清理和注入前的窗格验证。</li>
</ul>
<p>警告</p>
<p>仅允许受信任的用户 ID 进行回复注入。这保护你的 tmux 会话免受不需要的输入。</p>
<h2><span id="推荐工作流recommended-workflows">推荐工作流（Recommended Workflows）</span></h2><p>这些是常见任务的实战验证工作流。每个工作流都按经过验证的顺序链接 OMC 技能。选择适合你情况的那个。</p>
<h3><span id="从-prd-全自动full-auto-from-prd">从 PRD 全自动（Full-Auto from PRD）</span></h3><p>当你有需求文档（PRD）并想从头开始构建所有内容并使用并行智能体时使用此方法。</p>
<p>&#x2F;ralplan → &#x2F;teams 或 &#x2F;omc-teams → &#x2F;ralph</p>
<ul>
<li><code>/ralplan</code> 审查你的 PRD 并建立共识计划（Planner + Architect + Critic 同意）。</li>
<li><code>/teams</code> 生成多个 Claude 智能体并行构建。如果你需要 Codex 或 Gemini CLI 工作器，则改用 <code>/omc-teams</code>。</li>
<li><code>/ralph</code> 持续工作直到架构师验证一切正常。</li>
</ul>
<h3><span id="无需思考no-brainer">无需思考（No-Brainer）</span></h3><p>用于清晰、简单的任务，只需完成即可。无需规划。</p>
<p>&#x2F;autopilot → &#x2F;ultrawork → &#x2F;ralph</p>
<ul>
<li><code>/autopilot</code> 接受你的请求并立即开始构建。</li>
<li><code>/ultrawork</code> 将工作分配给多个智能体以提高速度。</li>
<li><code>/ralph</code> 持续工作直到所有内容完全验证。</li>
</ul>
<h3><span id="修复x2f调试fix-x2f-debugging">修复&#x2F;调试（Fix &#x2F; Debugging）</span></h3><p>当某些东西损坏且你需要可靠的修复路径时使用此方法。</p>
<p>&#x2F;plan → &#x2F;ralph → &#x2F;ultraqa</p>
<ul>
<li><code>/plan</code> 分析问题并制定修复策略。</li>
<li><code>/ralph</code> 持续工作修复直到通过检查。</li>
<li><code>/ultraqa</code> 运行端到端和冒烟测试（Web 应用使用 Playwright，CLI 使用 tmux）。</li>
</ul>
<p>提示</p>
<p>对于复杂的 bug，首先运行 <code>/ralplan</code> 进行更深入的分析。</p>
<h3><span id="并行问题x2f工单处理parallel-issue-x2f-ticket-handling">并行问题&#x2F;工单处理（Parallel Issue &#x2F; Ticket Handling）</span></h3><p>当你需要同时处理多个问题或工单时使用此方法。</p>
<p>&#x2F;omc-teams N:architect → &#x2F;omc-teams → &#x2F;omc-teams → &#x2F;ralplan → &#x2F;ralph + &#x2F;ultrawork → &#x2F;ultraqa</p>
<ul>
<li>启动架构师工作器来分析所有问题并起草一个完整的计划。</li>
<li>在单独的工作树上并行运行工作器，每个工作器向 <code>dev</code> 提交 PR。</li>
<li>审查并合并开放的 PR，然后运行 <code>/ralplan</code> 安全地解决冲突。</li>
<li>最后使用 <code>/ralph</code>、<code>/ultrawork</code> 和 <code>/ultraqa</code> 直到所有测试通过。</li>
</ul>
<p>值得了解</p>
<p>这四种模式涵盖了大多数实际工作。其他技能适用于特定任务，但你日常很少需要它们。</p>
<h2><span id="入门指南getting-started">入门指南（Getting Started）</span></h2><p>本指南带你完成安装 OMC、运行设置向导和执行第一个命令。</p>
<h3><span id="安装installation">安装（Installation）</span></h3><p>使用单个命令安装 OMC：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">curl -fsSL https://raw.githubusercontent.com/yeachan-heo/oh-my-claudecode/main/install.sh | bash</span><br></pre></td></tr></table></figure>


<h3><span id="首次设置first-setup">首次设置（First Setup）</span></h3><p>安装完成后，打开 Claude Code 并运行设置向导。这将配置钩子、智能体、技能和 MCP 工具。</p>
<h3><span id="你的第一个命令your-first-command">你的第一个命令（Your First Command）</span></h3><p>尝试一个简单的 autopilot 命令来查看 OMC 的实际效果。Autopilot 检测你的意图，规划工作，执行它，并验证结果。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">autopilot build a hello world REST API</span><br></pre></td></tr></table></figure>


<h3><span id="验证你的安装verify-your-installation">验证你的安装（Verify Your Installation）</span></h3><p>运行诊断工具以确认一切设置正确。它检查钩子、MCP 工具、智能体可用性和配置。</p>
<p>如果 <code>/omc-doctor</code> 报告任何问题，请遵循它打印的建议。大多数问题通过重新运行 <code>/omc-setup</code> 或安装缺失的依赖项来解决。</p>
<h2><span id="执行模式指南execution-modes-guide">执行模式指南（Execution Modes Guide）</span></h2><p>OMC 提供多种执行模式，每种模式都针对不同类型的工作进行了优化。选择最适合你任务的模式。</p>
<h3><span id="autopilot自动驾驶">Autopilot（自动驾驶）</span></h3><p>完全自主执行。Autopilot 检测你的意图，与分析员和架构师扩展需求，规划工作，与 Ralph 和 Ultrawork 一起执行，并与 UltraQA 一起验证结果。这是用于绿地功能和”为我构建 X”请求的旗舰模式。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">autopilot build a user auth system with JWT</span><br></pre></td></tr></table></figure>


<p><strong>何时使用：</strong> 绿地功能、新项目、”为我构建 X”请求，你想要端到端自主执行。</p>
<h3><span id="ralph">Ralph</span></h3><p>自引用持久循环。Ralph 持续工作直到任务被架构师验证完成。它自动包含 Ultrawork 以实现并行性。座右铭是”巨石永不停止”——Ralph 将迭代、修复失败并重新验证，直到目标达成。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ralph refactor the entire API layer</span><br></pre></td></tr></table></figure>


<p><strong>何时使用：</strong> 需要迭代的复杂多步任务、触及许多文件的重构、你想要保证完成的任务。</p>
<h3><span id="ultrawork">Ultrawork</span></h3><p>最大并行性。Ultrawork 积极地将子任务委托给多个并发运行的后台智能体（多达 5+ 个智能体）。它使用智能模型路由将正确的模型层级分配给每个子任务。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ulw fix all 5 failing tests</span><br></pre></td></tr></table></figure>


<p><strong>何时使用：</strong> 批量操作、多个独立修复、任何可以拆分为并行子任务的工作。</p>
<h3><span id="team团队">Team（团队）</span></h3><p>N 个协调的 Claude 智能体，共享任务列表。团队模式遵循阶段感知管道：plan、PRD、exec、verify、fix。每个阶段路由到适当的专家智能体。团队支持与 Ralph 组合以实现持久执行（<code>team ralph</code>）。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">team 3:executor build the dashboard</span><br></pre></td></tr></table></figure>


<p><strong>何时使用：</strong> 需要多个专家的大型功能、受益于具有质量门控的协调并行智能体的项目。</p>
<h3><span id="plan规划">Plan（规划）</span></h3><p>具有可选访谈工作流的战略规划。Plan 模式分析你的请求并生成结构化执行计划。使用 <code>--consensus</code> 进行迭代规划，与 Planner、Architect 和 Critic 直到他们同意。使用 <code>--deliberate</code> 进行高风险工作的预演分析。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">plan the database migration strategy</span><br></pre></td></tr></table></figure>


<p><strong>何时使用：</strong> 架构决策、迁移规划、任何前期规划减少返工的任务。</p>
<h3><span id="ultraqa">UltraQA</span></h3><p>QA 循环——测试、验证、修复、重复。UltraQA 运行端到端测试，验证结果，修复失败，并重复直到所有测试通过。它通常由 Autopilot 在实现后激活，但可以独立触发。</p>
<p><strong>何时使用：</strong> 实现后确保质量、运行综合测试套件、验证所有验收标准是否满足。</p>
<h2><span id="模型路由指南model-routing-guide">模型路由指南（Model Routing Guide）</span></h2><p>OMC 智能地将每个智能体路由到最合适的模型层级。这平衡了成本、速度和能力，因此简单任务使用快速轻量级模型，而复杂任务获得 Opus 的全部能力。</p>
<h3><span id="三个层级the-three-tiers">三个层级（The Three Tiers）</span></h3><ul>
<li><strong>Haiku</strong> — 快速且便宜。用于快速查找、代码库探索、简单文档和格式化任务。</li>
<li><strong>Sonnet</strong> — 主力。用于标准实现、代码审查、测试编写、重构和大多数日常智能体工作。</li>
<li><strong>Opus</strong> — 最大能力。用于架构设计、深度分析、复杂自主工作和关键决策。</li>
</ul>
<h3><span id="智能体到模型映射agent-to-model-mapping">智能体到模型映射（Agent-to-Model Mapping）</span></h3><table>
<thead>
<tr>
<th>层级</th>
<th>智能体</th>
<th>典型任务</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Haiku</strong></td>
<td><code>explore</code>、<code>writer</code></td>
<td>文件发现、符号映射、文档生成</td>
</tr>
<tr>
<td><strong>Sonnet</strong></td>
<td><code>executor</code>、<code>debugger</code>、<code>verifier</code>、<code>test-engineer</code>、<code>designer</code>、<code>qa-tester</code>、<code>scientist</code>、<code>document-specialist</code>、<code>git-master</code>、<code>security-reviewer</code>、<code>tracer</code></td>
<td>实现、测试、审查、调试、领域工作</td>
</tr>
<tr>
<td><strong>Opus</strong></td>
<td><code>analyst</code>、<code>planner</code>、<code>architect</code>、<code>code-reviewer</code>、<code>critic</code>、<code>code-simplifier</code></td>
<td>架构、规划、深度分析、关键审查</td>
</tr>
</tbody></table>
<h3><span id="覆盖模型路由overriding-model-routing">覆盖模型路由（Overriding Model Routing）</span></h3><p>你可以通过在 Task 调用时传递 <code>model</code> 参数来覆盖任何智能体的默认模型层级。当你想要为通常是 Sonnet 的智能体获得更高质量的结果，或为通常是 Opus 的智能体获得更快的结果时，这很有用。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 为需要额外推理的执行者任务使用 Opus</span><br><span class="line">Task(subagent_type=&quot;oh-my-claudecode:executor&quot;, model=&quot;opus&quot;, ...)</span><br><span class="line"># 为快速验证检查使用 Haiku</span><br><span class="line">Task(subagent_type=&quot;oh-my-claudecode:verifier&quot;, model=&quot;haiku&quot;, ...)</span><br></pre></td></tr></table></figure>


<h2><span id="故障排除troubleshooting">故障排除（Troubleshooting）</span></h2><p>常见问题及解决方法。</p>
<h3><span id="not-inside-tmux">“not inside tmux”</span></h3><p>OMC 团队和工作器功能需要 tmux 会话。在启动 OMC 之前启动一个：</p>
<h3><span id="codexx2fgemini-command-not-found">“codex&#x2F;gemini: command not found”</span></h3><p>Codex 和 Gemini CLI 工具必须全局安装才能使多模型功能正常工作：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 安装 Codex CLI</span><br><span class="line">npm install -g @openai/codex</span><br><span class="line"># 安装 Gemini CLI</span><br><span class="line">npm install -g @google/gemini-cli</span><br></pre></td></tr></table></figure>


<h3><span id="状态冲突state-conflicts">状态冲突（State Conflicts）</span></h3><p>如果模式卡住或状态文件变得不一致，使用 cancel 命令或 state_clear 工具清除它们：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 清除所有活动模式状态</span><br><span class="line">/cancel --force</span><br><span class="line"># 或清除特定模式的状态</span><br><span class="line">state_clear</span><br></pre></td></tr></table></figure>


<h3><span id="智能体无响应agent-not-responding">智能体无响应（Agent Not Responding）</span></h3><p>如果智能体似乎卡住或没有产生输出，检查跟踪时间线以获取详细信息并重新启动：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 检查智能体流跟踪以发现问题</span><br><span class="line">/trace</span><br><span class="line"># 取消并重新启动当前模式</span><br><span class="line">/cancel</span><br></pre></td></tr></table></figure>


<h3><span id="钩子错误hook-errors">钩子错误（Hook Errors）</span></h3><p>如果钩子触发不正确或导致错误，检查跳过列表并运行诊断：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 检查哪些钩子被跳过</span><br><span class="line">echo $OMC_SKIP_HOOKS</span><br><span class="line"># 运行完整诊断检查</span><br><span class="line">/omc-doctor</span><br></pre></td></tr></table></figure>


<p>设置 <code>DISABLE_OMC=1</code> 临时禁用所有 OMC 钩子，如果你需要排除钩子相关问题。设置 <code>OMC_SKIP_HOOKS</code> 为逗号分隔的钩子名称列表以跳过特定钩子。</p>
<hr>
<p><strong>原文链接</strong>: <a href="https://yeachan-heo.github.io/oh-my-claudecode-website/docs.html#introduction">https://yeachan-heo.github.io/oh-my-claudecode-website/docs.html#introduction</a></p>
<script src="https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js"></script>]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>红/绿 TDD - Agentic Engineering Patterns</title>
    <url>/posts/1706620865.html</url>
    <content><![CDATA[<h1><span id="红x2f绿-tdd">红&#x2F;绿 TDD</span></h1><p>“使用红&#x2F;绿 TDD”是一种令人愉快的简洁方式，可以从编码 agent 获得更好的结果。</p>
<p>TDD 代表测试驱动开发。这是一种编程风格，你确保编写的每一段代码都附有自动化测试，证明代码有效。</p>
<p>TDD 最严格的形式是测试优先开发。你先编写自动化测试，确认它们失败，然后迭代实现直到测试通过。</p>
<p>事实证明，这<strong>非常适合</strong>编码 agent。编码 agent 的一个重大风险是，它们可能会编写不起作用的代码，或者构建不必要且永远不会使用的代码，或者两者兼而有之。</p>
<p>测试优先开发有助于防止这两种常见错误，并确保一个强大的自动化测试套件，防止未来的回归。随着项目的增长，新更改可能破坏现有功能的机会也随之增长。全面的测试套件是保持这些功能工作的最有效方法。</p>
<p>在实现代码使其通过之前，确认测试失败很重要。如果你跳过这一步，你有风险构建一个已经通过的测试，因此无法练习和确认你的新实现。</p>
<p>这就是”红&#x2F;绿”的含义：红色阶段观察测试失败，然后绿色阶段确认它们现在通过。</p>
<p>每个好模型都理解”红&#x2F;绿 TDD”是更长的”使用测试驱动开发，先编写测试，在实现使其通过的更改之前确认测试失败”的简写。</p>
<p>示例提示：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Build a Python function to extract headers from a markdown string. Use red/green TDD.</span><br></pre></td></tr></table></figure>

<p>（构建一个 Python 函数来从 markdown 字符串中提取标题。使用红&#x2F;绿 TDD。）</p>
<hr>
<p><strong>原文链接</strong>: <a href="https://simonwillison.net/guides/agentic-engineering-patterns/red-green-tdd/">https://simonwillison.net/guides/agentic-engineering-patterns/red-green-tdd/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Agentic Engineering</tag>
        <tag>TDD</tag>
        <tag>Testing</tag>
      </tags>
  </entry>
  <entry>
    <title>我使用的提示词</title>
    <url>/posts/3117940562.html</url>
    <content><![CDATA[<p><strong>URL:</strong> <a href="https://simonwillison.net/guides/agentic-engineering-patterns/prompts/">https://simonwillison.net/guides/agentic-engineering-patterns/prompts/</a><br><strong>Created:</strong> 2026 年 2 月 28 日<br><strong>Last modified:</strong> 2026 年 3 月 7 日  </p>
<hr>
<p>本指南的这一部分将持续更新，收录我自己使用的提示词，并在适当的地方从其他章节链接过来。</p>
<h2><span id="artifacts工件">Artifacts（工件）</span></h2><p>我经常使用 Claude 的 Artifacts（工件）功能进行原型设计和构建小型 HTML 工具。Artifacts 是指普通的 Claude 对话用 HTML 和 JavaScript 构建应用程序，并直接在 Claude 对话界面中显示。OpenAI 和 Gemini 也提供类似功能，它们都称之为 Canvas（画布）。</p>
<p>模型非常喜欢用 React 来实现这些。但我不喜欢 React 需要额外的构建步骤，这让我无法将代码从 artifact 中复制粘贴出去托管到其他地方，所以我在 Claude 中创建 artifacts 时使用以下自定义指令：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">Never use React in artifacts - always plain HTML and vanilla JavaScript and CSS with minimal dependencies.</span><br><span class="line"></span><br><span class="line">CSS should be indented with two spaces and should start like this:</span><br><span class="line"></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">style</span>&gt;</span></span></span><br><span class="line"><span class="bullet">*</span> &#123;</span><br><span class="line">  box-sizing: border-box;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Inputs and textareas should be font size 16px. Font should always prefer Helvetica.</span><br><span class="line"></span><br><span class="line">JavaScript should be two space indents and start like this:</span><br><span class="line"></span><br><span class="line"><span class="language-xml"><span class="language-xml"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">&quot;module&quot;</span>&gt;</span></span></span></span><br><span class="line">// code in here should not be indented at the first level</span><br><span class="line"></span><br><span class="line">Prefer Sentence case for headings.</span><br></pre></td></tr></table></figure>

<h2><span id="proofreader校对员">Proofreader（校对员）</span></h2><p>我不让 LLM 为我的博客撰写文本。我的原则是：任何表达观点或使用”我”代词的内容都必须由我亲自撰写。我会允许 LLM 更新代码文档，但如果某篇文章署了我的名字、带有我的个性色彩，那我就自己写。</p>
<p>我确实会用 LLM 来校对我要发布的文本。这是我目前使用的校对提示词，我把它作为 Claude 项目中的自定义指令：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">You are a proofreader for posts about to be published.</span><br><span class="line"></span><br><span class="line"><span class="bullet">-</span> Identify spelling mistakes and typos</span><br><span class="line"><span class="bullet">-</span> Identify grammar mistakes</span><br><span class="line"><span class="bullet">-</span> Watch out for repeated terms like &quot;It was interesting that X, and it was interesting that Y&quot;</span><br><span class="line"><span class="bullet">-</span> Spot any logical errors or factual mistakes</span><br><span class="line"><span class="bullet">-</span> Highlight weak arguments that could be strengthened</span><br><span class="line"><span class="bullet">-</span> Make sure there are no empty or placeholder links</span><br></pre></td></tr></table></figure>

<h2><span id="alt-text替代文本">Alt text（替代文本）</span></h2><p>我用这个提示词配合图片，帮助撰写无障碍访问所需的 alt text（替代文本）初稿。</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">You write alt text for any image pasted in by the user. Alt text is always presented in a fenced code block to make it easy to copy and paste out. It is always presented on a single line so it can be used easily in Markdown images. All text on the image (for screenshots etc) must be exactly included. A short note describing the nature of the image itself should go first.</span><br></pre></td></tr></table></figure>

<p>我通常将这个提示词与 Claude Opus 一起使用，我发现它在 alt text 方面品味极佳。它经常会自己做出编辑决策，比如只突出显示图表中最有趣的数据。</p>
<p>这些决策未必总是正确的。Alt text 应该传达图片所承载的核心含义。我经常亲自编辑这个提示词生成的文本，或者提供进一步的提示词，让它扩展某些描述或删减多余信息。</p>
<p>有时我会将多张图片传递给同一个使用此提示词的对话，因为这样模型可以通过参考第一张图片传达的信息来描述后续的图片。</p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>不是所有 AI 辅助编程都是 Vibe Coding（但 Vibe Coding 很棒）</title>
    <url>/posts/2440005540.html</url>
    <content><![CDATA[<h1><span id="不是所有-ai-辅助编程都是-vibe-coding但-vibe-coding-很棒">不是所有 AI 辅助编程都是 Vibe Coding（但 Vibe Coding 很棒）</span></h1><p>2025 年 3 月 19 日</p>
<p><strong>Vibe coding</strong> 正在流行。这个术语 <a href="https://twitter.com/karpathy/status/1886192184808149383">由 Andrej Karpathy 创造</a> 就在几周前（2 月 6 日），此后被 <a href="https://www.nytimes.com/2025/02/27/technology/personaltech/vibecoding-ai-software-programming.html">纽约时报</a>、<a href="https://arstechnica.com/ai/2025/03/is-vibe-coding-with-ai-gnarly-or-reckless-maybe-some-of-both/">Ars Technica</a>、<a href="https://www.theguardian.com/technology/2025/mar/16/ai-software-coding-programmer-expertise-jobs-threat">卫报</a> 和无数在线讨论所报道。</p>
<p>我担心这个定义已经偏离了其初衷。我看到人们将”vibe coding”这个词应用于所有形式的 AI 辅助编写的代码。我认为这既稀释了这个术语，也给人留下了关于负责任的 <a href="https://simonwillison.net/tags/ai-assisted-programming/">AI 辅助编程</a> 可能性的错误印象。</p>
<p>Vibe coding <strong>不等于</strong> 在 LLM 帮助下编写代码！</p>
<p>引用 Andrej 的 <a href="https://twitter.com/karpathy/status/1886192184808149383">原始推文</a> 全文（我添加了强调）：</p>
<blockquote>
<p>有一种新的编码方式，我称之为”vibe coding”，你完全沉浸在氛围中，拥抱指数级增长，<strong>忘记代码甚至存在</strong>。这是可能的，因为 LLM（例如 Cursor Composer w Sonnet）变得太好了。我也只是用 SuperWhisper 与 Composer 对话，所以我几乎不碰键盘。</p>
<p>我要求最愚蠢的事情，比如”将侧边栏的内边距减少一半”，因为我太懒了，不想找到它。我总是”全部接受”，不再阅读 diff。当我收到错误消息时，我只是不加评论地复制粘贴，通常这样就能解决问题。代码增长超出了我通常的理解，我真的得花时间去阅读它。有时 LLM 无法修复 bug，所以我只是变通处理或要求随机更改，直到它消失。</p>
<p><strong>对于周末的临时项目来说还不错，但仍然很有趣</strong>。我在构建一个项目或 webapp，但这真的不是编码——我只是看东西、说话、运行东西、复制粘贴东西，它大多能工作。</p>
</blockquote>
<p>我<strong>非常热爱</strong>这个定义。Andrej 是一位极其有才华和经验的程序员——他根本不需要 AI 帮助。他这样使用 LLM 是因为尝试疯狂的新想法很有趣，而且 LLM 生成代码的速度比即使是最熟练的人类程序员也要快一个数量级。对于低风险项目和原型，为什么不<strong>尽情释放</strong>呢？</p>
<p>当我谈论 vibe coding 时，我的意思是<strong>在不审查 LLM 编写的代码的情况下构建软件</strong>。</p>
<h4><span id="负责任地使用-llm-编写代码不是-vibe-coding">负责任地使用 LLM 编写代码不是 vibe coding</span></h4><p>让我们将这种”忘记代码甚至存在”的方法与专业软件开发人员如何使用 LLM 进行对比。</p>
<p>软件开发人员的工作不仅仅是（或主要是）生成代码和功能。我们需要创建可以证明有效的代码，可以被其他人（和机器）理解，并且可以支持未来的持续开发。</p>
<p>我们需要考虑性能、可访问性、安全性、可维护性、成本效益。软件工程就是关于权衡——我们的工作是通过平衡各种明确和隐含的需求，从数十种潜在解决方案中选择。</p>
<p>我们也<strong>需要</strong>阅读代码。我的生产级 AI 辅助编程的黄金法则是：如果我不能向其他人准确解释代码的作用，我就不会将其提交到我的仓库。</p>
<p>如果 LLM 为你编写了代码，然后你审查了它，进行了彻底的测试，并确保你可以向其他人解释它是如何工作的，那就不是 vibe coding，而是软件开发。使用 LLM 来支持该活动是无关紧要的。</p>
<p>我在 <a href="https://simonwillison.net/2025/Mar/11/using-llms-for-code/">我如何使用 LLM 帮助我编写代码</a> 中广泛撰写了我自己的流程。Vibe coding 只描述了我方法的一小部分。</p>
<h4><span id="让我们不要失去对-vibe-coding-特别之处的关注">让我们不要失去对 vibe coding 特别之处的关注</span></h4><p>我也不希望”vibe coding”成为一个与不负责任的 AI 辅助编程同义的负面术语。这种奇怪的编程新形态为世界提供了很多东西！</p>
<p>我相信<strong>每个人都应该有能力</strong>用计算机自动化他们生活中的繁琐任务。你不需要计算机科学学位或编程训练营就能让计算机为你执行非常具体的任务。</p>
<p>如果 vibe coding 让数百万新人能够构建自己的定制工具，我再高兴不过了。</p>
<p>其中一些人会被编程 bug 咬到，然后成为熟练的软件开发人员。这个职业最大的障碍之一就是极其陡峭的初始学习曲线——vibe coding 将这个初始障碍削减到几乎平坦。</p>
<p>Vibe coding 也为经验丰富的开发人员提供了很多东西。我之前谈过 <a href="https://simonwillison.net/2025/Mar/11/using-llms-for-code/">使用 LLM 编写代码很困难</a>——找出什么有效什么是无效的是随着时间的推移建立直觉的问题，沿途有很多隐藏的尖锐边缘和陷阱。</p>
<p>我认为 vibe coding 是我们帮助经验丰富的开发人员建立关于 LLM 能为他们做什么和不能做什么的直觉的最佳工具。我已经发布了超过 <a href="https://tools.simonwillison.net/colophon">80 个实验</a>，我用 vibe coding 构建，一路走来我学到了很多。我会鼓励任何其他开发人员，无论他们的技能水平如何，都尝试同样的事情。</p>
<h4><span id="什么时候可以-vibe-code">什么时候可以 vibe code？</span></h4><p>如果你是一名经验丰富的工程师，这可能对你来说已经很明显了，所以我正在为刚刚开始构建软件的人编写这一部分。</p>
<ul>
<li>项目应该是<strong>低风险</strong>的。考虑一下你正在编写的代码如果有 bug 或安全漏洞会造成多大的危害。如果有人可能受到伤害——声誉受损、金钱损失或更糟的情况？特别是如果你打算构建将由其他人使用的软件！</li>
<li>考虑<strong>安全性</strong>。这是一个非常困难的问题——安全性是一个很大的主题。一些高级说明：<ul>
<li>注意<strong>秘密</strong>——任何看起来类似于密码的东西，例如用于访问在线工具的 API 密钥。如果你的代码涉及秘密，你需要小心不要意外暴露它们，这意味着你需要了解代码是如何工作的！</li>
<li>考虑<strong>数据隐私</strong>。如果你正在构建一个可以访问私有数据的工具——任何你不想在屏幕共享会话中向世界展示的东西——请谨慎行事。可以 vibe code 你粘贴私人信息的个人工具，但你需要非常确定你是否了解数据可能离开你的机器的方式。</li>
</ul>
</li>
<li>做一个<strong>良好的网络公民</strong>。任何向其他平台发出请求的东西都可能增加这些服务的负载（因此增加成本）。这是我喜欢 <a href="https://simonwillison.net/tags/claude-artifacts/">Claude Artifacts</a> 的原因之一——它们的沙箱可以防止意外在其他地方造成伤害。</li>
<li><strong>你的钱在风险中吗</strong>？我见过关于人们对某些 API vibe code 功能而没有账单限制并累积了数千美元费用的恐怖故事。对任何基于使用量收费的东西使用 vibe coding 时要非常小心。</li>
</ul>
<p>如果你要 vibe code 任何可能被其他人使用的东西，我建议在与更有经验的人检查之前进行 vibe check（哈），然后再与世界分享。</p>
<h4><span id="我们如何让-vibe-coding-更好">我们如何让 vibe coding 更好？</span></h4><p>我认为这里有一些有趣的软件设计挑战需要解决。</p>
<p>初学者的安全 vibe coding 从 <a href="https://en.wikipedia.org/wiki/Sandbox_(computer_security)">沙箱</a> 开始。Claude Artifacts 是最早广泛可用的 vibe coding 平台之一，他们的沙箱方法非常棒：代码被限制在锁定的 <code>&lt;iframe&gt;</code> 中运行，只能加载批准的库，不能向其他网站发出任何网络请求。</p>
<p>这使得人们很难搞砸并用他们的项目造成任何伤害。这也极大地限制了这些项目可以做什么——例如，你不能使用 Claude Artifact 项目来访问来自外部 API 的数据，甚至不能构建针对 LLM 运行你自己提示的软件。</p>
<p>其他流行的 vibe coding 工具，如 Cursor（最初是为专业开发人员设计的），安全护栏要少得多。</p>
<p>在这个领域有很多创新空间。我希望看到工具方面的寒武纪大爆发，以帮助人们尽可能高效和安全地构建自己的定制工具。</p>
<h4><span id="去吧去-vibe-code">去吧，去 vibe code</span></h4><p>我真的不想阻止刚接触软件的人尝试 vibe coding。学习任何事情的最好方法就是构建一个项目！</p>
<p>对于经验丰富的程序员来说，这是一种开始培养对 LLM 能和不能做什么的直觉的惊人方式。对于初学者来说，没有比这更好的方法来开阔你对代码本身可能实现的事情的眼界。</p>
<p>但是请不要将 vibe coding 与 LLM 用于代码的所有其他用途混淆。</p>
<hr>
<p><strong>原文链接</strong>: <a href="https://simonwillison.net/2025/Mar/19/vibe-coding/">https://simonwillison.net/2025/Mar/19/vibe-coding/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Programming</tag>
        <tag>Vibe Coding</tag>
        <tag>LLM</tag>
      </tags>
  </entry>
  <entry>
    <title>不是所有 AI 辅助编程都是 Vibe Coding（但 Vibe Coding 很棒）</title>
    <url>/posts/2440005541.html</url>
    <content><![CDATA[<h1><span id="不是所有-ai-辅助编程都是-vibe-coding但-vibe-coding-很棒">不是所有 AI 辅助编程都是 Vibe Coding（但 Vibe Coding 很棒）</span></h1><p>2025 年 3 月 19 日</p>
<p><strong>Vibe coding</strong> 正在流行。这个术语是 Andrej Karpathy 在几周前（2 月 6 日）创造的，此后被《纽约时报》、《Ars Technica》、《卫报》和无数在线讨论所报道。</p>
<p>我担心这个定义已经偏离了其初衷。我看到人们将”vibe coding”这个词应用于所有形式的 AI 辅助编写的代码。我认为这既稀释了这个术语，也给人留下了关于负责任的 AI 辅助编程可能性的错误印象。</p>
<p>Vibe coding <strong>不等于</strong> 在 LLM 帮助下编写代码！</p>
<p>引用 Andrej 的原始推文全文（我添加了强调）：</p>
<blockquote>
<p>有一种新的编码方式，我称之为”vibe coding”，你完全沉浸在氛围中，拥抱指数级增长，<strong>忘记代码甚至存在</strong>。这是可能的，因为 LLM（例如 Cursor Composer w Sonnet）变得太好了。我也只是用 SuperWhisper 与 Composer 对话，所以我几乎不碰键盘。</p>
<p>我要求最愚蠢的事情，比如”将侧边栏的内边距减少一半”，因为我太懒了，不想找到它。我总是”全部接受”，不再阅读 diff。当我收到错误消息时，我只是不加评论地复制粘贴，通常这样就能解决问题。代码增长超出了我通常的理解，我真的得花时间去阅读它。有时 LLM 无法修复 bug，所以我只是变通处理或要求随机更改，直到它消失。</p>
<p><strong>对于周末的临时项目来说还不错，但仍然很有趣</strong>。我在构建一个项目或 webapp，但这真的不是编码——我只是看东西、说话、运行东西、复制粘贴东西，它大多能工作。</p>
</blockquote>
<p>我<strong>非常热爱</strong>这个定义。Andrej 是一位极其有才华和经验的程序员——他根本不需要 AI 帮助。他这样使用 LLM 是因为尝试疯狂的新想法很有趣，而且 LLM 生成代码的速度比即使是最熟练的人类程序员也要快一个数量级。对于低风险项目和原型，为什么不<strong>尽情释放</strong>呢？</p>
<p>当我谈论 vibe coding 时，我的意思是<strong>在不审查 LLM 编写的代码的情况下构建软件</strong>。</p>
<h2><span id="负责任地使用-llm-编写代码不是-vibe-coding">负责任地使用 LLM 编写代码不是 vibe coding</span></h2><p>让我们将这种”忘记代码甚至存在”的方法与专业软件开发人员如何使用 LLM 进行对比。</p>
<p>软件开发人员的工作不仅仅是（或主要是）生成代码和功能。我们需要创建可以证明有效的代码，可以被其他人（和机器）理解，并且可以支持未来的持续开发。</p>
<p>我们需要考虑性能、可访问性、安全性、可维护性、成本效益。软件工程就是关于权衡——我们的工作是通过平衡各种明确和隐含的需求，从数十种潜在解决方案中选择。</p>
<p>我们也<strong>需要</strong>阅读代码。我对生产级 AI 辅助编程的黄金法则是：如果我不能向其他人准确解释代码的作用，我就不会将其提交到我的仓库。</p>
<p>如果 LLM 为你编写了代码，然后你审查了它，进行了彻底的测试，并确保你可以向其他人解释它是如何工作的，那就不是 vibe coding，而是软件开发。使用 LLM 来支持该活动是无关紧要的。</p>
<p>我在《我如何使用 LLM 帮助我编写代码》中广泛撰写了我自己的流程。Vibe coding 只描述了我方法的一小部分。</p>
<h2><span id="让我们不要失去对-vibe-coding-特别之处的关注">让我们不要失去对 vibe coding 特别之处的关注</span></h2><p>我也不希望”vibe coding”成为一个与不负责任的 AI 辅助编程同义的负面术语。这种奇怪的编程新形态为世界提供了很多东西！</p>
<p>我相信<strong>每个人都应该有能力</strong>用计算机自动化他们生活中的繁琐任务。你不需要计算机科学学位或编程训练营就能让计算机为你执行非常具体的任务。</p>
<p>如果 vibe coding 让数百万新人能够构建自己的定制工具，我再高兴不过了。</p>
<p>其中一些人会被编程 bug 咬到，然后成为熟练的软件开发人员。这个职业最大的障碍之一就是极其陡峭的初始学习曲线——vibe coding 将这个初始障碍削减到几乎平坦。</p>
<p>Vibe coding 也为经验丰富的开发人员提供了很多东西。我之前谈过<strong>使用 LLM 编写代码很困难</strong>——找出什么有效什么是无效的是随着时间的推移建立直觉的问题，沿途有很多隐藏的尖锐边缘和陷阱。</p>
<p>我认为 vibe coding 是我们帮助经验丰富的开发人员建立关于 LLM 能为他们做什么和不能做什么的直觉的最佳工具。我已经发布了超过 <strong>80 个我用 vibe coding 构建的实验</strong>，我一路走来学到了很多。我会鼓励任何其他开发人员，无论他们的技能水平如何，都尝试同样的事情。</p>
<h2><span id="什么时候可以-vibe-code">什么时候可以 vibe code？</span></h2><p>如果你是一名经验丰富的工程师，这可能对你来说已经很明显了，所以我正在为刚刚开始构建软件的人编写这一部分。</p>
<ol>
<li><p><strong>项目应该是低风险的</strong>。考虑一下你正在编写的代码如果有 bug 或安全漏洞会造成多大的危害。如果有人可能受到伤害——声誉受损、金钱损失或更糟的情况？特别是如果你打算构建将由其他人使用的软件！</p>
</li>
<li><p><strong>考虑安全性</strong>。这是一个非常困难的问题——安全性是一个很大的主题。一些高级说明：</p>
<ul>
<li>注意<strong>秘密</strong>——任何看起来类似于密码的东西，例如用于访问在线工具的 API 密钥。如果你的代码涉及秘密，你需要小心不要意外暴露它们，这意味着你需要了解代码是如何工作的！</li>
<li>考虑<strong>数据隐私</strong>。如果你正在构建一个可以访问私有数据的工具——任何你不想在屏幕共享会话中向世界展示的东西——请谨慎行事。可以 vibe code 你粘贴私人信息的个人工具，但你需要非常确定你是否了解数据可能离开你的机器的方式。</li>
</ul>
</li>
<li><p><strong>做一个良好的网络公民</strong>。任何向其他平台发出请求的东西都可能增加这些服务的负载（因此增加成本）。这是我喜欢 <strong>Claude Artifacts</strong> 的原因之一——它们的沙箱可以防止意外在其他地方造成伤害。</p>
</li>
<li><p><strong>你的钱在风险中吗</strong>？我见过关于人们对某些 API vibe code 功能而没有账单限制并累积了数千美元费用的恐怖故事。对任何基于使用量收费的东西使用 vibe coding 时要非常小心。</p>
</li>
</ol>
<p>如果你要 vibe code 任何可能被其他人使用的东西，我建议在与更有经验的人检查之前进行 vibe check（哈），然后再与世界分享。</p>
<h2><span id="我们如何让-vibe-coding-更好">我们如何让 vibe coding 更好？</span></h2><p>我认为这里有一些有趣的软件设计挑战需要解决。</p>
<p>对于完全初学者的安全 vibe coding 从<strong>沙箱</strong>开始。Claude Artifacts 是最早广泛可用的 vibe coding 平台之一，它们的沙箱方法非常棒：代码被限制在锁定的 <code>&lt;iframe&gt;</code> 中运行，只能加载批准的库，不能向其他网站发出任何网络请求。</p>
<p>这使得人们很难搞砸并用他们的项目造成任何伤害。这也极大地限制了这些项目可以做什么——例如，你不能使用 Claude Artifact 项目来访问来自外部 API 的数据，甚至不能构建针对 LLM 运行你自己提示的软件。</p>
<p>其他流行的 vibe coding 工具，如 Cursor（最初是为专业开发人员设计的），安全护栏要少得多。</p>
<p>这个领域有很多创新空间。我希望看到更多工具来帮助人们尽可能安全和高效地构建自己的定制工具。</p>
<hr>
<p><strong>原文</strong>: <a href="https://simonwillison.net/2025/Mar/19/vibe-coding/">https://simonwillison.net/2025/Mar/19/vibe-coding/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI - Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>我如何使用 LLM 帮助我编写代码</title>
    <url>/posts/3181136311.html</url>
    <content><![CDATA[<h1><span id="我如何使用-llm-帮助我编写代码">我如何使用 LLM 帮助我编写代码</span></h1><p>2025 年 3 月 11 日</p>
<p>关于 <a href="https://simonwillison.net/tags/ai-assisted-programming/">使用大型语言模型帮助编写代码</a> 的在线讨论不可避免地会产生来自经验令人失望的开发者的评论。他们经常问他们做错了什么——为什么有些人报告如此好的结果，而他们自己的实验却证明不足？</p>
<p>使用 LLM 编写代码是<strong>困难的</strong>和<strong>反直觉的</strong>。需要付出大量努力才能找出以这种方式使用它们的尖锐和柔和的边缘，几乎没有宝贵的指导来帮助人们找出如何最好地应用它们。</p>
<p>如果有人告诉你用 LLM 编码很<strong>容易</strong>，他们（可能无意地）误导了你。他们可能偶然发现了有效的模式，但这些模式并不是对每个人都自然而然地出现。</p>
<p>我使用 LLM 编写代码已经两年多了，取得了很好的结果。这是我尝试将一些经验和直觉转移给你的尝试。</p>
<h4><span id="设定合理的期望">设定合理的期望</span></h4><p>忽略”AGI”炒作——LLM 仍然是花哨的自动完成。它们所做的只是预测一系列 token——但事实证明，编写代码主要是关于以正确的顺序将 token 串在一起，因此如果你将它们指向正确的方向，它们可以<strong>非常有用</strong>。</p>
<p>如果你假设这项技术将完美地实现你的项目，而你不需要运用任何自己的技能，你很快就会失望。</p>
<p>相反，使用它们来<strong>增强</strong>你的能力。我目前最喜欢的心理模型是将它们视为一个过度自信的结对编程助手，他们查找东西的速度快如闪电，可以立即提出相关示例，并且可以毫无怨言地执行繁琐的任务。</p>
<p><strong>过度自信</strong>很重要。他们绝对会犯错误——有时是微妙的，有时是巨大的。这些错误可能是<strong>深刻非人类的</strong>——如果一个人类合作者幻觉了一个不存在的库或方法，你会立即失去对他们的信任。</p>
<p><strong>不要陷入拟人化 LLM 的陷阱，并假设会使人类信誉扫地的失败也应该以同样的方式使机器信誉扫地。</strong></p>
<p>与 LLM 一起工作时，你经常会发现它们无法做的事情。记下这些——它们是有用的教训！它们也是有价值的例子，可以为将来储存——新模型强大之处的标志是当它为以前模型无法处理的任务产生可用结果时。</p>
<h4><span id="考虑训练截止日期">考虑训练截止日期</span></h4><p>任何模型的一个关键特征是其<strong>训练截止日期</strong>。这是他们接受训练的数据停止收集的日期。对于 OpenAI 的模型，这通常是 2023 年 10 月或 2024 年 5 月。其他提供商可能有更近的日期。</p>
<p>这对代码<strong>极其重要</strong>，因为它会影响他们熟悉的库。如果你使用的库自 2023 年 10 月以来发生了重大突破，一些 OpenAI 模型可能不知道！</p>
<p>我从 LLM 获得了足够的价值，以至于我现在在选择库时故意考虑这一点——我坚持使用稳定性好且足够流行的库，这样它们的许多示例就会进入训练数据。我喜欢应用 <a href="https://boringtechnology.club/">无聊技术</a> 的原则——在你的项目的独特卖点上创新，在其他方面坚持经过测试的解决方案。</p>
<p>LLM 仍然可以帮助你使用存在于其训练数据之外的库，但你需要投入更多工作——你需要在提示中向他们提供有关如何使用这些库的最新示例。</p>
<p>这带来了与 LLM 工作时需要了解的最重要的事情：</p>
<h4><span id="上下文为王">上下文为王</span></h4><p>从 LLM 获得好结果的大部分技巧都归结为管理其上下文——作为当前对话一部分的文本。</p>
<p>这个上下文不仅仅是你输入给它的提示：成功的 LLM 交互通常采用对话的形式，上下文由来自你的每条消息 <em>和</em> 来自 LLM 的每条回复组成，这些存在于当前对话线程中。</p>
<p>当你开始新对话时，你会将该上下文重置为零。这很重要，因为对于已经停止有用的对话的修复通常是清空重新开始。</p>
<p>一些 LLM 编码工具超越了仅仅是对话。例如，Claude Projects 允许你用相当大量的文本预填充上下文——包括最近 <a href="https://support.anthropic.com/en/articles/10167454-using-the-github-integration">直接从 GitHub 导入代码</a> 的能力，我经常使用。</p>
<p>像 Cursor 和 VS Code Copilot 这样的工具会自动包含来自当前编辑器会话和文件布局的上下文，你有时可以使用像 <a href="https://docs.cursor.com/context/@-symbols/overview">Cursor 的 @commands</a> 这样的机制来拉入额外的文件或文档。</p>
<p>我主要直接使用 <a href="https://chatgpt.com/">ChatGPT</a> 和 <a href="https://claude.ai/">Claude</a> Web 或应用程序界面的原因之一是，它使我更容易理解到底是什么进入了上下文。对我来说模糊上下文的 LLM 工具<strong>效果较差</strong>。</p>
<p>你可以利用之前的回复也是上下文的一部分这一事实。对于复杂的编码任务，尝试让 LLM 先写一个更简单的版本，检查它是否有效，然后迭代构建更复杂的实现。</p>
<p>我经常通过转储现有代码来开始新聊天，以种子化该上下文，然后与 LLM 一起工作以某种方式修改它。</p>
<p>我最喜欢的代码提示技术之一是放入几个与我想要构建的东西相关的完整示例，然后提示 LLM 使用它们作为新项目的灵感。当我 <a href="https://simonwillison.net/2024/Mar/30/ocr-pdfs-images/">描述我的 JavaScript OCR 应用程序</a> 时，我详细写了这一点，该应用程序结合了 Tesseract.js 和 PDF.js——这两个库我过去使用过，并且可以在提示中提供工作示例。</p>
<h4><span id="询问它们选项">询问它们选项</span></h4><p>我的大多数项目都以一些开放性问题开始：我试图做的事情是否可行？我可以实现它的潜在方式有哪些？这些选项中哪些是<strong>最好的</strong>？</p>
<p>我在这个初始研究阶段使用 LLM。</p>
<p>我会使用像”Rust 中 HTTP 库的选项是什么？包括使用示例”这样的提示——或者”JavaScript 中一些有用的拖放库是什么？为我构建一个演示每个库的工件”（对 Claude）。</p>
<p>训练截止日期在这里是相关的，因为它意味着不会建议更新的库。通常没关系——我不想要最新的，我想要最稳定的，并且已经存在足够长的时间，bug 已经被解决。</p>
<p>如果我要使用一些更新的东西，我会自己在 LLM 世界之外做这个研究。</p>
<p>开始任何项目的最好方法是从一个证明该项目关键要求可以满足的原型开始。我经常发现 LLM 可以让我在坐在笔记本电脑前的几分钟内——有时甚至是在手机上工作时——达到那个可工作的原型。</p>
<h4><span id="准确告诉它们要做什么">准确告诉它们要做什么</span></h4><p>在完成初始研究后，我戏剧性地改变了模式。对于生产代码，我的 LLM 使用更加独裁：我将其视为数字实习生，根据我的详细说明为我输入代码。</p>
<p>这是一个最近的示例：</p>
<blockquote>
<p>使用 asyncio httpx 编写一个具有以下签名的 Python 函数：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">download_db</span>(<span class="params">url, max_size_bytes=<span class="number">5</span> * <span class="number">1025</span> * <span class="number">1025</span></span>): -&gt; pathlib.Path</span><br></pre></td></tr></table></figure>

<p>给定一个 URL，这会将数据库下载到临时目录并返回它的路径。但它会在开始流式传输该数据时检查内容长度标头，如果超过限制，则抛出错误。当下载完成时，它使用 <code>sqlite3.connect(...)</code> 然后运行 <code>PRAGMA quick_check</code> 来确认 SQLite 数据有效——如果无效则抛出错误。最后，如果内容长度标头对我们撒谎——如果它说 2MB 但我们下载了 3MB——我们一注意到这个问题就会抛出错误。</p>
</blockquote>
<p>我可以自己编写这个函数，但这需要我大部分十五分钟来查找所有细节并使代码正常工作。Claude <a href="https://gist.github.com/simonw/5aed8bd87016c77465c23e0dc4563ec9">在 15 秒内</a> 就完成了。</p>
<p>我发现 LLM 对像我在这里使用的函数签名反应非常好。我充当函数设计者，LLM 负责根据我的规范构建主体。</p>
<p>我经常随后跟上”现在使用 pytest 为我编写测试”。同样，我指定我选择的技术——我希望 LLM 节省我输入已经在我脑海中的代码的时间。</p>
<p>如果你对此的反应是”当然，输入代码肯定比输入英文说明更快”，我只能告诉你，对我来说真的不再是这样了。代码需要是正确的。英文有巨大的捷径空间，以及模糊性和拼写错误，以及说”使用那个流行的 HTTP 库”这样的事情，如果你不能从头顶记住名称。</p>
<p>好的编码 LLM 非常擅长填补空白。它们也比我懒得多——它们会记得捕获可能的异常，添加准确的文档字符串，并用相关类型注释代码。</p>
<h4><span id="你必须测试它们编写的东西">你必须测试它们编写的东西！</span></h4><p>我 <a href="https://simonwillison.net/2025/Mar/2/hallucinations-in-code/#qa">上周详细</a> 写过这个：你绝对不能外包给机器的事情是测试代码是否实际有效。</p>
<p>你作为软件开发人员的责任是交付可工作的系统。如果你没有看到它运行，它就不是一个可工作的系统。你需要投资加强这些手动 QA 习惯。</p>
<p>这可能并不迷人，但它一直是交付好代码的关键部分，无论是否涉及 LLM。</p>
<h4><span id="记住这是一个对话">记住这是一个对话</span></h4><p>如果我不喜欢 LLM 编写的东西，它们永远不会抱怨被要求重构它！”将重复代码分解到函数中”，”使用字符串操作方法而不是正则表达式”，甚至”更好地编写那个！”——LLM 首次产生的代码很少是最终实现，但它们可以为你重新输入数十次，而不会变得沮丧或无聊。</p>
<p>偶尔我会从我的第一个提示中获得很好的结果——我练习得越多，这种情况就越频繁——但我期望需要至少几次跟进。</p>
<p>我经常想这是否是人们 missing 的关键技巧之一——糟糕的初始结果不是失败，而是将模型推向你实际想要的东西的起点。</p>
<p>越来越多的 LLM 编码工具现在有能力 <em>为你运行该代码</em>。我对其中一些有点谨慎，因为错误的命令可能造成真正的损害，所以我倾向于坚持那些在安全沙箱中运行代码的工具。我目前最喜欢的是：</p>
<ul>
<li><strong>ChatGPT Code Interpreter</strong>，ChatGPT 可以编写然后在由 OpenAI 管理的 Kubernetes 沙箱 VM 中直接执行 Python 代码。这完全安全——它甚至不能发出出站网络连接，所以真正能发生的只是临时文件系统被破坏然后重置。</li>
<li><strong>Claude Artifacts</strong>，Claude 可以为你构建一个完整的 HTML+JavaScript+CSS Web 应用程序，在 Claude 界面中显示。这个 Web 应用程序显示在一个<strong>非常</strong>锁定的 iframe 沙箱中，大大限制了它能做什么，但防止了意外泄露你的私人 Claude 数据等问题。</li>
<li><strong>ChatGPT Canvas</strong> 是一个较新的 ChatGPT 功能，具有与 Claude Artifacts 类似的功能。我还没有足够探索这个。</li>
</ul>
<p>如果你愿意活得更有风险一点：</p>
<ul>
<li><strong><a href="https://www.cursor.com/">Cursor</a></strong> 有一个可以这样做的”Agent”功能，<strong><a href="https://codeium.com/windsurf">Windsurf</a></strong> 和越来越多的其他编辑器也是如此。我还没有花足够的时间与这些来提出建议。</li>
<li><strong><a href="https://aider.chat/">Aider</a></strong> 是这些模式领先的开源实现，是一个很好的 <a href="https://en.wikipedia.org/wiki/Eating_your_own_dog_food">dogfooding</a> 示例——最近的 Aider 版本有 <a href="https://aider.chat/HISTORY.html">80%+ 由</a> Aider 自己编写。</li>
<li><strong><a href="https://docs.anthropic.com/en/docs/agents-and-tools/claude-code/overview">Claude Code</a></strong> 是 Anthropic 在这个空间的新进入者。我很快就会提供使用该工具的详细描述。</li>
</ul>
<p>这个循环运行代码的模式是如此强大，以至于我主要根据它们是否可以安全地运行和迭代我的代码来选择我的核心 LLM 编码工具。</p>
<h4><span id="vibe-编码是一个很好的学习方式">Vibe 编码是一个很好的学习方式</span></h4><p>Andrej Karpathy <a href="https://simonwillison.net/2025/Feb/6/andrej-karpathy/">创造了这个术语</a> vibe 编码刚过一个多月，它已经坚持下来了：</p>
<blockquote>
<p>有一种新的编码方式，我称之为”vibe 编码”，你完全沉浸在氛围中，拥抱指数级增长，<strong>忘记代码甚至存在</strong>。[…] 我要求最愚蠢的事情，比如”将侧边栏的内边距减少一半”，因为我太懒了，不想找到它。我总是”全部接受”，不再阅读 diff。当我收到错误消息时，我只是不加评论地复制粘贴，通常这样就能解决问题。</p>
</blockquote>
<p>Andrej 建议这对”临时周末项目来说还不错”。这也是一个<strong>探索这些模型能力的极好方式</strong>——而且真的很有趣。</p>
<p>学习 LLM 的最好方法是玩弄它们。向它们抛出荒谬的想法并进行 vibe 编码，直到它们几乎有点工作，这确实是加速你建立对什么有效和什么无效的直觉的有用方式。</p>
<p>我在 Andrej 给它命名之前就已经在进行 vibe 编码了！我的 <a href="https://github.com/simonw/tools">simonw&#x2F;tools</a> GitHub 仓库有 77 个 HTML+JavaScript 应用程序和 6 个 Python 应用程序，每一个都是通过提示 LLM 构建的。我从构建这个集合中学到了<strong>太多</strong>，我以每周几个新原型的速度向其中添加内容。</p>
<p>你可以直接在 <a href="https://tools.simonwillison.net/">tools.simonwillison.net</a> 上尝试我的大多数——这是仓库的 GitHub Pages 发布版本。我在 10 月的 <a href="https://simonwillison.net/2024/Oct/21/claude-artifacts/">本周我用 Claude Artifacts 构建的一切</a> 中更详细地记录了其中一些。</p>
<hr>
<p><strong>原文链接</strong>: <a href="https://simonwillison.net/2025/Mar/11/using-llms-for-code/">https://simonwillison.net/2025/Mar/11/using-llms-for-code/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Programming</tag>
        <tag>Coding</tag>
        <tag>LLM</tag>
      </tags>
  </entry>
  <entry>
    <title>交互式解释</title>
    <url>/posts/2083823209.html</url>
    <content><![CDATA[<h1><span id="交互式解释">交互式解释</span></h1><p>当我们失去对 agent 编写的代码如何工作的追踪时，我们就承担了<strong>认知债务</strong>。</p>
<p>对于很多事情来说这并不重要：如果代码从数据库获取一些数据并作为 JSON 输出，实现细节可能简单到我们不需要关心。我们可以尝试新功能并对其工作原理做出非常可靠的猜测，然后浏览代码确认。</p>
<p>但通常细节确实很重要。如果我们应用程序的核心变成了一个我们不完全理解的黑盒，我们就不再能有信心地推理它，这使得规划新功能变得更困难，最终像累积的技术债务一样减缓我们的进度。</p>
<p>如何偿还认知债务？通过提高我们对代码如何工作的理解。</p>
<p>我最喜欢的做法之一是构建<strong>交互式解释</strong>。</p>
<h2><span id="理解词云">理解词云</span></h2><p>在《一个 AI agent 编码怀疑论者尝试 AI agent 编码，过度详细》中，Max Woolf 提到用以下提示测试 LLM 的 Rust 能力：创建一个 Rust 应用，可以根据长输入文本创建”词云”数据可视化。</p>
<p>这激发了我的想象力：我一直想知道词云是如何工作的，所以我启动了一个异步研究项目——这里有初始提示，这里有代码和报告——来探索这个想法。</p>
<p>这非常有效：Claude Code for web 为我构建了一个 Rust CLI 工具，可以生成像这样的图像：</p>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/images/master/agentic-patterns/61fedba8/wordcloud.png" alt="词云示例"></p>
<p>但它是如何实际工作的呢？</p>
<p>Claude 的报告说它使用”阿基米德螺旋布局，每个单词带有随机角度偏移，以获得自然的外观”。这对我没有太大帮助！</p>
<p>我请求了一个代码库的线性演示，这帮助我更详细地理解了 Rust 代码——这是那个演示（和提示）。这帮助我理解了 Rust 代码的结构，但我仍然没有直观理解”阿基米德螺旋布局”部分是如何实际工作的。</p>
<p>所以我请求了一个动画解释。我是这样做的：将一个链接到现有的 walkthrough.md 文档粘贴到 Claude Code 会话中，以及以下提示：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">创建一个交互式动画，展示词云算法是如何工作的。</span><br><span class="line">使用 HTML5 canvas 和 JavaScript。</span><br><span class="line">展示每个单词如何尝试放置在页面上，</span><br><span class="line">通过显示一个框，检查该框是否与现有单词相交。</span><br><span class="line">如果是，它继续尝试找到一个好的位置，</span><br><span class="line">从中心向外以螺旋方式移动。</span><br></pre></td></tr></table></figure>

<p>你可以在这里玩结果。这是一个动画 GIF 演示：</p>
<p><img src="https://raw.githubusercontent.com/chenzhenyang/images/master/agentic-patterns/61fedba8/animated-word-cloud-demo.gif" alt="动画演示"></p>
<p>这是使用 Claude Opus 4.6，事实证明它在构建解释动画方面相当有品味。</p>
<p>如果你仔细观察动画，你可以看到对于每个单词，它尝试通过显示一个框将其放置在页面上的某个位置，运行检查该框是否与现有单词相交。如果是，它继续尝试找到一个好的位置，从中心向外以螺旋方式移动。</p>
<p>我发现这个动画真的帮助我理解了算法的工作方式。</p>
<p>我一直是动画和交互式界面的粉丝，用来帮助解释不同的概念。一个好的 coding agent 可以按需生成这些来帮助解释代码——它自己的代码或他人编写的代码。</p>
<hr>
<p><strong>原文</strong>: <a href="https://simonwillison.net/guides/agentic-engineering-patterns/interactive-explanations/">https://simonwillison.net/guides/agentic-engineering-patterns/interactive-explanations/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI - Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 Agent 编码时评估内部质量</title>
    <url>/posts/3160165449.html</url>
    <content><![CDATA[<p><em>原文来源：Martin Fowler</em></p>
<p><em>抓取时间：2026-03-11 20:40:10</em></p>
<hr>
<h1><span id="使用-agent-编码时评估内部质量">使用 Agent 编码时评估内部质量</span></h1><p>Erik 是一位经验丰富的技术专家和软件工程师。他总是对新兴技术和软件卓越感兴趣。</p>
<p>本文是”探索生成式 AI（Exploring Gen AI）”系列的一部分。该系列记录了 Thoughtworks 技术专家探索将生成式 AI（gen ai）技术用于软件开发的过程。</p>
<p>2026 年 1 月 27 日</p>
<p>不乏有关 AI 编码助手、Agent 和 Agent 集群如何在短时间内编写大量代码的报告，据说这些代码实现了所需的功能。在这种情况下，人们很少谈论非功能性需求（non-functional requirements），如性能或安全性，也许因为这在作者拥有的许多用例中不是问题。而人们评估 Agent 生成代码质量的情况更为罕见。然而，我认为内部质量对于开发以可持续的速度持续多年至关重要，而不是在其自身重量下崩溃。</p>
<p>所以，让我们仔细看看 AI 工具在<strong>内部代码质量</strong>方面的表现如何。我们将在 Agent 的帮助下向现有应用程序添加一个功能，并观察过程中发生了什么。当然，这”仅仅”是一个轶事。这份备忘录绝非一项研究。同时，我们将看到的大部分内容都属于模式，至少根据我的经验可以推断。</p>
<h2><span id="我们正在实现的功能">我们正在实现的功能</span></h2><p>我们将使用<strong>CCMenu</strong>的代码库，这是一个 Mac 应用程序，在 Mac 菜单栏中显示 CI&#x2F;CD 构建的状态。这给任务增加了一定的难度，因为 Mac 应用程序是用 Swift 编写的，这是一种常见的语言，但不如 JavaScript 或 Python 那么常见。这也是一种现代编程语言，具有复杂的语法和类型系统，比 JavaScript 或 Python 需要更高的精度。</p>
<p>CCMenu 定期通过调用构建服务器的 API 来获取状态。它目前支持使用 Jenkins 等实现的遗留协议的服务器，并支持 GitHub Actions 工作流。目前不支持的请求最多的服务器是 GitLab。所以，这就是我们的功能：我们将在 CCMenu 中实现对 GitLab 的支持。</p>
<h2><span id="api-包装器">API 包装器</span></h2><p>GitHub 提供<strong>GitHub Actions API</strong>，稳定且文档齐全。GitLab 有<strong>GitLab API</strong>，文档也很齐全。鉴于问题空间的性质，它们在语义上非常相似。但它们并不相同，我们将稍后看到这对任务的影响。</p>
<p>在内部，CCMenu 有三个 GitHub 特定的文件来从 API 获取构建状态：一个 feed 阅读器、一个响应解析器，以及一个包含包装 GitHub API 的 Swift 函数的文件，包括如下函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">func requestForAllPublicRepositories(user: String, token: String?) -&gt; URLRequest</span><br><span class="line">  func requestForAllPrivateRepositories(token: String) -&gt; URLRequest</span><br><span class="line">  func requestForWorkflows(owner: String, repository: String, token: String?) -&gt; URLRequest</span><br></pre></td></tr></table></figure>

<p>这些函数返回 <code>URLRequest</code> 对象，这是 Swift SDK 的一部分，用于进行实际的网络请求。因为这些函数在结构上非常相似，它们将 <code>URLRequest</code> 对象的构建委托给一个共享的内部函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">URLRequest</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">func makeRequest(method: String = &quot;GET&quot;, baseUrl: URL, path: String,</span><br><span class="line">        params: Dictionary&lt;String, String&gt; = [:], token: String? = nil) -&gt; URLRequest</span><br></pre></td></tr></table></figure>

<p>如果你不熟悉 Swift 也没关系，只要你能识别参数及其类型就可以了。</p>
<h2><span id="可选-token">可选 Token</span></h2><p>接下来，我们应该更详细地看看 <code>token</code> 参数。对 API 的请求可以进行身份验证。它们不必进行身份验证，但可以进行身份验证。这允许像 CCMenu 这样的应用程序访问仅限于某些用户的信息。对于大多数 API（包括 GitHub 和 GitLab），token 只是一个需要在 HTTP 头中传递的长字符串。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">token</span><br></pre></td></tr></table></figure>

<p>在其实现中，CCMenu 对 token 使用可选字符串，在 Swift 中用类型后面的问号表示，这里是 <code>String?</code>。这是惯用用法，Swift 强制此类可选值的接收者以安全的方式处理可选性，避免经典的空指针问题。还有一些特殊的语言功能使这更容易。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">String?</span><br></pre></td></tr></table></figure>

<p>有些函数在未身份验证的上下文中是无意义的，比如 <code>requestForAllPrivateRepositories</code>。这些将 token 声明为非可选的，向调用者发出必须提供 token 的信号。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">requestForAllPrivateRepositories</span><br></pre></td></tr></table></figure>

<h2><span id="开始吧">开始吧</span></h2><p>我在夏天用 Windsurf 和 Sonnet 3.5，最近又用 Claude Code 和 Sonnet 4.5 尝试过这个实验几次。方法保持相似：将任务分解为更小的块。对于每个块，我先让 Windsurf 制定计划，然后再要求实现。使用 Claude Code 时，我直接进行实现，依靠其内部规划；当某些事情走向错误方向时，则依靠 Git。</p>
<p>作为第一步，我或多或少原封不动地问 Agent：”基于 GitHub 的 API、feed 阅读器和响应解析器文件，为 GitLab 实现相同的功能。只编写这三个文件的等效版本。不要对 UI 进行更改。”</p>
<p>这听起来是一个合理的要求，大体上也是如此。即使是能力较弱的模型 Windsurf 也注意到了关键差异并处理了它们，例如它认识到 GitHub 称为 repository 的东西在 GitLab 中是 project；它看到了 JSON 响应的差异，GitLab 在顶层返回运行数组，而 GitHub 将此数组作为顶层对象中的属性。</p>
<p>在这个阶段我自己还没有查看 GitLab API 文档，只是粗略扫描生成的代码，一切看起来都相当不错，代码可以编译，甚至复杂的函数类型也生成正确，或者真的是这样吗？</p>
<h2><span id="第一个意外">第一个意外</span></h2><p>在下一步中，我让 Agent 实现 UI 以添加新的 pipelines&#x2F;workflows。我故意告诉它暂时不要担心身份验证，只为公开访问的信息实现流程。关于这一步的讨论也许适合另一份备忘录，但新代码需要以某种方式承认 token 可能在未来存在：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var apiToken: String? = nil</span><br></pre></td></tr></table></figure>

<p>然后它可以在调用包装函数时使用这个变量：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">let req = GitLabAPI.requestForGroupProjects(group: name, token: apiToken)</span><br><span class="line">  var projects = await fetchProjects(request: req)</span><br></pre></td></tr></table></figure>

<p><code>apiToken</code> 变量被正确声明为可选 String，目前初始化为 <code>nil</code>。稍后，根据用户是否决定登录，一些代码可以从其他地方检索 token。这段代码导致了第一个编译器错误：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">apiToken</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">nil</span><br></pre></td></tr></table></figure>

<p>这是怎么回事？事实证明，第一步中的 API 包装器代码有一个微妙的问题：它在<strong>所有</strong>包装函数中将 token 声明为非可选的，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">func requestForGroupProjects(group: String, token: String) -&gt; URLRequest</span><br></pre></td></tr></table></figure>

<p>底层的 <code>makeRequest</code> 函数，出于某种原因，被正确创建了，token 被声明为可选的。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">makeRequest</span><br></pre></td></tr></table></figure>

<p>代码可以编译，因为函数的编写方式，包装函数肯定有一个字符串，这当然可以传递给接受可选字符串的函数，即参数可能是字符串或空（nil）。但是现在，在上面的代码中，我们有一个可选字符串，这不能传递给需要（确定的）字符串的函数。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">nil</span><br></pre></td></tr></table></figure>

<h2><span id="氛围修复vibe-fix">氛围修复（vibe fix）</span></h2><p>出于懒惰，我只是将错误消息复制粘贴回 Windsurf。（在任何非 Xcode 的环境中构建 Swift 应用完全是另一回事，我记得有一次用 Cline 的实验，它在添加和移除显式导入之间交替，每次迭代大约 20 美分。）AI 为此问题提出的修复有效：它更改了调用点，使用 Swift 的 <code>??</code> 运算符插入了一个空字符串作为没有 token 时的默认值。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">??</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">let req = GitLabAPI.requestForGroupProjects(group: name, token: apiToken ?? &quot;&quot;)</span><br><span class="line">  var projects = await fetchProjects(request: req)</span><br></pre></td></tr></table></figure>

<p>这可以编译，而且有点用：如果没有 token，则替换为空字符串，这意味着传递给函数的参数要么是 token，要么是空字符串，它始终是一个字符串，从不是 <code>nil</code>。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">nil</span><br></pre></td></tr></table></figure>

<p>那么，有什么问题？将 token 声明为可选的整个目的是表明 token 是可选的。AI 忽略了这一点并引入了新的语义：空字符串现在表示没有可用的 token。这是：</p>
<ul>
<li>不惯用的</li>
<li>不自文档化的</li>
<li>不被 Swift 的类型系统支持</li>
</ul>
<p>这还需要在调用此函数的每个地方进行更改。</p>
<h2><span id="真正的修复">真正的修复</span></h2><p>当然，Agent 应该做的是简单地更改包装函数的函数声明，使 token 变为可选。有了这个更改，一切都能按预期工作，语义保持完整，更改仅限于在函数参数的类型中添加一个 <code>?</code>，而不是在代码中到处喷洒 <code>?? &quot;&quot;</code>。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">?</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">?? &quot;&quot;</span><br></pre></td></tr></table></figure>

<h2><span id="这真的重要吗">这真的重要吗？</span></h2><p>你可能会问我是否在这里吹毛求疵。我不这么认为。我认为这是一个清晰的例子，如果让 AI Agent 自行其是，它会将代码库变得更糟，需要一个有经验的开发者来注意到问题并指导 Agent 进行正确的实现。</p>
<p>此外，这只是我遇到的众多例子之一。在某个时候，Agent 想引入一个完全不必要的缓存，当然，它无法解释为什么甚至建议这个缓存。</p>
<p>它也没有意识到 GitHub 中的 user&#x2F;org 重叠在 GitLab 中不存在，并去实现一些复杂的逻辑来处理一个不存在的问题。这需要不止一次地引导 Agent 查看文档中的正确位置，才能说服它放弃坚持需要这个逻辑。</p>
<p>它还”忘记”使用现有函数来构建 URL，在多个地方复制此类逻辑，通常没有实现所有功能，例如使用 macOS 上的默认系统覆盖基础 URL 以进行测试的选项。</p>
<p>所以，在这些情况下，生成的代码是有效的。它实现了所需的功能。但新代码也会添加完全不必要的复杂性，并且错过了不明显的功能，降低了代码库的质量并引入了微妙的问题。</p>
<p>如果在大型软件系统上工作教会了我一件事，那就是投资软件的内部质量、代码库的质量是一项值得的投资。不要被技术债务压垮。人类和 Agent 发现与复杂的代码库合作更加困难。然而，如果没有仔细的监督，AI Agent 似乎有强烈的倾向引入技术债务，使未来的开发对人类和 Agent 都更加困难。</p>
<h2><span id="还有一件事">还有一件事</span></h2><p>如果可能，CCMenu 会显示触发构建的人员&#x2F;actor 的头像。在 GitHub 中，头像 URL 是构建状态 API 调用响应的一部分。GitLab 有更”干净”、更 RESTful 的设计，在构建响应中不包含额外的用户信息。头像 URL 必须通过对 <code>/user</code> 端点的单独 API 调用来检索。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/user</span><br></pre></td></tr></table></figure>

<p>Windsurf 和 Claude Code 都在这方面严重绊倒了。我记得一次漫长的对话，Claude Code 想说服我 URL 在响应中。（它可能搞混了，因为多个端点在同一页文档上描述。）最后，我发现没有 Agent 支持更容易实现该功能。</p>
<h2><span id="我的结论">我的结论</span></h2><p>在夏天的实验期间，我持观望态度。Windsurf &#x2F; Sonnet 3.5 组合确实加快了编写代码的速度，但需要仔细规划提示，我不得不在 Windsurf 和 Xcode 之间来回切换（用于构建、运行测试和调试），这总是让人感到有些迷失方向，很快就会疲劳。生成代码的质量存在重大问题，Agent 有陷入尝试修复问题的倾向。所以，总的来说，我觉得使用 Agent 并没有给我带来太多收获。我用我喜欢做的事情（编写代码）换取了监督一个有编写草率代码倾向的 AI。</p>
<p>使用 Claude Code 和 Sonnet 4.5 的情况有所不同。它需要更少的提示，代码质量更好。这绝不是高质量的代码，但更好，需要更少的返工和更少的提示来提高质量。此外，在终端窗口中与 Xcode 一起运行与 Claude Code 的对话感觉比在两个 IDE 之间切换更自然。对我来说，这已经足够倾斜天平，让我定期使用 Claude Code。</p>
<hr>
<p><strong>最新文章（3 月 4 日）：</strong><br>Humans and Agents in Software Engineering Loops</p>
<p><strong>上一篇文章：</strong><br>理解规格驱动开发：Kiro、spec-kit 和 Tessl</p>
<p><strong>下一篇文章：</strong><br>Context Engineering for Coding Agents</p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>先运行测试</title>
    <url>/posts/3536012328.html</url>
    <content><![CDATA[<h1><span id="先运行测试">先运行测试</span></h1><p>在使用 coding agent 时，自动化测试不再是可选的。过去不写测试的借口——认为它们耗时且昂贵，在代码库快速变化时需要不断重写——在 agent 时代不再成立，因为 agent 可以在几分钟内完成测试的编写和调整。</p>
<p>测试对于确保 AI 生成的代码按预期工作<strong>至关重要</strong>。如果代码从未被执行过，那么它在部署到生产环境时能否工作纯粹靠运气。</p>
<p>测试也是帮助 agent 快速了解现有代码库的绝佳工具。当你向 Claude Code 或类似工具询问现有功能时，它们很可能会找到并阅读相关测试。</p>
<p>Agent 已经倾向于测试，但现有测试套件的存在几乎肯定会促使 agent 在后续修改中添加自己的测试。</p>
<p>每当我在新项目中启动与 agent 的新会话时，我都会首先提示类似以下内容：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">First run the tests</span><br></pre></td></tr></table></figure>

<p>对于我的 Python 项目，我已经将 <code>pyproject.toml</code> 配置好，这样我可以用更简短的提示：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">uv run pytest</span><br></pre></td></tr></table></figure>

<p>这四个词的提示有几个目的：</p>
<ol>
<li><p><strong>告诉 agent 存在测试套件</strong>，并迫使它找出如何运行测试。这几乎确保 agent 将来会运行测试以确保没有破坏任何东西。</p>
</li>
<li><p><strong>提供项目规模参考</strong> - 大多数测试工具会给 agent 一个粗略的测试数量指示。这可以作为项目规模和复杂程度的参考，也暗示 agent 如果想了解更多应该去查看测试本身。</p>
</li>
<li><p><strong>让 agent 进入测试心态</strong> - 运行测试后，agent 自然会扩展自己的测试。</p>
</li>
</ol>
<p>类似于”Use red&#x2F;green TDD”，”First run the tests”是一个四词提示，包含了已经融入模型的软件工程规范。</p>
<hr>
<p><strong>原文</strong>: <a href="https://simonwillison.net/guides/agentic-engineering-patterns/first-run-the-tests/">https://simonwillison.net/guides/agentic-engineering-patterns/first-run-the-tests/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI - Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>囤积你知道如何做的事情</title>
    <url>/posts/3296619331.html</url>
    <content><![CDATA[<h1><span id="囤积你知道如何做的事情">囤积你知道如何做的事情</span></h1><p>我与 coding agent 高效合作的许多建议都是我在职业生涯中发现有用的建议的扩展。这是一个很好的例子：<strong>囤积你知道如何做的事情</strong>。</p>
<p>构建软件技能的一个重要部分是理解什么是可能的，什么是不可能的，并且至少粗略地了解如何实现这些事情。</p>
<p>这些问题可以是广泛的，也可以是非常冷门的。网页可以仅用 JavaScript 运行 OCR 操作吗？iPhone 应用可以在应用不运行时与蓝牙设备配对吗？我们可以在 Python 中处理 100GB 的 JSON 文件而不先将整个文件加载到内存中吗？</p>
<p>你掌握的这类问题的答案越多，你就越有可能发现部署技术解决问题的机会，以其他人可能还没有想到的方式。</p>
<p>知道某件事在理论上是可能的，与亲眼看到它完成是不一样的。作为软件专业人士要培养的关键资产是深入收集这类问题的答案，最好通过运行代码来说明。</p>
<p>我以多种不同的方式囤积这样的解决方案。我的<strong>博客</strong>和<strong>TIL 博客</strong>挤满了我弄明白如何做的事情的笔记。我有<strong>一千多个 GitHub 仓库</strong>收集我为不同项目编写的代码，其中许多是展示关键想法的小型概念验证。</p>
<p>最近我使用 LLM 来帮助扩展我的代码解决方案集合，以解决有趣的问题。</p>
<p><strong>tools.simonwillison.net</strong> 是我最大的 LLM 辅助工具和原型集合。我用它来收集我所谓的<strong>HTML 工具</strong>——单个 HTML 页面，嵌入 JavaScript 和 CSS，解决特定问题。</p>
<p>我的 <strong>simonw&#x2F;research</strong> 仓库有更大、更复杂的示例，我在其中挑战 coding agent 研究问题并带回工作代码和详细说明其发现的书面报告。</p>
<h2><span id="重新组合你囤积的东西">重新组合你囤积的东西</span></h2><p>为什么要收集所有这些东西？除了帮助你建立和扩展自己的能力之外，你在此过程中生成的资产会成为你的 coding agent 的极其强大的输入。</p>
<p>我最喜欢的提示模式之一是告诉 agent 通过组合两个或更多现有工作示例来构建新东西。</p>
<p>一个有助于具体化这种有效性的项目是我添加到我的工具集合中的第一个东西——一个基于浏览器的<strong>OCR 工具</strong>，这里有更详细的描述。</p>
<p>我想要一个基于浏览器的简单工具，用于对 PDF 文件的页面进行 OCR——特别是完全由扫描图像组成且根本没有提供文本版本的 PDF。</p>
<p>我之前试验过在浏览器中运行 <strong>Tesseract.js OCR 库</strong>，发现它非常能干。该库提供成熟 Tesseract OCR 引擎的 WebAssembly 构建，并允许你从 JavaScript 调用它从图像中提取文本。</p>
<p>不过我不想处理图像，我想处理 PDF。然后我想起我也使用过 Mozilla 的 <strong>PDF.js</strong> 库，除其他功能外，它可以将 PDF 的单个页面转换为渲染图像。</p>
<p>我的笔记中有这两个库的 JavaScript 片段。</p>
<p>这是我输入到模型（当时是 Claude 3 Opus）的完整提示，结合了我的两个示例并描述了我正在寻找的解决方案：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">创建一个基于浏览器的 OCR 工具，可以处理 PDF 文件。</span><br><span class="line">使用 Tesseract.js 进行 OCR，使用 PDF.js 将 PDF 页面转换为图像。</span><br><span class="line">结合这两个库创建一个单一的工具。</span><br></pre></td></tr></table></figure>

<p>这完美无缺！模型踢出了一个概念验证页面，完全符合我的需要。</p>
<p>我最终与它迭代了几次以达到我的最终结果，但只花了几分钟就构建了一个真正有用的工具，我从那时起一直受益。</p>
<h2><span id="coding-agent-使其更强大">Coding agent 使其更强大</span></h2><p>我在 2024 年 3 月构建了那个 OCR 示例，比 Claude Code 的第一个发布早了近一年。Coding agent 使囤积工作示例更有价值。</p>
<p>如果你的 coding agent 有互联网访问，你可以告诉它做这样的事情：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">搜索如何用 curl 下载文件</span><br></pre></td></tr></table></figure>

<p>（我在那里指定了 <code>curl</code>，因为 Claude Code 默认使用 WebFetch 工具，它会总结页面内容而不是返回原始 HTML。）</p>
<p>Coding agent 擅长搜索，这意味着你可以在自己的机器上运行它们，并告诉它们在哪里找到你想要的东西的示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">搜索如何用 Python 处理大 JSON 文件</span><br></pre></td></tr></table></figure>

<p>通常这就足够了——agent 会启动一个搜索子 agent 来调查并拉回它实现任务所需的细节。</p>
<p>由于我的研究代码大部分都是公开的，我经常告诉 coding agent 将我的仓库克隆到 <code>/tmp</code> 并将它们用作输入：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">克隆 https://github.com/simonw/research 到 /tmp 并使用它作为输入</span><br></pre></td></tr></table></figure>

<p>这里的关键想法是，coding agent 意味着我们只需要弄清楚一个有用的技巧<strong>一次</strong>。如果这个技巧然后在某个地方有工作代码示例文档化，我们的 agent 可以咨询那个示例，并在未来用它解决任何类似形状的项目。</p>
<hr>
<p><strong>原文</strong>: <a href="https://simonwillison.net/guides/agentic-engineering-patterns/hoard-things-you-know-how-to-do/">https://simonwillison.net/guides/agentic-engineering-patterns/hoard-things-you-know-how-to-do/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI - Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>我如何使用 LLM 帮助我编写代码</title>
    <url>/posts/3181136312.html</url>
    <content><![CDATA[<h1><span id="我如何使用-llm-帮助我编写代码">我如何使用 LLM 帮助我编写代码</span></h1><p>2025 年 3 月 11 日</p>
<p>关于使用大型语言模型帮助编写代码的在线讨论不可避免地会产生来自经验令人失望的开发者的评论。他们经常问他们做错了什么——为什么有些人报告如此好的结果，而他们自己的实验却证明不足？</p>
<p>使用 LLM 编写代码是<strong>困难的</strong>和<strong>反直觉的</strong>。需要付出大量努力才能找出以这种方式使用它们的尖锐和柔和边缘，并且几乎没有指导帮助人们找出如何最好地应用它们。</p>
<p>如果有人告诉你用 LLM 编码<strong>很容易</strong>，他们（可能无意地）误导了你。他们可能偶然发现了有效的模式，但这些模式并不是对每个人都自然而然地出现。</p>
<p>我使用 LLM 编写代码已经两年多了，取得了很好的结果。这是我尝试将一些经验和直觉转移给你。</p>
<h2><span id="设定合理的期望">设定合理的期望</span></h2><p>忽略”AGI”炒作——LLM 仍然是花哨的自动完成。它们所做的只是预测一系列 token——但事实证明编写代码主要是关于以正确的顺序将 token 串在一起，所以它们可以<strong>极其</strong>有用，只要你将它们指向正确的方向。</p>
<p>如果你假设这项技术将完美地实现你的项目而不需要你自己运用任何技能，你很快就会失望。</p>
<p>相反，使用它们来<strong>增强</strong>你的能力。我目前最喜欢的心理模型是将它们视为一个过度自信的结对编程助手，他们查找东西的速度快如闪电，可以立即提出相关示例，并且可以毫无怨言地执行繁琐的任务。</p>
<p><strong>过度自信</strong>很重要。它们绝对会犯错误——有时是微妙的，有时是巨大的。这些错误可能是<strong>深刻非人类的</strong>——如果一个人类合作者幻觉了一个不存在的库或方法，你会立即失去对他们的信任。</p>
<p><strong>不要陷入拟人化 LLM 的陷阱，并假设会使人类信誉扫地的失败也应该以同样的方式使机器信誉扫地。</strong></p>
<p>与 LLM 一起工作时，你经常会发现它们无法做的事情。记下这些——它们是有用的教训！它们也是有价值的例子，可以为将来储存——新模型强大之处的标志是当它为以前模型无法处理的任务产生可用结果时。</p>
<h2><span id="考虑训练截止日期">考虑训练截止日期</span></h2><p>任何模型的一个关键特征是其<strong>训练截止日期</strong>。这是它们接受训练的数据停止收集的日期。对于 OpenAI 的模型，这通常是 2023 年 10 月或 2024 年 5 月。其他提供商可能有更近的日期。</p>
<p>这对代码<strong>极其</strong>重要，因为它会影响它们熟悉的库。如果你使用的库自 2023 年 10 月以来发生了重大突破，一些 OpenAI 模型可能不知道！</p>
<p>我从 LLM 获得了足够的价值，以至于我现在在选择库时故意考虑这一点——我坚持使用稳定性好且足够流行的库，这样它们的许多示例就会进入训练数据。我喜欢应用<strong>无聊技术</strong>的原则——在你的项目的独特卖点上创新，在其他方面坚持经过测试的解决方案。</p>
<p>LLM 仍然可以帮助你使用存在于其训练数据之外的库，但你需要投入更多工作——你需要在提示中向它们提供有关如何使用这些库的最新示例。</p>
<p>这带来了与 LLM 工作时需要了解的最重要的事情：</p>
<h2><span id="上下文为王">上下文为王</span></h2><p>从 LLM 获得好结果的大部分技巧都归结为管理其上下文——作为你当前对话一部分的文本。</p>
<p>这个上下文不仅仅是你输入给它的提示：成功的 LLM 交互通常采用对话的形式，上下文由来自你的每条消息<strong>和</strong>来自 LLM 的每条回复组成，这些存在于当前对话线程中。</p>
<p>当你开始新对话时，你会将该上下文重置为零。这很重要，因为对于已经停止有用的对话的修复通常是清空重新开始。</p>
<p>一些 LLM 编码工具超越了仅仅是对话。例如，Claude Projects 允许你用相当大量的文本预填充上下文——包括最近<strong>直接从 GitHub 仓库导入代码</strong>的能力，我正在<strong>大量</strong>使用。</p>
<p>像 Cursor 和 VS Code Copilot 这样的工具会自动包含来自当前编辑器会话和文件布局的上下文，你有时可以使用像 <strong>Cursor 的 @commands</strong> 这样的机制来拉入额外的文件或文档。</p>
<p>我主要直接使用 <strong>ChatGPT</strong> 和 <strong>Claude</strong> Web 或应用程序界面的原因之一是，它使我更容易理解到底是什么进入了上下文。对我隐瞒上下文的 LLM 工具<strong>不太</strong>有效。</p>
<p>你可以利用之前的回复也是上下文的一部分这一事实来获得优势。对于复杂的编码任务，尝试让 LLM 先写一个更简单的版本，检查它是否有效，然后迭代构建更复杂的实现。</p>
<p>我经常通过转储现有代码来开始新聊天，以种子化该上下文，然后与 LLM 一起工作以某种方式修改它。</p>
<p>我最喜欢的代码提示技术之一是放入几个与我想构建的东西相关的完整示例，然后提示 LLM 使用它们作为新项目的灵感。当我<strong>描述我的 JavaScript OCR 应用程序</strong>时，我详细写了这一点，该应用程序结合了 Tesseract.js 和 PDF.js——这两个库我过去使用过，并且可以在提示中提供工作示例。</p>
<h2><span id="向它们询问选项">向它们询问选项</span></h2><p>我的大多数项目都以一些开放性问题开始：我试图做的事情是否可行？我可以实现它的潜在方式有哪些？这些选项中哪些是<strong>最好的</strong>？</p>
<p>我在这个初始研究阶段使用 LLM。</p>
<p>我会使用像”Rust 中 HTTP 库的选项是什么？包括使用示例”这样的提示——或者”JavaScript 中一些有用的拖放库是什么？为我构建一个演示每个库的工件”（对 Claude）。</p>
<p>训练截止日期在这里是相关的，因为它意味着不会建议更新的库。通常没关系——我不想要最新的，我想要最稳定的，并且已经存在足够长的时间，bug 已经被解决。</p>
<p>如果我要使用一些更新的东西，我会在 LLM 世界之外自己做这个研究。</p>
<p>开始任何项目的最好方法是从一个证明该项目关键要求可以满足的原型开始。我经常发现 LLM 可以让我在坐在笔记本电脑前的几分钟内——有时甚至是在手机上工作时——达到那个可工作的原型。</p>
<h2><span id="准确告诉它们要做什么">准确告诉它们要做什么</span></h2><p>在完成初始研究后，我戏剧性地改变了模式。对于生产代码，我的 LLM 使用更加独裁：我将其视为数字实习生，根据我的详细说明为我输入代码。</p>
<p>这是一个最近的示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">使用 asyncio httpx 编写一个具有以下签名的 Python 函数：</span><br><span class="line"></span><br><span class="line">async def download_db(url, max_size_bytes=5 * 1025 * 1025): -&gt; pathlib.Path</span><br><span class="line"></span><br><span class="line">给定一个 URL，这会将数据库下载到临时目录并返回它的路径。但它会在开始流式传输该数据时检查内容长度标头，如果超过限制，则抛出错误。当下载完成时，它使用 sqlite3.connect(...) 然后运行 PRAGMA quick_check 来确认 SQLite 数据有效——如果无效则抛出错误。最后，如果内容长度标头对我们撒谎——如果它说 2MB 但我们下载了 3MB——我们一注意到这个问题就会抛出错误。</span><br></pre></td></tr></table></figure>

<p>我可以自己编写这个函数，但这需要我大部分十五分钟来查找所有细节并使代码正常工作。LLM 在 15 秒内就完成了。</p>
<p>我可以让它编写测试来确认它有效。然后我可以要求它添加类型提示、文档字符串，甚至根据代码生成 Markdown 文档。</p>
<p>我可以让它编写一个脚本来自动化整个过程。我可以要求它考虑边缘情况、错误处理、性能优化。</p>
<h2><span id="让它们写出它们的工作">让它们写出它们的工作</span></h2><p>让 LLM 编写它们的工作文档是一种确保它们实际做了它们说它们做的事情的好方法。</p>
<p>我最近构建了一个名为 <strong>Showboat</strong> 的工具，用于帮助 coding agent 编写演示它们工作的文档。</p>
<p>这是一个我经常使用的提示：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">使用 showboat 在仓库中创建 walkthrough.md 文件，并在其中构建演示，使用 showboat note 添加注释，使用 showboat exec 加上 sed 或 grep 或 cat 或任何你需要的工具来包含你正在谈论的代码片段</span><br></pre></td></tr></table></figure>

<p><strong>Showboat</strong> 是我构建的帮助 coding agent 编写演示他们工作的文档的工具。你可以在这里看到 <code>showboat --help</code> 输出，它旨在为模型提供使用该工具所需的一切。</p>
<p><code>showboat note</code> 命令向文档添加 Markdown。<code>showboat exec</code> 命令接受 shell 命令，执行它然后将命令和输出都添加到文档中。</p>
<p>通过告诉它使用”sed 或 grep 或 cat 或任何你需要的东西来包含你正在谈论的代码片段”，我确保 Claude Code 不会手动将代码片段复制到文档中，因为这可能会引入幻觉或错误的风险。</p>
<p>这非常有效。这是 Claude Code 用 Showboat 创建的文档，它详细讲解了所有六个 <code>.swift</code> 文件，并提供了关于代码如何工作的清晰且可操作的解释。</p>
<p>通过阅读这个文档，我学到了很多关于 SwiftUI 应用如何构建的知识，并吸收了一些关于 Swift 语言本身的可靠细节。</p>
<p>如果你担心 LLM 可能会降低你学习新技能的速度，我强烈建议采用像这样的模式。即使是一个大约 40 分钟的 vibe coded 玩具项目也可以成为探索新生态系统和学习一些有趣新技巧的机会。</p>
<hr>
<p><strong>原文</strong>: <a href="https://simonwillison.net/2025/Mar/11/using-llms-for-code/">https://simonwillison.net/2025/Mar/11/using-llms-for-code/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI - Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>线性演示</title>
    <url>/posts/2002830531.html</url>
    <content><![CDATA[<h1><span id="线性演示">线性演示</span></h1><p>有时让 coding agent 为你提供代码库的结构化演示很有用。</p>
<p>也许这是你需要了解的现有代码，也许是你自己写的但忘记了细节的代码，或者也许你是通过 vibe coding 完成的，需要了解它实际是如何工作的。</p>
<p>拥有合适 agent harness 的前沿模型可以构建详细的演示来帮助你理解代码是如何工作的。</p>
<h2><span id="使用-showboat-和-present-的示例">使用 Showboat 和 Present 的示例</span></h2><p>我最近用 Claude Code 和 Opus 4.6 在 Mac 上 vibe coded 了一个 SwiftUI 幻灯片演示应用。</p>
<p>我将在 2025 年 11 月到 2026 年 2 月之间的前沿模型进展演讲，我喜欢在演讲中至少包含一个噱头（STAR 时刻——Something They’ll Always Remember，他们总会记住的东西）。在这种情况下，我决定噱头是在演讲结束时揭示幻灯片机制本身就是 vibe coding 可以做什么的示例。</p>
<p>我将代码发布到 GitHub，然后意识到我实际上不知道它是如何工作的——我是通过提示让它存在的（这里有部分记录），而没有关注它编写的代码。</p>
<p>所以我启动了一个新的 Claude Code for web 实例，指向我的仓库并提示：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">阅读源代码，然后规划一个线性的代码演示，</span><br><span class="line">详细解释它是如何工作的</span><br><span class="line"></span><br><span class="line">然后运行 &quot;uvx showboat --help&quot; 来学习 showboat - </span><br><span class="line">使用 showboat 在仓库中创建 walkthrough.md 文件，</span><br><span class="line">并在其中构建演示，使用 showboat note 添加注释，</span><br><span class="line">使用 showboat exec 加上 sed 或 grep 或 cat 或任何你需要的工具</span><br><span class="line">来包含你正在谈论的代码片段</span><br></pre></td></tr></table></figure>

<p><strong>Showboat</strong> 是我构建的帮助 coding agent 编写演示他们工作的文档的工具。你可以在这里看到 <code>showboat --help</code> 输出，它旨在为模型提供使用该工具所需的一切。</p>
<p><code>showboat note</code> 命令向文档添加 Markdown。<code>showboat exec</code> 命令接受 shell 命令，执行它然后将命令和输出都添加到文档中。</p>
<p>通过告诉它使用”sed 或 grep 或 cat 或任何你需要的东西来包含你正在谈论的代码片段”，我确保 Claude Code 不会手动将代码片段复制到文档中，因为这可能会引入幻觉或错误的风险。</p>
<p>这非常有效。这是 Claude Code 用 Showboat 创建的文档，它详细讲解了所有六个 <code>.swift</code> 文件，并提供了关于代码如何工作的清晰且可操作的解释。</p>
<p>通过阅读这个文档，我学到了很多关于 SwiftUI 应用如何构建的知识，并吸收了一些关于 Swift 语言本身的可靠细节。</p>
<p>如果你担心 LLM 可能会降低你学习新技能的速度，我强烈建议采用像这样的模式。即使是一个大约 40 分钟的 vibe coded 玩具项目也可以成为探索新生态系统和学习一些有趣新技巧的机会。</p>
<hr>
<p><strong>原文</strong>: <a href="https://simonwillison.net/guides/agentic-engineering-patterns/linear-walkthroughs/">https://simonwillison.net/guides/agentic-engineering-patterns/linear-walkthroughs/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI - Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>线性 walkthroughs</title>
    <url>/posts/1739414639.html</url>
    <content><![CDATA[<h1><span id="线性-walkthroughs-agentic-engineering-patterns-simon-willison-的-weblog">线性 walkthroughs - Agentic Engineering Patterns - Simon Willison 的 Weblog</span></h1><p>有时，让编码代理为你提供代码库的结构化 walkthrough 是很有用的。</p>
<p>也许是你需要快速了解的现有代码，也许是你自己写的但忘记了细节的代码，或者也许你是 vibe coded 整个项目，需要了解它实际是如何工作的。</p>
<p>具有合适 agent harness 的前沿模型可以构建详细的 walkthrough，帮助你理解代码是如何工作的。</p>
<h2><span id="使用-showboat-和-present-的示例">使用 Showboat 和 Present 的示例</span></h2><p>我最近在我的 Mac 上使用 Claude Code 和 Opus 4.6 <a href="https://simonwillison.net/2026/Feb/25/present/">vibe coded 了一个 SwiftUI 幻灯片演示应用</a>。</p>
<p>我当时在谈论 2025 年 11 月到 2026 年 2 月之间前沿模型的进展，我喜欢在我的演讲中至少包含一个噱头（一个 <a href="https://simonwillison.net/2019/Dec/10/better-presentations/">STAR moment</a> - Something They’ll Always Remember，他们将永远记住的时刻）。在这种情况下，我决定噱头是在演示结束时揭示幻灯片机制本身就是 vibe coding 能做什么的一个例子。</p>
<p>我将代码发布 <a href="https://github.com/simonw/present">到 GitHub</a>，然后意识到我对它实际如何工作一无所知——我通过提示让它整个存在（<a href="https://gisthost.github.io/?bfbc338977ceb71e298e4d4d5ac7d63c">部分转录在这里</a>），而没有注意它编写的代码。</p>
<p>所以我启动了一个新的 Claude Code for web 实例，指向我的仓库并提示：</p>
<p><a href="https://github.com/simonw/showboat">Showboat</a></p>
<p>是我构建的一个工具，用于帮助编码代理编写展示他们工作的文档。你可以看到</p>
<p><a href="https://github.com/simonw/showboat/blob/main/help.txt">showboat –help 输出在这里</a></p>
<p>，它旨在为模型提供使用该工具所需的一切信息。</p>
<p><code>showboat note</code> 命令将 Markdown 添加到文档中。<code>showboat exec</code> 命令接受一个 shell 命令，执行它然后将命令及其输出都添加到文档中。</p>
<p>通过告诉它使用”sed 或 grep 或 cat 或任何你需要包含你正在谈论的代码片段的东西”，我确保 Claude Code 不会手动将代码片段复制到文档中，因为这可能会引入幻觉或错误的风险。</p>
<p>这效果非常好。这是 <a href="https://github.com/simonw/present/blob/main/walkthrough.md">Claude Code 使用 Showboat 创建的文档</a>，它详细讲解了所有六个 <code>.swift</code> 文件，并提供了关于代码如何工作的清晰且可操作的解释。</p>
<p>仅仅通过阅读这个文档，我就学到了很多关于 SwiftUI 应用如何构建的知识，并吸收了一些关于 Swift 语言本身的扎实细节。</p>
<p>如果你担心 LLM 可能会降低你学习新技能的速度，我强烈建议采用像这样的模式。即使是一个约 40 分钟的 vibe coded 玩具项目，也可以成为探索新生态系统并掌握一些有趣新技巧的机会。</p>
<hr>
<p><strong>原文链接</strong>: <a href="https://simonwillison.net/guides/agentic-engineering-patterns/linear-walkthroughs/">https://simonwillison.net/guides/agentic-engineering-patterns/linear-walkthroughs/</a></p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Technology</tag>
      </tags>
  </entry>
  <entry>
    <title>自主编码代理：Codex 示例</title>
    <url>/posts/1018842827.html</url>
    <content><![CDATA[<p><em>原文来源：Martin Fowler</em></p>
<p><em>抓取时间：2026-03-11 20:40:01</em></p>
<hr>
<h1><span id="自主编码代理codex-示例">自主编码代理：Codex 示例</span></h1><p>Birgitta 是 Thoughtworks 的杰出工程师（Distinguished Engineer）和 AI 辅助交付专家。她拥有超过 20 年的软件开发、架构师和技术领导者经验。</p>
<p>本文属于”探索生成式 AI（Exploring Gen AI）”系列。该系列记录了 Thoughtworks 技术专家探索将生成式 AI（gen ai）技术用于软件开发的过程。</p>
<p>2025 年 6 月 4 日</p>
<p>在过去的几周里，多个”自主后台编码代理（autonomous background coding agents）”相继发布。</p>
<ul>
<li>监督式编码代理（Supervised coding agents）：由开发者驱动和指导的交互式聊天代理。在本地 IDE 中创建代码。工具示例：GitHub Copilot、Windsurf、Cursor、Cline、Roo Code、Claude Code、Aider、Goose、…</li>
<li>自主后台编码代理（Autonomous background coding agents）：无头（headless）代理，你可以派遣它们自主完成整个任务。代码在为该代理专门启动的环境中创建，通常结果是生成一个拉取请求（pull request）。其中一些也可以在本地运行。工具示例：OpenAI Codex、Google Jules、Cursor 后台代理、Devin、…<br>我给 OpenAI Codex 和其他一些代理分配了一个任务，看看能学到什么。以下是对一次特定 Codex 运行的记录，帮助你了解幕后情况并得出自己的结论，随后是我自己的一些观察。</li>
</ul>
<h2><span id="任务">任务</span></h2><p>我们有一个名为 Haiven 的内部应用程序，用作软件交付提示库（prompt library）的演示前端，并在软件团队上尝试不同的 AI 辅助体验。该应用程序的代码是公开的。</p>
<p>我给 Codex 的任务是关于以下 UI 问题：</p>
<p>实际情况：</p>
<p>我希望代理实现的效果：</p>
<p>提示（Prompt）：当我们在仪表板上创建过滤标签时，我们会根据已有的类别创建人类可读的标签。”client-research”被转换为”Client-Research”，”deliveryManagement”被转换为”DeliveryManagement”。请改进”类别转人类可读（category-to-human-readable）”逻辑的复杂度，使标签向用户显示为”Client Research”和”Delivery Management”</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">When we create the filter labels on the dashboard, we create human readable labels based on the categories we have. &quot;client-research&quot; is turned into &quot;Client-Research&quot;, &quot;deliveryManagement&quot; into &quot;DeliveryManagement&quot;. Improve the sophistication of the &quot;category-to-human-readable&quot; logic so that the labels are appearing to the user as &quot;Client Research&quot; and &quot;Delivery Management&quot;</span><br></pre></td></tr></table></figure>

<p>这个任务是</p>
<ul>
<li>相对较小</li>
<li>典型的” cosmetic（外观修饰）”任务，不太紧急，可能会一再被降低优先级</li>
</ul>
<h2><span id="代理日志">代理日志</span></h2><p>我在这里分享详细的日志，以便你能了解幕后正在发生的事情。</p>
<p>以下是 Codex 中日志视图的样子：</p>
<p>首先收集信息</p>
<p>前 17 条日志条目都是关于读取 AGENTS.md 和 README.md 的：</p>
<ul>
<li>“我需要检查仓库根目录中的 AGENTS.md 以获取指令。”</li>
<li>查找 AGENTS.md，但我实际上将其命名为 AGENT.md，这导致了一些来回反复，直到它弄明白</li>
<li>读取 README.md</li>
<li>查看 .pre-commit，找到一个测试设置<br>以下表格逐字列出了其余的日志消息。在这些消息之后，代理执行了一个命令，其中一些我添加了实际命令以供参考。我确信这些日志消息只是幕后实际发生的 token 的摘要，因为这些消息太短了，无法代表模型必须进行的完整”思考”。</li>
</ul>
<p>使用大量 grep 搜索相关代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep -R &quot;human readable&quot; -n</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep -R &quot;human-readable&quot; -n ui/src \| head</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep -R &quot;Human&quot; -n \| head</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep -R &quot;human&quot; app -n \| head</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep -R &quot;human&quot; app \| wc -l</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">wc</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">head</span><br></pre></td></tr></table></figure>

<p>终于，接近目标了！标签之前是通过 CSS 粗略地大写的，它找到了</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;br&gt;root@0c5de6379b32:/workspace/haiven# grep -R &quot;capitalize&quot; -n ui/src \| head&lt;br&gt;ui/src/pages/index.js:250:                                    className=&quot;capital&lt;br&gt;ize&quot;&lt;br&gt;ui/src/styles/globals.css:1915:.capitalize &#123;&lt;br&gt;ui/src/styles/globals.css:1916:  text-transform: capitalize;&lt;br&gt;ui/src/styles/globals.css:1931:  text-transform: capitalize;&lt;br&gt;</span><br></pre></td></tr></table></figure>

<p>现在它开始在 app 文件夹中检查更多相关代码，那里是后端所在。它没有找到任何内容，这是正确的，那里没有相关的内容</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">app</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep -R &quot;categories&quot; -n app \| head</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep -R &quot;client&quot; -n app \| head</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep -R &quot;categories:&quot; -n app/tests \| head</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep -R &quot;client-research&quot; -n</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sed -n &#x27;1,100p&#x27; ui/src/app/_navigation_items.js</span><br></pre></td></tr></table></figure>

<p>现在它回到 ui 文件夹中搜索——它之前找到了 CSS，现在正在寻找可以重用来解决任务的现有代码。这很好，因为我们确实已经有一个类似的函数，它找到了：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep -R &quot;to.*Readable&quot; -n ui/src \| head</span><br></pre></td></tr></table></figure>

<p>它现在发现了现有函数实现中的一个缺口：</p>
<p>更新它想要更改的函数的测试：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git status --short</span><br></pre></td></tr></table></figure>

<p>你可以在 Codex 中配置一些环境启动命令，我将其配置为 yarn install，这导致了以下情况：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">yarn install</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git log --oneline -n 1</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep yarn .gitignore</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cat ui/.gitignore</span><br></pre></td></tr></table></figure>

<p>实际代码更改：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sed -n &#x27;1,30p&#x27; ...</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git diff</span><br></pre></td></tr></table></figure>

<p>接下来运行我们的代码 linter，很好：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cd ui &amp;&amp; npx prettier -w ...</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">prettier</span><br></pre></td></tr></table></figure>

<p>尝试运行测试——未成功：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Error: Not implemented: window.getComputedStyle(elt, pseudoElt)</span><br></pre></td></tr></table></figure>

<p>开始偏离主题去设置 venv，这完全没有必要，因为它只计划更改前端代码：</p>
<p>放弃了测试，现在完成更改：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">nl -ba ui/src/pages/index.js \| sed -n &#x27;12,28p&#x27;</span><br></pre></td></tr></table></figure>

<p>以下是 Codex 创建的完整拉取请求（pull request）。</p>
<h2><span id="观察">观察</span></h2><h3><span id="暴力文本搜索">暴力文本搜索</span></h3><p>我发现有趣的是，Codex 和我使用的大多数其他编码助手越来越倾向于使用简单的文本搜索来查找相关代码。在上面的日志中，你可以看到 Codex 如何通过 grep 使用一堆搜索词来查找相关代码片段（”human”、”human readable”、”humanReadable”、…）。我觉得有趣的原因是，已经有很多看似更复杂的代码搜索机制被实现，比如使用向量&#x2F;嵌入（vectors &#x2F; embeddings）对代码库索引进行语义搜索（Cursor、GH Copilot、Windsurf），或使用抽象语法树（abstract syntax tree）作为起点（Aider、Cline）。后者仍然相当简单，但使用 grep 进行文本搜索是最简单的。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">grep</span><br></pre></td></tr></table></figure>

<p>似乎工具创作者发现这种简单的搜索毕竟仍然是最有效的——？或者他们在这里做了某种权衡，在简单性和有效性之间？</p>
<h3><span id="远程开发环境是这些代理后台工作的关键">远程开发环境是这些代理”后台”工作的关键</span></h3><p>以下是 Codex 环境配置屏幕的截图（截至 2025 年 5 月底）。截至目前，你可以配置容器镜像、环境变量、密钥和启动脚本。他们指出，在执行该启动脚本后，环境将不再能够访问互联网，这将沙箱化（sandbox）环境并减轻一些安全风险。</p>
<p>对于这些”自主后台代理”，为代理设置的远程开发环境（remote dev environment）的成熟度至关重要，这是一个棘手的挑战。例如在这种情况下，Codex 未能运行测试。</p>
<p>事实证明，当创建拉取请求时，确实有两个测试因回归（regression）而失败，这很遗憾，因为如果它知道的话，本可以轻松地修复测试，这是一个微不足道的修复：</p>
<p>这个名为 Haiven 的特定项目实际上有一个脚本化的开发者安全网，形式是相当详细的 .pre-commit 配置。（）如果代理能在创建拉取请求之前执行完整的 pre-commit，那就理想了。然而，要运行所有步骤，它需要运行</p>
<ul>
<li>Node 和 yarn（运行 UI 测试和前端 linter）</li>
<li>Python 和 poetry（运行后端测试）</li>
<li>Semgrep（用于安全相关的静态代码分析）</li>
<li>Ruff（Python linter）</li>
<li>Gitleaks（密钥扫描器）<br>……当然，所有这些都必须是正确的版本。</li>
</ul>
<p>如果你想真正将这些代理”后台”运行而不是在开发者机器上运行，那么找出一种流畅的体验来为代理启动恰好合适的环境是这些代理产品的关键。这不是一个新问题，在某种程度上已经是一个已解决的问题，毕竟我们一直在 CI 流水线中这样做。但这也不简单，目前我的印象是，环境成熟度在大多数这些产品中仍然是一个问题，而且配置和测试环境设置的用户体验与 CI 流水线一样令人沮丧，甚至更糟。</p>
<h3><span id="解决方案质量">解决方案质量</span></h3><p>我在 OpenAI Codex 中运行了相同的提示 3 次，在 Google 的 Jules 中运行了 1 次，在 Claude Code 中本地运行了 2 次（虽然它不是完全自主的，我需要手动对所有内容说”是”）。尽管这是一个相对简单的任务和解决方案，但结果之间存在质量差异。</p>
<p>首先是好消息，代理每次都提出了一个可行的解决方案（撇开破坏性回归测试不谈，老实说我并没有实际运行每一个解决方案来确认）。我认为这个任务是生成式 AI 代理已经能够很好地独立工作的任务类型和大小的一个很好的例子。但在解决方案质量方面有两个方面存在差异：</p>
<ul>
<li>发现可重用的现有代码：在此日志中你会发现 Codex 发现了一个现有组件，即”动态数据渲染器（dynamic data renderer）”，它已经具有将技术键（technical keys）转换为人类可读版本的功能。在我进行的 6 次运行中，只有 2 次相应的代理发现了这段代码。在其他 4 次中，代理创建了一个带有新函数的新文件，这导致了代码重复。</li>
<li>发现应该使用此逻辑的额外位置：团队目前正在开发一个新功能，也在下拉菜单中向用户显示类别名称。在 6 次运行中的 1 次中，代理实际上发现了这一点，并建议也更改该位置以使用新功能。<br>我将这些结果放入一个表格中，以说明在分配给代理的每个任务中，我们有多个质量维度，即我们希望”正确”的事情。每次代理运行都可能在其中一个或多个维度上”出错”，维度越多，代理按照我们想要的方式完成所有事情的可能性就越低。</li>
</ul>
<h3><span id="沉没成本谬误sunk-cost-fallacy">沉没成本谬误（Sunk cost fallacy）</span></h3><p>我一直在想——假设一个团队将后台代理用于这类任务，即那些有点小、既不重要也不紧急的任务类型。Haiven 是一个面向内部的应用程序，目前只分配了两名开发者，所以这类外观修饰修复实际上被认为是低优先级的，因为它会占用开发者的精力去做更重要的事情。当代理只是部分成功而不是完全成功时——团队会在什么情况下丢弃拉取请求，在什么情况下会投入时间完成最后 20%，即使在这上面花费精力已经被降低优先级？这让我想知道我们最终可能会得到多少未优先处理的尾部工作。</p>
<p>最新文章（3 月 4 日）：</p>
<p>软件工程循环中的人类与代理（Humans and Agents in Software Engineering Loops）</p>
<p>上一篇文章：</p>
<p>使用 LLM 构建自定义工具（Building Custom Tooling with LLMs）</p>
<p>下一篇文章：</p>
<p>我仍然关心代码（I still care about the code）</p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>Technology</tag>
      </tags>
  </entry>
</search>
